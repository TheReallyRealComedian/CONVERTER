# constraints.txt — OKTOBER P2 (2026-08-22): Einfrieren der zehn ungepinnten
# transitiven Pakete auf die Versionen des letzten Python-3.10-Builds.
#
# WAS: Diese zehn Pakete stehen nicht in requirements.txt (sie kommen über
# unstructured[all-docs], torch und Co.). Auf Python 3.10 waren sie ohnehin
# eingefroren, weil ihre neueren Releases 3.10 nicht mehr beliefern (numpy
# 2.3+ / pandas 3.0 / scipy 1.16+ / matplotlib 3.11 / onnxruntime 1.24+ /
# unstructured-inference 1.3+ verlangen >= 3.11). Mit dem Umzug auf 3.12
# würden sie sich von selbst bewegen — niemand hätte das bestellt.
#
# WARUM: Der Umzug soll beweisbar NUR Python + Playwright sein (Fehlersuche
# in einem Zwei-Elemente-Raum), und der Konvertierungs-Stapel (unstructured-
# inference 1.2.0 -> 1.6.13, pandas 2.3 -> 3.0) muss byte-gleiche Ausgaben
# liefern — sonst wäre ein Engine-Generation-Bump fällig, der Olis
# gespeicherte Konvertierungen entwertet. Alle zehn alten Versionen haben
# cp312-Wheels (gegen PyPI geprüft, Sprint-Bericht OKTOBER P1).
#
# ABBAU: ein eigenes BACKLOG-Item (DEPS-FLOAT); nicht „bei Gelegenheit"
# lockern. Wird in beiden pip-Installs des Dockerfiles per -c gezogen.
contourpy==1.3.2
filelock==3.14.0
matplotlib==3.10.9
networkx==3.4.2
numpy==2.2.6
onnxruntime==1.23.2
pandas==2.3.3
scipy==1.15.3
unstructured-client==0.42.12
unstructured-inference==1.2.0
