# STATUS

**Stand**: 2026-06-20
**Live**: `localhost:5656` im Docker-Stack auf Mintbox + `converter.smallpieces.de`. Pytest **352/352** grün (Mac-Host; Container-Verify durch Master) — 217→262 durch MCP1 (P1 +16 List/Single-Reader, P2 +29 recorded_at), 262→273 durch MCP1-FIX (+11 Diktiergerät-Dialekt `YYMMDD_NNNN` + Präzedenz-Flip; **MCP1 + MCP1-FIX ☑ done 2026-06-14**); 273→277 durch **R2-H** Flache Vier-Orte-IA (netto +4: IA-Tests neu/umgebaut, `?status`-Filter-Tests retired; **R2-H ☑ done + Mintbox-deployed 2026-06-18**); 277→336 durch **R4-LEARN** SR-/Recall-Layer (+59: Schema/`before_delete`/recent · Token-Write+Reads · FSRS-Engine/Swap · Rate/Annotate/Review-UI; **R4-LEARN ☑ done 2026-06-19**, Mintbox-Deploy offen — `fsrs` neu + `CARD_TOKEN` setzen, keine Migration); 336→340 durch **R4-LEARN-FIX** (+4: Card-`DELETE /api/cards/<id>` Session/ORM-Cascade + „Löschen" im Review-Footer; **☑ done 2026-06-20**, im selben Mintbox-Deploy, keine Migration); 340→352 durch **R4-LEARN-P6** (+12: Token-`PATCH /api/highlights/<id>/annotate` für Agent-Tags/Notiz auf bestehenden Highlights; **☑ done 2026-06-20**, keine Migration). **R2-F (Reader-Abschluss-Leiste + „Gelesen"-Label + Server-Forward-Clamp → `c4e5b7f`) + R2-G (Bar = furthest-read + „Als ungelesen"-Reset, bewusste Revision der R2-F-Bar-Entscheidung → `ec36c8b`) Code fertig + lokal gesmoked (2026-06-13); Mintbox-Deploy beider in EINEM Schritt offen (Olivers Schritt: reiner Code-Deploy, KEINE Migration — `last_read_percent` existiert seit R2-B; danach Browser-Hard-Reload). Damit R2-Cluster A–G komplett.** **R2-E (Library-IA „Readwise-3er" + Tag-Leiste + Tag-Daten) ist live auf der Mintbox** — Deploy + `scripts/cleanup_tags.py --apply` am 2026-06-13 gelaufen (Tag-Bestand **102→97**: 5 Merges / 2 Renames, 0 Deletes; idempotenter Re-Lauf 97→0 bestätigt). Vorher: Mintbox-Deploy R2-C + R2-D erfolgt (2026-06-12, beide Inline-Migrationen gegen die Prod-DB gelaufen).

**Mintbox-Deploy-Stand 2026-06-14 (authoritativ — supersedet alle „Deploy steht aus"-Notizen oben + in den Dispatch-Absätzen)**: Der gesamte aufgelaufene Bundle ist **live** auf der Mintbox — **R2-F + R2-G + VIS1 + VIS2 + MCP1 + MCP1-FIX** (`git pull` + `docker compose up -d --build`, keine Migration). Der MCP1-FIX-**Backfill der 5 echten Diktate lief erfolgreich** (id 28–32, dry-run→apply→0-Row-Idempotenz live bestätigt; tragen jetzt `metadata.recorded_at` aus dem Dateinamen). **MCP1-P3 browser-E2E-Smoke ☑ done 2026-06-15**: echtes Diktat `260409_0144.MP3` → Upload→Transkript→Save (id 40), via converter-mcp `get_transcript(40)` gegen Live-Prod bestätigt (`recorded_at=2026-04-09T00:00:00+02:00`, `source='filename'`, Transkript da). **Damit kein offenes CONVERTER-Item mehr** — der ganze Reader-/MCP-Bogen (R2-A…G + VIS1/2 + MCP1 + MCP1-FIX) ist deployt + verifiziert.

**R2-H Mintbox-deployed 2026-06-18** (`git pull` + `up -d --build`, keine Migration — Orte aus Bestandsspalten abgeleitet): die flache Vier-Orte-IA ist live. **Post-Deploy-Health-Check via converter-mcp** (Master, gegen Live-Prod): GET-API + `to_dict` intakt (sauber strukturierte Antwort); Daten vollständig (`list_audio_transcripts include_archived` = **8 Transkripte**, alle vorhanden); `recorded_at` läuft weiter auf neuen Diktaten (id 43 `260618_0190.MP3` → `2026-06-18` `source=filename`). Un-archivierte Triage-Liste = leer, weil alle Transkripte verarbeitet/archiviert sind (intendierter Workflow). **converter-mcp damit live-verifiziert R2-H-kompatibel.**

**Dispatcht 2026-06-19**: **R4-LEARN** SR-/Recall-Layer (L) → [docs/archive/sprint-prompts/SPRINT_R4-LEARN_sr-recall-layer_2026-06-19.md](docs/archive/sprint-prompts/SPRINT_R4-LEARN_sr-recall-layer_2026-06-19.md). Spaced-Repetition-Lernkarten über die Highlights — das ursprüngliche Readwise-Herz. Aus einem 2×-code-review'ten Produkt-Brief abgeleitet (Master fand u.a. den **`ON DELETE SET NULL`-inert-Trap**: kein FK-Pragma → Lösch-Mechanik muss ORM-`before_delete`-Event sein). **Teilung**: Agent erzeugt Karten (Token-Write-API), CONVERTER speichert/plant (FSRS via `py-fsrs`, swappable)/zeigt (Review-UI); Reads + Rate = `@login_required`. Neue Tabellen `card`/`review`/`card_tags` via `db.create_all` (kein Migration-Aufwand); `Highlight.created_at` existiert+indexed (since-Reader direkt). Wissenslandkarte raus (spätere Phase). 5 Phasen (Schema → Write-API → FSRS-Engine+Rate → Review-UI → Wrap), Engine vor UI. converter-mcp-Tool-Wrapping = Koordinator-Scope. **☑ done 2026-06-19** (P1 `ded0dc4` · P2 `41bb945` · P3 `881071f` · P4 `647195b` · Doc-Wrap dieser Commit — Detail unter „Aktueller Sprint"; Live-Smoke dark+light/0-Console-Errors, pytest 336; Mintbox-Deploy + converter-mcp-Wrapping = Olis/Koordinator-Schritte).

**R4-LEARN live end-to-end 2026-06-19**: Mintbox-deployed (`CARD_TOKEN` in beiden `.env`), converter-mcp um 6 Tools erweitert (Koordinator: 4 Session-Reads + 2 Bearer-Writes, `review`/`annotate` UI-only nicht gewrappt), Live-Smoke grün (POST /api/cards → 201, Karte id=1, `due_count=1`, Round-trip). Kontrakt-Doc [docs/card_api_contract.md](docs/card_api_contract.md). **Dabei Lücke gefunden → R4-LEARN-FIX dispatcht**: kein Card-DELETE (Agent erzeugt/User bewertet, „User löscht" vergessen) → [docs/archive/sprint-prompts/SPRINT_R4-LEARN-FIX_card-delete_2026-06-19.md](docs/archive/sprint-prompts/SPRINT_R4-LEARN-FIX_card-delete_2026-06-19.md): `DELETE /api/cards/<id>` (Session, ORM-Cascade) + Lösch-Affordanz im Review-Footer, XS–S, **☑ done 2026-06-20** (P1 `e8fee36` + P2 Doc-Wrap dieser Commit — Detail unter „Aktueller Sprint"). Hand-SQL verboten (FK-Pragma-inert).

**Dispatcht 2026-06-15**: **R2-H** Flache Vier-Orte-IA → [docs/archive/sprint-prompts/SPRINT_R2-H_flat-four-place-ia_2026-06-15.md](docs/archive/sprint-prompts/SPRINT_R2-H_flat-four-place-ia_2026-06-15.md). Ping-Pong mit Oliver ergab: die drei Bedien-Achsen (Ort/Lese-Liste-Flag/Favorit) + zwei Nav-Listen sind verwirrend → Kollaps auf **eine flache Achse, vier exklusive Orte (Inbox · Lese-Liste · Bibliothek · Archiv)**, identisch als Tabs + Karten-Move-Control + Detail. „Später"/Favoriten-Stern/Queue-Flag raus, Weiterlesen-Sektion raus. **Kein Schema-Touch** (Orte aus `lifecycle_status`+`queue_position` abgeleitet), Archivieren dequeued, „alles" via Suche, Lese-Fortschritt bleibt orthogonal. M, 3 Phasen. **☑ done 2026-06-18** (P1 `82e786c` · P2A `fa759b3` · P2B `fd20ec6` · Wrap dieser Commit — Detail unter „Aktueller Sprint"). **Master-Umlenkung in 2A**: der globale Finder triggert auf **jeden** Content-Filter (search **OR** tag **OR** type), nicht nur Suche. Danach erst die **R3/R4-Gabelung** (Web-Article-Save vs. Spaced-Repetition, je Workshop nötig).

**Dispatcht 2026-06-12** (Master-Workshop nach dem ersten echten Nutzungs-Burst, drei Live-Befunde: Tag-Flut inkl. kaputter `**`-Tags · Zwei-Leisten-IA-Verwirrung · fehlende Abschluss-Affordance + Fortschritts-Anzeige wirkt wie Reset): **R2-E** Library-IA „Readwise-3er" + Tag-Leiste + Tag-Daten (L) → [docs/archive/sprint-prompts/SPRINT_R2-E_library-ia-readwise3_2026-06-12.md](docs/archive/sprint-prompts/SPRINT_R2-E_library-ia-readwise3_2026-06-12.md) · **R2-F** Reader-Abschluss-Leiste + Fortschritts-Klarheit (S) → [docs/archive/sprint-prompts/SPRINT_R2-F_reader-finish-bar_2026-06-12.md](docs/archive/sprint-prompts/SPRINT_R2-F_reader-finish-bar_2026-06-12.md). **R2-E + R2-F ☑ done 2026-06-13.**

**Dispatcht 2026-06-13** (Folge-Feedback nach R2-F): **R2-G** Fortschritts-Bar = furthest-read + „Als ungelesen"-Reset (S) → [docs/archive/sprint-prompts/SPRINT_R2-G_furthest-read-bar-und-reset_2026-06-13.md](docs/archive/sprint-prompts/SPRINT_R2-G_furthest-read-bar-und-reset_2026-06-13.md). Bewusste **Revision** der R2-F-Bar-Entscheidung (Bar zeigt jetzt den Max statt der Scroll-Position) + Reset-Pfad (explizites `reset`-Flag umgeht den R2-F-Forward-Clamp → `NULL`). Berührt `library_detail.*` + den Progress-Endpoint in `library.py`, kein Schema-Touch. **R2-E ☑ done 2026-06-13**; **R2-F ☑ done 2026-06-13** (`c4e5b7f`); **R2-G ☑ done 2026-06-13** (`ec36c8b`, Detail unter „Aktueller Sprint") — Mintbox-Deploy von R2-F + R2-G zusammen offen. **Damit ist der R2-Cluster A–G komplett — alle drei Live-Befunde des Nutzungs-Bursts abgeräumt (Tag-Flut R2-E · IA-Verwirrung R2-E · Fortschritt/Abschluss R2-F, Fortschritts-Bar-Revision + Reset R2-G).**

**Dispatcht 2026-06-14**: **VIS1** Neomorphism-Design-System-Angleichung (L+) → [docs/archive/sprint-prompts/SPRINT_VIS1_neomorphism-alignment_2026-06-14.md](docs/archive/sprint-prompts/SPRINT_VIS1_neomorphism-alignment_2026-06-14.md). Das aus CONVERTER extrahierte + im Review **nachgeschärfte** DS (`/Users/olivergluth/CODE/Neomorphism Design System`) zurück-angleichen: **alle Screens**, Inter→**Nunito**, Elevation-Budget („pillows on pillows" ab), ≥32px-Spacing, Status-als-Tint, Hardcoded-Hex→Var. **Kein Reskin** (Farben/Radii/Shadow-Primitives identisch), **kein Schema-/Verhaltens-Touch**. Library-Karte: R2-Funktion erhalten, Ruhezustand flach (nicht der read-only-Referenz-Weg). Tokens in die eine `style.css` gemergt (nicht `tokens/*.css` vendoren). Gesegnet/nicht-anfassen: Reading-Progress-Bar + Unicode-Glyphen. 5 Phasen, jede Stop+Live-Smoke (visueller Sprint — `pytest` fängt CSS nicht); Phase 4 (Tools/Login/Tags) darf bei Länge → VIS2 abgespalten werden. **→ VIS1 P1–P4A ☑ done 2026-06-14** (Hashes unter „Aktueller Sprint"; `library*.js` unberührt, pytest 217/217); **Phase 4B (audio+mermaid) + 4C (login+tags) → VIS2** (`5a2d889`) **→ VIS2 ☑ done 2026-06-14** (`93c85bf` + `2e033aa`, Detail unter „Aktueller Sprint"); **damit ist die App-weite DS-Angleichung (alle Screens, VIS1 + VIS2) komplett**. **Offener Real-Welt-Schritt**: Mintbox-Deploy **R2-F + R2-G + VIS1 + VIS2 + MCP1 in EINEM Schwung** (alle undeployed, keine Migration — MCP1's `recorded_at` lebt im bestehenden `metadata_json`-Beutel; `git pull` + `docker compose up -d --build` — Template-Jinja-Cache braucht **`--build`, nicht `restart`** —, danach Browser-Hard-Reload).

**Dispatcht 2026-06-14 (Rollen-Korrektur)**: **MCP1** JSON-Read-API + `recorded_at`-Capture (M) → [docs/archive/sprint-prompts/SPRINT_MCP1_api-read-and-recorded-at_2026-06-14.md](docs/archive/sprint-prompts/SPRINT_MCP1_api-read-and-recorded-at_2026-06-14.md). Macht CONVERTER lese-fähig für eine kommende `converter-mcp`-Anbindung (Treiber-Use-Case Top-Level-Koordinator: unarchivierte Audio-Transkripte → Notion). **Ursprünglich aus dem übergeordneten Thread dispatcht** (der versehentlich Master spielte); CONVERTER-Master-Rolle 2026-06-14 zurück an diesen Thread. **Cross-cutting bleibt beim Top-Level-Koordinator** (nicht mein Scope): `converter-mcp`-Projekt, Notion/Kalender, Mintbox-Deploy, `PROJECTS.md`. **MCP1 ☑ done 2026-06-14** (alle 3 Phasen — Detail unter „Aktueller Sprint"): P1 GET-Liste+Einzel (`e491ac3`, Master-committet bei der Rollen-Übergabe), P2 `recorded_at`-Capture additiv (`839e3a3`), P3 Audio-Save-JS discovery-gated (`bcac6da`); **pytest 262**, `to_dict()` unangetastet, kein bestehendes Verhalten regressiert. Clone-Koordination + Arbeitsort-Disziplin: Memory `reference_two_clone_coordination_mac_mintbox`, CLAUDE.md Working-Practice 2026-06-14 gefixt (war fälschlich `/Volumes/MintHome/...`, Root-Cause des Split-Brain).

**Dispatcht 2026-06-14**: **MCP1-FIX** `recorded_at` Diktiergerät-Dialekt + Präzedenz-Flip + Backfill (S) → [docs/archive/sprint-prompts/SPRINT_MCP1-FIX_recorded-at-dictation-dialect_2026-06-14.md](docs/archive/sprint-prompts/SPRINT_MCP1-FIX_recorded-at-dictation-dialect_2026-06-14.md). Real-Diktat-Befund: Olis Diktiergerät nutzt **`YYMMDD_NNNN.MP3`** (2-stelliges Jahr, keine Uhrzeit) → der MCP1-Parser (nur 4-stellig) liefert `None` für alle realen Diktate. **Master-Entscheidungen**: Präzedenz-Flip auf `metadata.recorded_at` > **Dateiname** > Client-`lastModified` (Diktiergerät-Name ist autoritativ, lastModified evtl. Kopier-Zeit) + Backfill der 5 Bestands-Rows (Script importiert den Runtime-Parser, dry-run-Default, Mintbox-`--apply` = Olis Schritt). Additiv/konservativ, kein Schema-Touch. 2 Phasen. **MCP1-FIX ☑ done 2026-06-14** (P1 Parser+Flip `4c709be`, P2 Backfill-Script `4d02059`, Doc-Wrap dieser Commit — Detail unter „Aktueller Sprint"); **pytest 273**, kein Schema-/Endpoint-Bruch. **Olis Real-Welt-Schritte offen**: Mintbox-Deploy (zieht MCP1-FIX additiv mit, keine Migration) + danach `scripts/backfill_recorded_at.py --apply` für die 5 Diktate.

## Aktueller Sprint

_R4-LEARN-P6 ☑ done 2026-06-20 → commits `9ef8a13` (P1 Endpoint+Tests) + Doc-Wrap (dieser Commit, P2). **Highlight-Write-API (Agent-Tags/Notiz)** — der Karten-Agent (converter-mcp, Token) kann jetzt Tags + Notiz auf **bestehenden** Highlights zurückschreiben, damit Bucket-Tagging persistent ist statt pro Lauf neu abgeleitet; **exakt analog zur Card-Write-Schicht (R4-LEARN-P2)**. **P1** (`9ef8a13`, [app_pkg/cards.py](app_pkg/cards.py) + [tests/test_cards.py](tests/test_cards.py)): `PATCH /api/highlights/<int:highlight_id>/annotate` (neuer Pfad — `PATCH /api/highlights/<id>` ist von der Session-Notiz-View belegt, Flask kann Pfad+Methode nicht doppelt registrieren), **kein** `@login_required`; Auth `_authorize_card_write()` **1:1 wiederverwendet** (503 fail-closed ohne `CARD_TOKEN`, 401 missing/wrong constant-time `hmac.compare_digest`, Ziel-User `INGEST_USER`/first()), **dritter** CSRF-exempt-View. **Ownership = 404, nicht 400**: die `highlight_id` ist hier ein **Pfad-Parameter = adressierte Ressource** (Muster `api_patch_highlight`: `Highlight.query.filter_by(id).first()` + `is None or hl.conversion.user_id != target.id` → 404), bewusst **nicht** der 400-mappende `_validate_highlight_ownership`-Helper (der ist für die *Body*-`highlight_id`-Ref der Card-Writes = „ungültige Eingabe"); fremd **und** fehlend → 404, kein Existenz-Leak. Body `{tags?: [str], note?: str|null}`, mind. ein Key Pflicht (sonst 400 „Nichts zu ändern…"); `tags` = **Voll-Ersetzung** via neuem Modul-Helper `_replace_highlight_tags` über `Tag.get_or_create` (**geteiltes Vokabular**, identische Tag-Rows wie Card-/UI-Tags — kein Parallelsystem), `[]` leert, non-list → 400; `note ''`→NULL (Lösch-Intent), `len>2000`→400. **`exact`/`prefix`/`suffix` werden bewusst ignoriert** (nicht gelesen, nicht durchgereicht) — der Agent annotiert, **bewegt nie einen Anker**. Response = volles Highlight inkl. aufgelöster Tags (`Highlight.to_dict()` trägt sie schon → **kein Modell-Touch**). **+12 Tests** (340→**352**, exakt erwartet): fail-closed/401 · fremd+fehlend **404** (kein Existenz-Leak) · tags setzen/ersetzen/leeren · tags-non-list 400 · note setzen/leeren/Typ-Guard · Normalisierung (`["KI","ki"," KI "]`→eine Row) · **cross-surface Tag-Row-Reuse** (Tag erst auf einer Card angelegt → gleiche `id` am Highlight) · Anker-Immutabilität · leerer/non-dict Body · CSRF-exempt unter erzwungenem CSRF; `test_only_card_write_views_are_csrf_exempt` um den neuen View erweitert. **P2** (dieser Commit): Doc-Wrap — [docs/card_api_contract.md](docs/card_api_contract.md) (`PATCH …/annotate` in „Writes (Bearer `CARD_TOKEN`)", 6→7 Tools) + [docs/card_agent_guide.md](docs/card_agent_guide.md) (Tool-Zeile `update_highlight` + „Highlights annotieren"-Absatz; die überholte Grenze „kein Highlight-Schreiben" auf **kein Delete / keine Anker-Edits / kein Neu-Anlegen** eingedampft). **Kein Schema-Touch, keine Migration** (`highlight_tags`/`note`/`updated_at` existieren). **Olis/Koordinator-Deploy-Kette**: Mac push → Mintbox `git pull` + `docker compose up -d --build` (CONVERTER, **keine Migration**) → dann converter-mcp deployen (`update_highlight` liegt dort schon → `up -d --build`); `CARD_TOKEN` ist in beiden `.env` gesetzt, **kein neuer Token**. Der end-to-end-Beweis (`update_highlight` gegen Live-Prod) ist Koordinator-Schritt nach dem Deploy. **Master-Aktivität nächste: R3 Web-Article-Save.**_

## Vorheriger Sprint

_R4-LEARN-FIX ☑ done 2026-06-20 → commits `e8fee36` (P1 Endpoint+Tests) + Doc-Wrap (dieser Commit, P2 UI+Wrap). **Karten löschen** — die Lücke aus dem R4-LEARN-end-to-end-Smoke: „Agent erzeugt / User bewertet" war gebaut, **„User löscht" vergessen** (eine echte Smoke-Karte id=1 hing auf der Mintbox in der Queue). **P1** (`e8fee36`, [app_pkg/cards.py](app_pkg/cards.py) + [tests/test_cards.py](tests/test_cards.py)): `DELETE /api/cards/<int:card_id>` `@login_required`, owner-scoped via `Card.query.filter_by(id, user_id=current_user.id).first_or_404()` (fremd/fehlend → 404, kein Existenz-Leak, Posture wie `api_annotate_card`); `db.session.delete(card)` → **ORM-Cascade** nimmt die `review`-Zeile (`Card.review` `cascade='all, delete-orphan'`) **und** die `card_tags`-Junction (secondary) mit — **kein Raw-SQL** (ohne FK-Pragma würde `DELETE FROM card` beide verwaisen, Memory `reference_sqlite_no_fk_pragma_orm_delete`); **nicht** CSRF-exempt (Session-Write über den globalen `base.html`-fetch-Wrapper, der X-CSRFToken auch an DELETE hängt); Response `{"success": true}` 200. **+4 Tests** (Cascade-Beleg: `review`+`card_tags` direkt geprobt weg/kein Orphan/shared Tag überlebt · fremde Karte 404 · nicht-existent 404 · CARD_TOKEN-Bearer ≠ Auth = Session-only) + die CSRF-Exempt-Registry-Probe um `api_delete_card`-not-exempt erweitert. **P2** (dieser Commit, [templates/review.html](templates/review.html) + [static/js/review.js](static/js/review.js) + REVIEW-CSS): „Löschen"-Button im Karten-Footer (neben Vertiefen/Notiz), danger-toned `.review-link-btn--danger` via `--nm-danger` (token-driven, dezent, light+dark; mirror `.card-action-btn--danger`), natives `confirm()` (Konvention `library.js`); `deleteCard()` spiegelt `deepen()` → `DELETE` über den CSRF-fetch-Wrapper → spliced die Karte aus `queue`+`totalDue`, rückt zur nächsten fälligen vor (Counter aktualisiert) bzw. re-lädt zum Empty-State (`load()` clear-t jetzt auch den stale „Karte N von M"-Progress), Fehler → `showToast(level:danger)`. **Verify**: pytest **336→340** (+4). **Live-Smoke** (lokale Docker-Instanz `:5656`, echte Review-Klicks über die DOM-Click-Event-Kette, MacChrome **dark+light**, **0 Console-Errors**): **Cancel** (confirm=false) blockt — kein Request, Karte bleibt; **Accept** → `DELETE /api/cards/3` **200** (kein 400, CSRF ok) → nächste Karte „Smoke-Frage 2" + Counter „1 von 1" + Toast „Karte gelöscht"; **letzte Karte** → `DELETE` 200 + `GET /api/review-state`-re-fetch → Empty-State „Nichts fällig." **ohne** stale Counter; **DB-Probe live** nach jedem Delete: Karte + Review + `card_tags`-Junction weg, shared `smoke`-Tag überlebt; Button-Danger-Tone in beiden Themes verifiziert (`--nm-danger` light `#e17055`/dark `#e07060`). **Olis Real-Welt-Schritte**: Mintbox-Deploy (`git pull` + `docker compose up -d --build` — Templates/static ins Image gebacken → `--build`, **keine Migration**) + danach die **Smoke-Karte id=1 im „Lernen"-Tab löschen** (erster echter Anwendungsfall, NICHT von mir geräumt). **DELETE ist UI-only — nicht in den converter-mcp wrappen** (Hinweis an den Koordinator). **Master-Aktivität nächste: R3 Web-Article-Save.**_

_Vorgänger: R4-LEARN ☑ done 2026-06-19 → commits `ded0dc4` (P1) + `41bb945` (P2) + `881071f` (P3) + `647195b` (P4) + Doc-Wrap (dieser Commit). **SR-/Recall-Layer über den Highlights** — Spaced-Repetition-Lernkarten, das ursprüngliche Readwise-Herz. **Teilung**: der externe Agent erzeugt Karten (Token-Write), CONVERTER speichert/plant (FSRS)/zeigt (Review-UI). **P1** (`ded0dc4`, [models.py](models.py) + neues [app_pkg/cards.py](app_pkg/cards.py)): Tabellen `card` (self-contained front/back/cloze/prompt, `user_id`-Owner-Scope, `highlight_id` nullable = Best-Effort-Provenienz) / `review` (1:1 FSRS-State) / `card_tags` via `db.create_all` — **keine Migration**; **Muss-Fix `before_delete`-Event auf `Highlight`** nullt `card.highlight_id` ORM-seitig (kein FK-Pragma → deklariertes `ON DELETE SET NULL` inert; greift direkt **und** über den Conversion-`delete-orphan`-Cascade; Karte+Review überleben; Test **rot→grün** bewiesen); globaler `GET /api/highlights/recent` (Session, owner-scoped, `?since=`/`?limit=`). **P2** (`41bb945`): `POST`/`PATCH /api/cards` **Token** (`CARD_TOKEN`, Ingest-Muster — constant-time `hmac.compare_digest`, fail-closed 503, nur diese 2 Views CSRF-exempt, Token nie geloggt; Ziel-User via `INGEST_USER`/first()), Pro-Typ-Validierung (atomic = front+back ODER cloze; generativ = prompt), Review-Zeile gleich mit angelegt; Reads **Session** owner-scoped (`GET /api/cards`, `/api/cards/<id>`, `/api/review-state`). **P3** (`881071f`): `services/scheduler/` swappable — FSRS via `fsrs==6.3.1` (`enable_fuzzing=False`) + SM-2-Fallback hinter derselben ABC, Config `SCHEDULER_ENGINE`/`FSRS_DESIRED_RETENTION`; `POST /api/cards/<id>/review` **Session** (der User bewertet, NICHT Token), generativ+schwach (again/hard) → `wackelt`; FSRS-`state`/`step` nicht persistiert (graduiert rekonstruiert — dokumentierte Vereinfachung, Intervall-Math erhalten). **P4** (`647195b`): `/review` + [templates/review.html](templates/review.html) + [static/js/review.js](static/js/review.js) + REVIEW-CSS + Nav „Lernen"; läuft die `due<=jetzt`-Queue aus `/api/review-state` ab (atomar/cloze/generativ, Aufdecken→Rating→nächste), **Vertiefen**→`wackelt` + Inline-**Notiz** über neuen Session-Pfad `POST /api/cards/<id>/annotate` (PATCH ist token-only → eigener Session-Pfad), Empty/Done, Tastatur (Space/1–4); alle POSTs über den globalen `base.html`-CSRF-fetch-Wrapper. **Drei-Wege-Auth-Split**: Agent-Write=Token · User-Rate=Session · User-Annotate=Session. **DS-konform** (`c-surface--flat`, „Gut"-primary, Status-als-Tint, Cloze-XSS-safe, token-driven, 0 Hardcode-Hex). **Verify**: pytest **277→336** (+59: P1 +11 · P2 +20 · P3 +21 · P4 +7). **Live-Smoke** (lokale Instanz via conftest-Stub-Launcher, MacChrome **dark+light**, **0 Console-Errors**; Karten via `POST /api/cards`+`CARD_TOKEN` geseedet): atomar bewertet → `due` rückt vor + nächste; Cloze-Lücke → Highlight-Reveal; generativ erklären → Musterantwort; **Vertiefen** → WACKELT-Badge+Toast; **Notiz** gespeichert; Done + Empty; alle CSRF-POSTs grün. **Memory**: `reference_sqlite_no_fk_pragma_orm_delete` + `reference_swappable_scheduler_interface`. **Olis offene Schritte**: Mintbox-Deploy (`git pull` + `docker compose up -d --build` — `fsrs` neu → Image baut neu; **`CARD_TOKEN` in `.env` setzen**; **keine Migration** — neue Tabellen via `db.create_all`) + converter-mcp-Tool-Wrapping (Koordinator: `POST`/`PATCH /api/cards` + die 4 Reader `/api/highlights/recent`,`/api/cards`,`/api/cards/<id>`,`/api/review-state` wrappen; **Rate + Annotate sind UI-only**, nicht für den Agent). **Master-Aktivität nächste: R3 Web-Article-Save.**_

_Vorgänger: R2-H ☑ done 2026-06-18 → commits `82e786c` (P1 Backend) + `fa759b3` (P2A Liste) + `fd20ec6` (P2B Detail) + Doc-Wrap (dieser Commit). **Flache Vier-Orte-IA** — die drei sich überlappenden Bedien-Achsen (Ort `lifecycle_status` · Lese-Liste-Flag `queue_position` · Favorit `is_favorite`) **plus** die zwei konkurrierenden Nav-Listen (Tabs + Ort-Chips) sind auf **EINE flache Achse mit vier exklusiven Orten** kollabiert — **Inbox · Lese-Liste · Bibliothek · Archiv** —, **identisch in Tab ↔ Karte ↔ Detail**. „Später"/Favoriten-Stern/Queue-Flag/Weiterlesen-Sektion sind weg. **Kein Schema-Touch, keine Migration**: die vier Orte werden aus `lifecycle_status`+`queue_position` **abgeleitet** (Präzedenz **archive > queued > inbox > neutrales Regal**); `is_favorite` bleibt als Spalte unsichtbar liegen (reversibel). **P1** (`82e786c`, Backend [app_pkg/library.py](app_pkg/library.py)): neuer `POST /api/conversions/<id>/place {place}` = die EINE Move-Aktion (setzt die `(lifecycle,queue)`-Kombi, hält Exklusivität — Inbox/Bibliothek/**Archiv** nullen `queue_position`, Lese-Liste appendet idempotent ans Ende; **Archiv dequeued jetzt**, löst R2-D's „kein Auto-Dequeue" ab); `library()` views ∈ {inbox,leseliste,bibliothek,archiv} über die Ableitung; `/queue` auf **Reorder (up/down)** verengt (R2-D-Swap-über-sichtbare-Menge bleibt); `?status`+`?favorites`-Filter raus; `inbox_count` = Ort=Inbox; PUT `lifecycle_status`/`is_favorite` + GET-API + `to_dict()` **unberührt** (MCP). **P2A** (`fa759b3`, Liste [templates/library.html](templates/library.html) + [static/js/library.js](static/js/library.js) + CSS): vier Tabs (Archiv neu, Ort-Chip-Zeile raus); Karten-`status-segmented`+`queue-btn`+`favorite-btn`+Status-Badge → **EIN `.place-control`** (gleiche Optik wie die Tabs, abgeleiteter Ort gedrückt, Klick = `setPlace` → POST `/place`, Reload-Gate aus Memory `reference_reorder_over_filtered_set`: verlässt die Karte den sichtbaren Ort → reload, im Finder nur bei Archiv, sonst in-place); **Master-Umlenkung**: der globale Finder triggert auf **jeden** Content-Filter (`search OR tag OR type`) → spannt alle Nicht-Archiv-Orte; Bibliothek ohne Filter = neutrales Regal; Phase-1-Kompat-Shim + `pagination_args`-Altlast (favorites/status) vollständig raus. **P2B** (`fd20ec6`, Detail [templates/library_detail.html](templates/library_detail.html) + [static/js/library_detail.js](static/js/library_detail.js) + CSS): Sidebar-`status-segmented--full`+`queue-toggle-btn` → **dasselbe `.place-control`** (fixt zugleich den 400enden Queue-Toggle); `finishArchive` nutzt jetzt POST `/place` archiv (dequeued, kein PUT); Favoriten-Stern raus; **R2-G „Als ungelesen"/Lese-Fortschritt-Card + Abschluss-Leiste + „Gelesen"-Flag + `resetProgress` + Reading-Progress/Highlights/Notion unberührt**; **CSS-Cleanup** der jetzt toten Klassen (`.favorite-btn`, `.status-badge`, `.status-segmented`, `.queue-btn`, `.queue-toggle-btn` — grep-geprüft 0 Reste, Braces balanced). **Such-/Lese-Fortschritt orthogonal**: Karten-Fortschrittsbalken + Resume-on-Open + R2-G-Reset bleiben, nur die Weiterlesen-Sektion entfällt. **Verify**: pytest **277** (273→+4 netto; neue [tests/test_library_ia.py](tests/test_library_ia.py) = Vier-Orte-IA inkl. Präzedenz/`/place`/Finder/Pagination, `test_reading_list` auf Reorder-Mechanik fokussiert, `?status`-Tests retired, `test_conversion_tags`-Fixtures auf Bibliothek-Ort). **Live-Smoke** (echte MacChrome-Klicks, dark+light, **0 Console-Errors**): Liste — alle 4 Tabs, Karte Inbox→Bibliothek (Reload-Gate, Inbox-Badge 2→1), Reorder-Swap, Archiv dequeued, Finder (search/tag/type) spannt Nicht-Archiv + schließt Archiv aus; Detail — Ablage-Control durch alle Orte (in-place, persistiert `lifecycle=archive`+`queue=null`), R2-G-Reset `pct→null`, „Gelesen"-Flag, `finishArchive`→`/place`→zurück zur Library, **kein 400**. **converter-mcp unberührt** (GET-API + `to_dict` + `lifecycle_status`/`queue_position`-Spalten bleiben; `list_audio_transcripts`' „`!= archive`" hält weiter). **Olis offener Schritt**: Mintbox-Deploy (`git pull` + `docker compose up -d --build` — Templates ins Image gebacken → `--build`, nicht `restart` —, **keine Migration**, danach Browser-Hard-Reload). **Master-Aktivität nächste**: die **R3/R4-Gabelung** (R3 Web-Article-Save vs. R4 Spaced-Repetition, je Workshop)._

_Vorgänger: MCP1-FIX ☑ done 2026-06-14 → commits `4c709be` (P1 Parser+Flip) + `4d02059` (P2 Backfill-Script) + Doc-Wrap (dieser Commit). **Diktiergerät-Dialekt `YYMMDD_NNNN` + Präzedenz-Flip + Backfill** — Real-Diktat-Befund: Olis Diktiergerät benennt Files `YYMMDD_NNNN.MP3` (2-stelliges Jahr + laufende Nummer, **keine Uhrzeit**, z.B. `260521_0176.MP3` = 2026-05-21); der MCP1-Parser kannte nur 4-stellige Jahre → `None` für **alle** realen Diktate. **P1** (`4c709be`): `parse_recorded_at_from_filename` erkennt zusätzlich den Diktat-Dialekt über eine **am Stem-Anfang verankerte** Regex `^(\d{2})(\d{2})(\d{2})_\d+` (kein 6-Ziffern-Run irgendwo im Namen, kein Match ohne `_NNNN`); `YY→20YY`, MM/DD-Range + echte `datetime()`-Konstruktion (fängt z.B. Feb-30), `00:00` Europe/Berlin tz-aware, Sequenz-Nr. nie als Datum; der Diktat-Kandidat teilt das `found`-Set mit der YYYY-Logik → das bestehende `len==1`-Ambiguitäts-Gate bleibt konservativ. **Präzedenz-Flip** in `api_create_conversion`: `metadata.recorded_at` (explizit) > **Dateiname** > Client-`recorded_at`-Feld (war `… > Client > Dateiname`) — der Diktiergerät-Name ist geräte-autoritativ, Client-`lastModified` evtl. nur die Kopier-Zeit; das Client-Feld wird nur noch konsultiert, wenn der Dateiname `None` liefert (Fallback unverändert, unparsebarer Client-Wert → `logger.warning` + nie 400). **+11 Tests** ([tests/test_recorded_at.py](tests/test_recorded_at.py)): 5 reale Diktat-Positive, 4 Diktat-Negative (nicht-am-Anfang `notes_…`/MM=34/Feb-30/ohne-`_NNNN`), Fallback-unchanged-Test, Diktat-E2E-POST; der **P2-Präzedenz-Test invertiert** (filename>client) + Garbage-Client-Test umbenannt. **P2** (`4d02059`): [scripts/backfill_recorded_at.py](scripts/backfill_recorded_at.py) — **importiert** den Runtime-Parser (nie reimplementiert, Memory `reference_tag_vocab_central_gate_plus_backfill_script`); Scope `audio_transcription`-Rows ohne `metadata.recorded_at` mit parsebarem `source_filename` → `recorded_at` + `recorded_at_source='filename'`, bestehende metadata-Felder erhalten; **`--dry-run` Default** (Rollback → Vorhersage == Apply), `--apply` schreibt, 2. `--apply` = no-op (idempotent), konservativ (non-parse / schon-gesetzt / falscher-Typ bleiben unangetastet). **Lokal verifiziert** (Mac-dev-DB, synthetische Rows): dry-run → apply → 2. apply durchgespielt — Feld-Erhalt (`deepgram`/`lang` überleben), Skip-Logik (C non-parse / D hat-schon / E falscher-Typ) und der **CLI-Entry-Point** (`python scripts/backfill_recorded_at.py` + `--apply` als echter Subprozess) bewiesen; **die 5 echten Diktate liegen auf der Mintbox-Prod-DB** — der reale `--apply` ist **Olis Schritt**. **Verify**: pytest **273/273** (262→+11), `test_recorded_at.py` isoliert 40 grün. **Memory**: neuer `reference_dictation_recorder_filename_pattern.md` (reales Muster `YYMMDD_NNNN`, 2-stelliges Jahr, keine Zeit — wiederverwendbares Faktum fürs Meeting-Matching). **Olis offene Real-Welt-Schritte**: (1) Mintbox-Deploy (MCP1-FIX additiv im bestehenden Bundle R2-F/G+VIS1/2+MCP1, **keine Migration**, `git pull` + `docker compose up -d --build`), (2) danach `scripts/backfill_recorded_at.py --apply` auf der Mintbox für die 5 Diktate (Kommandos im Schluss-Bericht), (3) der separate **MCP1-P3 browser-E2E-Smoke** (bleibt offen, eigenes Backlog-Item). **Master-Aktivität nächste**: zurück an der **R3/R4-Gabelung**._

_Vorgänger: MCP1 ☑ done 2026-06-14 → commits `e491ac3` (P1, Master-committet bei der Rollen-Übergabe) + `839e3a3` (P2) + `bcac6da` (P3) + Doc-Wrap. **JSON-Read-API + `recorded_at`-Capture** — macht CONVERTER lese-fähig für die kommende `converter-mcp`-Anbindung (Treiber-Use-Case beim Top-Level-Koordinator: unarchivierte Audio-Transkripte → Notion). **Alles read-only bzw. additiv — kein bestehendes Verhalten geändert, `to_dict()` unangetastet.** **P1** (`e491ac3`): zwei **separate** GET-View-Funktionen in [app_pkg/library.py](app_pkg/library.py) (eigener Funktionsname = eigener Endpoint, Flask dispatcht nach Methode, kein Blueprint) — `GET /api/conversions` → `api_list_conversions` (owner-scoped; Query `type`/`status`/`exclude_status` je gegen `ALLOWED_CONVERSION_TYPES`/`LIFECYCLE_STATUSES` → 400, `limit` Default 100 **Cap 500** dokumentiert, `offset` Default 0, beide non-int/<1 bzw. <0 → 400; Response `{items,total,limit,offset}`, `total` **vor** dem Fenster, Sort `created_at desc`) + `GET /api/conversions/<id>` → `api_get_conversion` (`get_owned_conversion` → 404). Modul-Helper `_conversion_summary` = schlankes Dict **ohne** `content` (statt­dessen `content_length` + `content_preview` 300 Z.), `tag_refs` als `{id,name}`. **+16 Tests** ([tests/test_library.py](tests/test_library.py)). **P2** (`839e3a3`): `parse_recorded_at_from_filename` (pure, konservativ — YYYY-first-Regex mit `(?<!\d)`/`(?!\d)`-Ankern, Berlin-DST-Offset via `zoneinfo`, „falsch ist schlimmer als leer": out-of-range/unmögliche-Kalender-/mehrdeutige Namen → None) + `_normalize_client_recorded_at` (ISO-String **oder** Epoch-ms-Zahl → ISO-UTC); `api_create_conversion` schreibt `metadata.recorded_at` + `metadata.recorded_at_source` **additiv** — Präzedenz **Client-Feld > Dateiname**, unparsebarer Client-Wert → `app.logger.warning` + Fallback auf Dateiname (**nie 400**), vom Client vorgesetztes `metadata.recorded_at` bleibt unangetastet, bestehende metadata-Felder erhalten, metadata defensiv→`{}` bei non-dict. **+29 Tests** ([tests/test_recorded_at.py](tests/test_recorded_at.py): Parser positiv/negativ/invalide/mehrdeutig + Create client-iso/epoch-ms/filename/precedence/none/preset/preserve). **P3** (`bcac6da`, **discovery-gated → Save-Pfad existiert**): [static/js/audio_converter.js](static/js/audio_converter.js) File-Upload-Save (`save-transcription-btn`) captured `fileInput.files[0].lastModified` in `_transcriptionData` und sendet es als `recorded_at` (Epoch-ms) im POST mit (`source_filename` war schon da; `JSON.stringify` lässt `recorded_at` weg falls undefined → Backend-Fallback auf Dateiname-Parser); **Live-Mikro-Pfad (`save-live-btn`) bewusst NICHT angefasst** — keine Upload-Datei, kein `file.lastModified`. **Verify**: pytest **262/262** (217→+45); **P3-Smoke ohne Deepgram** (Fixture `sample.mp3` zu klein zum Transkribieren + Mintbox läuft das Alt-Image): `node --check` grün, dann node-Replay der **exakten** JS-Objektliterale mit simuliertem File (Fixture-mtime als `lastModified`) → echte Payload (`recorded_at` Epoch-ms + `source_filename`) durch den **echten** Endpoint → `GET /api/conversions/<id>` bestätigt `source_filename` + `metadata.recorded_at` (`recorded_at_source='client'`, **Client-Präzedenz über Dateiname end-to-end bewiesen**) + bestehende metadata-Felder erhalten. **Memory**: kein neuer Eintrag — etablierte Patterns (separate View-Funktion pro Methode, `get_owned_conversion`-Reuse, additive-metadata); Parser-Konservatismus ist sprint-spezifisch, in Docstring + Tests dokumentiert. **Out-of-scope → Top-Level-Koordinator** (nicht CONVERTER-Master-Scope): `converter-mcp`-Projekt scaffolden, Notion/Kalender-Orchestrierung, Mintbox-Deploy. **Backlog-Folge-Item** (CONVERTER-seitig, optional): `recorded_at` retroaktiv für bestehende Audio-Conversions (Backfill-Script über `source_filename`, importiert die Runtime-`parse_recorded_at_from_filename`). **Mintbox-Deploy = Olivers offener Schritt — MCP1 reiht sich in den bestehenden Bundle** (R2-F + R2-G + VIS1 + VIS2 + MCP1): `git pull` + `docker compose up -d --build`, **keine Migration** (recorded_at lebt im bestehenden `metadata_json`-Beutel), danach Browser-Hard-Reload. **Master-Aktivität nächste**: zurück an der **R3/R4-Gabelung** (R3 Web-Article-Save oder R4-LEARN), kein Sprint präjudiziert._

_Vorgänger: VIS2 ☑ done 2026-06-14 → commits `93c85bf` (P1 Audio) + `2e033aa` (P2 Mermaid/Login/Tags) + Doc-Wrap (dieser Commit). **Abschluss der „alle Screens"-Angleichung — VIS1 + VIS2 komplett, die ganze App steht jetzt auf den nachgeschärften DS-Regeln** (Elevation-Budget · ≥32px · Status-als-Tint · kein Hardcode · Nunito aus VIS1-P1). **P1 Audio** (`93c85bf`, der schwerste Screen): Mic-`.recording` von gesättigtem Terracotta-Fill (`#fff`/`rgba(225,112,85,.85)`) → `tint-danger`-Wash + danger-Ink + pressed (Regel 7/8); **Mode-Radios echtes Segmented** — aktiv pressed+tint, inaktiv flach, die **rule-7-verbotene farbige `inset 4px 0 0`-Left-Bar entfernt** (spiegelt das geshippte Tab-Control); Prompt-Editor-Disclosure **entnestet** (raised-in-raised → `.prompt-editor` flach); **Podcast-Stage-Bar echter Bug**: `background: var(--nm-pressed-sm,…)` — ein *Shadow*-Token als `background` ist ungültig, fiel still auf den rgba-Fallback → `tint-accent`-Track + `accent`-Fill + Pill-Radii, Hex-Fallbacks raus; char-counter `#c79100`/`#c0392b` → `--nm-alert-warning/danger-ink`; Action-Cluster (live/file Copy-Leeren, Podcast-Download) `flex gap-2` → `.c-btn-row`; Step-Karten `mb-4` → `mb-8` (≥32px). **P2** (`2e033aa`): **Mermaid** = `pane-header`-Separator-Hardcode → `var(--nm-sep-bottom)` + redundanten Dark-Override gelöscht (Render bit-identisch, profitiert auch Markdown; der Screen erbt sonst komplett VIS1's Split-Pane — Textarea schon pressed/tokenisiert, **keine Buttons**); **Login** = die EINE gehobene `c-surface` + gepresste `c-input`s + `c-btn--primary` standen schon korrekt, reine Spacing-Härtung `mb-4`/`mb-6` → `mb-8`; **Tags** = `.tag-manager-card`-Reihen von N raised-Pillows → flach getönt (`surface-grad`, kein Ruhe-Shadow), nur die gehoverte/fokussierte Reihe hebt (raised-sm) + enthüllt das Delete (von prominentem `c-btn--danger` → flacher danger-Text, dezent/hover-revealed, spiegelt das Library-`card-actions`-Muster inkl. `:focus-within`-a11y), Liste `gap-2` → `gap-4`. **Carry-forward aus VIS1 erledigt**: `.save-library-btn.saved`-Grün (`#5a9a7a`/`#70c898`) → `--nm-alert-success-ink` + Dark-Override weg. **Verify**: pytest **217/217** (visueller Sprint, kein getesteter-Code-Touch); **Live-Smoke jede Phase dark+light über echten MacChrome, 0 Console-Errors** — Mic-Toggle-Zustände + Mode-Wechsel + Podcast-Flow + Tags-Hover-Reveal per Screenshot **und** Computed-Style verifiziert. **Diagnostik-Fund (Mess-Artefakt, kein Bug)**: Computed-Style **während** der 150ms-Mic-Transition liefert den Zwischenwert (box-shadow snapt, weil inset↔outset nicht interpolierbar — color/bg überblenden 150ms) → mit `transition:none` löst der Recording-State exakt auf. **Login-Smoke-Caveat**: `/login` redirectet eingeloggte User weg → statt Logout (nicht re-authentifizierbar, Session-Schutz) ein **getreuer Inject-Preview** der neuen `mb-8`-Markup in beiden Themes. **Template-Reload-Fund (verstärkt VIS1)**: Web läuft **gunicorn ohne Template-Auto-Reload** (override-File mountet `static`+`templates`, dokumentiert selbst „templates/ nach `docker compose restart`"); CSS live auf Reload, Template-Edits nur live nach Restart/Rebuild — im Wrap lief `docker compose restart markdown-converter`, Login serviert jetzt frisch `mb-8`. **Mintbox-Deploy = Olivers offener Schritt — der nächste Deploy zieht R2-F + R2-G + VIS1 + VIS2 in EINEM**: `git pull` + **`docker compose up -d --build`** (Templates ins Image gebaked, Prod hat keinen Override-Mount → `--build`, **nicht** `restart`), **keine Migration**, danach **Browser-Hard-Reload** (Static-Cache). **Master-Aktivität nächste**: zurück an der **R3/R4-Gabelung** (R3 Web-Article-Save oder R4-LEARN), kein Sprint präjudiziert._

_Vorgänger: VIS1 ☑ done (P1–P4A) 2026-06-14 → commits `036d37c` (P1 Token-Fundament+Nunito) + `c133680` (P2 Library-Karte/Liste) + `b598c0e` (P3 Reader/Detail) + `467d1ec` (P4A Globals+Markdown+Document) + Doc-Wrap (dieser Commit). **Neomorphism-DS-Rück-Angleichung — kein Reskin, sondern Elevation-Budget-Audit + Brand-Font + Token-Härtung** (das DS wurde aus CONVERTER extrahiert, im Review nachgeschärft → die Regeln fließen zurück). **P1** (`036d37c`): Inter→**Nunito** global (base.html Link+Tailwind, style.css `--nm-font`, **plus login.html eigener `<head>` — Eigenständig-Fund**); neue `:root`-Tokens 1:1 aus `tokens/*.css` (gap-control/section, Type-Skala, Weights, Leading, Eyebrow); `--nm-surface-grad` + `--nm-sep-*` als Token promoted (surface/card/main-container + Separatoren konsumieren sie, 5 redundante Dark-Overrides weg); Dark-Block gegen `dark.css` abgeglichen. **P2** (`c133680`): Library-Karte/Liste auf Budget — Tone-Token-Palette `--nm-tone-*` (light+dark, = DS-Badge-Tones; Dark-Badge-Override-Block gelöscht), Segmented + View-Switcher **flacher Track + aktives gepresst** (war pressed-Track+raised-active), Kopieren/Löschen **Hover-Reveal pure-CSS** (`:hover`/`:focus-within`, `pointer-events`-Gate), Karten-Grid+Filter-Spacing ≥24/32px. **R2-Triage überlebt die Abflachung** (volle Smoke-Matrix: Favorit/Status-Segment/Queue/Tab/Ort-/Tag-Filter/+N/Reorder-Rail, dark+light, Echtdaten round-trip-restauriert; library.js-Klassen-Kontrakte unberührt). **P3** (`b598c0e`): Reader/Detail — Sidebar-Cards ≥32px-Gaps + innen flach, `queue-toggle` Regel-5 (flat-off/pressed-on), Highlight-Popover entbordert→`--nm-raised`, Highlight-Sidebar-Cards entpillowed (raised→Tint), Cross-Format-`border-left`→Tint (Regel 7), `reader-finish`/`highlight-tag`-Divider→`--nm-sep-top`, Notion-Target flat/pressed, Pill-Radii; **R2-F/G-Verhalten unberührt** (`library_detail.js` nicht angefasst, Reset-Smoke: `last_read_percent`→NULL persistiert + Bar 0). **P4A** (`467d1ec`): 3 cross-cutting Globals — `.c-alert` Regel-7 (`border-left`-Bar weg) + Alert-Ink-Tokens `--nm-alert-*-ink` (4 Dark-Overrides weg); **`.c-btn-row`-Toolbar-Konvention** (≥24px für raised `.c-btn`-Geschwister, alle Tool-Header + Detail-Top-Toolbar); Reader-Toolbar `[data-theme="dark"]` **entglast** (neomorph, opake Clay+Dark-Bevel) — plus Markdown (Orientation-Toggle Regel 5) + Document (DropZone war schon der korrekte pressed well). **Verify durchgehend pytest 217/217** (visueller Sprint, kein getesteter-Code-Touch), **Live-Smoke jede Phase dark+light über echten MacChrome, 0 Console-Errors**. **Zwei Eigenständig-Funde**: login.html-Duplikat-Font-Wiring (sonst bliebe Login bis 4C auf Inter) + der **Jinja-Template-Cache** (Static ist live-gemountet, **Template-Änderungen brauchen `restart`/`--build`** — daraus die Prod-Deploy-Schärfung unten). **Phase 4B (audio+mermaid) + 4C (login+tags) sauber → VIS2 abgespalten** (`5a2d889` VIS2-dispatch, Master-parallel). **Carry-forward → VIS2**: `.save-library-btn.saved`-Grün noch hardcoded (= `--nm-alert-success-ink`). **Mintbox-Deploy = Olivers offener Schritt — VIS1 + R2-F + R2-G in EINEM Schwung**: reiner CSS/Template/Static-Touch, **keine Migration**; `git pull` + **`docker compose up -d --build`** (Template-Jinja-Cache braucht **`--build`, nicht `restart`** — Prod hat keinen Override-Mount), danach **Browser-Hard-Reload** wegen Static-Asset-Cache. **Master-Aktivität nächste**: VIS2 (Sub-Thread, Prompt steht `5a2d889`) — danach R3/R4._

_Vorgänger: R2-G ☑ done 2026-06-13 → commits `ec36c8b` (Code) + Doc-Wrap. **Fortschritts-Bar = furthest-read + „Als ungelesen"-Reset** — Folge-Feedback nach R2-F (Oliver 2026-06-13): die Bar soll **den Max** zeigen statt der Position, plus ein Reset-Pfad. **Bewusste Revision** der R2-F-Bar-Entscheidung (Architektur Knoten 8 → Knoten 9): die Bar lief beim Hochscrollen als Positions-Anzeige zurück und las sich weiter als „Reset", obwohl Phase 1 von R2-F bewies, dass der DB-Wert nie sinkt. **Phase 1 Backend** (`ec36c8b`): Reset über explizites `{"reset":true}` im **bestehenden** Progress-PATCH → `last_read_percent = NULL`, **umgeht bewusst den R2-F-Forward-Clamp** (die eine sanktionierte Senkung — genau die R2-F-BACKLOG-Notiz, jetzt gebaut); ignoriert ein mitgeschicktes `percent`; **non-bool `reset` = 400** (gleiche strikte Bool-Validierung wie `percent`, konsistent zur bestehenden Endpoint-Strenge — truthy-Garbage `1`/`"true"` löst keinen Reset aus). 4 neue Tests (Clamp-Bypass trotz Bestand 80→NULL, reset-gewinnt-über-percent, reset:false-Normalpfad, non-bool-400). **Phase 2 Frontend**: Bar `fill = max(position, maxReached)` statt nur `position` (das R2-F-Vorwärts-Gate `persistArmed && percent>maxReached` **unverändert** — nur die Fill-Zeile koppelt an den Max; `maxReached` aus `last_read_percent` geseedet → Bar steht sofort beim Öffnen am Max, auch vor dem rAF-Arming); `syncReadFlag()` von nur-monoton-an auf **bidirektional** (`hidden = !(maxReached>=95)` → „Gelesen"-Label verschwindet beim Reset, kommt bei erneutem ≥95 zurück); **Reset-Button** „Als ungelesen markieren" in eigener **„Lese-Fortschritt"-Sidebar-Card** (orthogonal zu Status/Lese-Liste, die schon Ort+Priorität bündeln — eigene Card statt 3-in-1); `resetProgress()` PATCHt `{reset:true}` über den globalen CSRF-fetch-Wrapper, bei Erfolg `maxReached=0` + `update()` + `syncReadFlag()` + Toast „Als ungelesen markiert.", kein Auto-Scroll, `window.resetProgress` aus der Closure exposed. **Korrektheits-Zusatz über den Pseudo-Code hinaus (Re-Persist-Schutz)**: der literale „`maxReached=0; update()`"-Pfad hätte den NULL-Zustand sofort zerstört — bei `persistArmed=true` und Position>0 feuert `update()`s Persist-Block → schreibt die aktuelle Position zurück (NULL→Pos), und ein pending Trailing-Timer hätte `0.0` geschrieben. `resetProgress()` **cancelt darum den Persist-Timer und disarmt vor `update()`** (gleiches Self-Persist-Idiom wie der Resume-Scroll, Memory `reference_scroll_progress_persistence`), rearmt im Folge-rAF. **Verify**: pytest **217/217** (213→+4); `node --check` clean. **Live-Smoke** (lokale Docker-Instanz frisch rebuilt — Code gebaked; user `smoke`, Doc id=8 „Erklärbär" 13k Zeichen, echtes Wheel-Scrollen): Bar steht am geseedeten Max (50 % bei `scrollTop` 0, wo R2-F 0 % gezeigt hätte) → **hochscrollen → Bar bleibt** (Kern-Fix) → über den Max scrollen → Bar **wächst**, `maxReached` advanced + **persistiert** (DB 50→70→98); „Gelesen"-Label erscheint ab ≥95, bleibt beim Hochscrollen; **Reset** (Position 35, vorher 98, Label an, `persistArmed=true`) → Bar **fällt auf 35 %**, Label **weg**, Toast „Als ungelesen markiert.", **DB → NULL und bleibt NULL** — der **Re-Persist-Schutz scharf bewiesen** (persistArmed=true + Position 35>0, ohne den Disarm hätte `update()` 35 zurückpersistiert); zurück zur Bibliothek → id=8-Karte **kein** Fortschrittsbalken; Lese-Liste → id=8 **nicht** in Weiterlesen; **dark + light** beide rendern (Bar + neue Card + Button), **0 Console-Errors**; DB/Theme/Smoke-User-Passwort/`launch.json` restauriert (`git status` zeigt nur die 4 Feature-Files). **Smoke-Mechanik-Notiz**: der MCP-Chrome-Tab war zeitweise occluded (`document.hidden` → rAF pausiert), aber `persistArmed` war im initialen sichtbaren Moment bereits scharf und bleibt true → die Smoke ist **voll repräsentativ** inkl. Persist + Re-Persist-Schutz (anders als R2-F, wo der occluded-Tab den persistArmed-Pfad blockierte und Dekomposition nötig machte); Scroll-Events real über die echte Listener-Kette, `update()` liest `scrollTop` nie das Event-Objekt (kein Synthetik-Trap wie bei Selection). **Memory**: kein neuer Eintrag — der Reset-Disarm ist ein **zweiter Präzedenzfall** für den Self-Persist-Guard, mit einer Zeile in `reference_scroll_progress_persistence.md` ergänzt (Pattern stand schon drin, Resume-Scroll war der erste). **Mintbox-Deploy = Olivers offener Schritt — R2-F + R2-G in EINEM Deploy** (beide undeployed, keine Migration — `last_read_percent` existiert seit R2-B; `git pull` + `docker compose up -d --build`, danach Browser-Hard-Reload wegen Static-Asset-Cache). **R2-Cluster A–G komplett.** **Master-Aktivität nächste**: nächstes P1 — **R3 Web-Article-Save** oder **R4-LEARN** (R3/R4-Gabelung offen)._

_Vorgänger: R2-F ☑ done 2026-06-13 → commits `c4e5b7f` (Code) + Doc-Wrap. **Reader-Abschluss-Leiste + Fortschritts-Klarheit** — Antwort auf den dritten Live-Befund nach dem Nutzungs-Burst (Oliver: „nach dem fertig lesen muss ich wieder hoch um zurück zu gehen … aber das setzt den fortschritt zurück"). **Phase 1 Live-Befund (read-only, kein Code) → Ergebnis (a): der DB-Wert sinkt nie.** Full-stack-Repro auf der **echten Mintbox** (id=22, Start 8.07%, archiviert, Tab sichtbar+fokussiert): Resume öffnet auf 8.1% (Seed via `{{…|tojson}}` deckungsgleich), Scroll ans Ende → 2 PATCHes → DB **100.0**; **Hochscrollen → Bar läuft sichtbar auf 0% zurück (genau Olivers „Reset"-Eindruck) aber NULL PATCHes** (Client-Gate `percent>maxReached`) + DB bleibt **100.0**; nav-away (unload-Flush sendet `maxReached`=100, nicht Position) + reopen+sofort-verlassen halten 100 (Seeding korrekt). **Kein echter Bug** — furthest-read clientseitig dicht; das UX-Loch ist (1) fehlende Abschluss-Affordance am Doc-Ende + (2) die Bar als Positions-Anzeige, die beim Hochscrollen zurückläuft und „Reset" *suggeriert*. id=22 nach dem (vom Repro erzwungenen) Schreibzugriff auf Originalwert 8.0721 restauriert (Prod-DB-Disziplin). Sprint-Doc-Korrekturen: Tabelle `conversion` (nicht `conversion_history`), Progress-Endpoint bei `library.py:291-311` (nicht 255-275 — R2-E hat ihn ~36 Z. nach unten geschoben), DB-Pfad `/app/data/converter.db`. **Phase 2 Code** (`c4e5b7f`): **Abschluss-Leiste** am Content-Ende (nur bei `content_html`) — „Zurück zur Library" (`history.back()` wenn direkter Referrer = Library-Liste, sonst `href=/library`-Fallback) + „Archivieren" (`finishArchive`: derselbe R2-C-Status-PUT, dann Toast „Archiviert" + Navigation, ~700 ms Delay damit der Toast vor dem Unload sichtbar ist). **„Gelesen"-Label** an **`maxReached>=95`** (persistierter furthest-read, NICHT Scroll-Position) — `syncReadFlag()` schaltet nur **an** (monoton), bei init + nach jedem maxReached-Bump; sticky height:0-Wrapper + absolute Pill (oben-rechts gepinnt, keine Flow-Höhe, neumorphic pressed-sm, token-driven → Dark gratis); `READ_COMPLETE_PERCENT=95` einmal benannt (Flag **und** Resume-Check); **kein** Mid-Progress-Doppel-Label (Karte + Resume decken das ab — Master-Schärfung). **Server-Forward-Clamp** in `api_update_conversion_progress`: `last_read_percent = max(stored, range_clamp(percent))` — range-clamp **zuerst** (−7→0.0), dann `max` mit `or 0.0`-None-Schutz; Response liefert den effektiven Wert. furthest-read jetzt **doppelt garantiert** (Client-Gate + Server), Defense-in-Depth gegen jeden künftigen Seeding-/Client-Bug (Phase 1 bestätigte: aktuell keiner). **4 neue Clamp-Tests** ([tests/test_conversion_progress.py](tests/test_conversion_progress.py)): kleinerer Wert no-op + Response zeigt Bestand; größerer übernimmt; −7 hält Bestand (50); 100.0001→100. Bestehende Progress-Tests unverändert grün (alle mit None-Baseline → `max(0, x)=x`). **Verify**: pytest **213/213** (209→+4); `node --check` + `py_compile` clean. **Live-Smoke** (lokale Instanz frisch rebuilt — Code gebaked, nicht gemountet; user `smoke`, Doc id=8 „Erklärbär" 13k Zeichen): Schwelle **94 versteckt / 95 sichtbar**; **Kern-Fix (Path A)**: gelesenes Doc → Flag bei init sichtbar → runterscrollen ans Ende + **wieder hochscrollen → Flag bleibt** (`hidden=false` durchgehend, der eigentliche „Reset"-Fix); Sticky/Pill-CSS (position sticky, height 0, z-11, Pill `--nm-text-muted` auf `--nm-bg`); **Dark/Light** Token-Flip (Pill+Border `131,149,167`↔`107,112,144`, Layout intakt); **Zurück** history.back-Branch (aus Liste, referrer=/library → preventDefault + history.back) + href-Fallback `/library`; **Archivieren** PUT → **DB id=8=archive** + Toast „Archiviert" + Segment-Flip inbox→archive + Navigation zu `/library`; id=8 nach Smoke auf Pre-State (archive, NULL) restauriert. **Smoke-Vorbehalt (ehrlich, kein Code-Risiko)**: die MCP-Chrome-Tab war durchgehend **occluded** (`visibilityState:hidden`, per AppleScript nicht in den Vordergrund zu zwingen — Tab liegt in für AppleScript unerreichbarem Fenster) → `requestAnimationFrame` pausiert → der **`persistArmed`-Gate armt nicht** → der eine rAF-abhängige Pfad — *Flag erscheint **während** frischem Scroll auf einem ungelesenen Doc* — wurde **nicht direkt beobachtet**, sondern per **Dekomposition** abgesichert: (a) Schwellen-Test (94/95) beweist `syncReadFlag()` korrekt; (b) `syncReadFlag()` sitzt in `update()` **direkt nach** dem in **Phase 1 auf der echten Mintbox bewiesenen** `schedulePersist(maxReached)` im selben `persistArmed && percent>maxReached`-Gate (Linchpin per Master-Diff-Read unabhängig bestätigt) → feuert der Persist beim Scroll-ans-Ende, feuert das Flag mit. Path A (der „bleibt-beim-Hochscrollen"-Fix) ist **direkt** bewiesen. **Memory**: kein neuer Eintrag (etablierte Patterns — R2-C-Status-PUT-Reuse, Token-Dark, sticky-Pin; der Hidden-Tab-rAF-Caveat ist bereits in `reference_scroll_progress_persistence.md` dokumentiert). **Out-of-scope → Backlog**: „Fortschritt zurücksetzen" bräuchte künftig ein explizites Flag (Server clampt jetzt vorwärts) — notiert, nicht gebaut; Auto-Archivieren bei 100% (Workshop verworfen). **Mintbox-Deploy = Olivers offener Schritt** (anders als R2-E: keine Forcing-Function, reiner Code-Deploy ohne Migration — `git pull` + `docker compose up -d --build`; Browser-Hard-Reload wegen Static-Asset-Cache). **R2-Cluster A–F komplett.** **Master-Aktivität nächste**: nächstes P1 — **R3 Web-Article-Save** oder **R4-LEARN**._

_Vorgänger: R2-E ☑ done 2026-06-13 → commits `de44aee` (Backend) + `cb35c1d` (Frontend) + `e5107fe` (Tag-Leiste) + `cfa3b6c` (Tag-Daten). **Library-IA „Readwise-3er" + Tag-Leiste + Tag-Daten** — Antwort auf drei Live-Befunde nach dem ersten Nutzungs-Burst (Zwei-Leisten-IA-Verwirrung · Tag-Flut inkl. kaputter `**`-Tags · Quasi-Duplikate). **Präsentations-Sprint: die drei orthogonalen Daten-Achsen (Ort/Lese-Zustand/Priorität aus R2-B/C/D) bleiben unangetastet, nur ihre Darstellung wird zu drei Top-Level-Zielen kondensiert.** Workshop-Entscheidungen (Master 2026-06-12, nicht neu diskutiert): **eine Tab-Leiste mit drei Tabs** (Inbox/Lese-Liste/Bibliothek) ersetzt View-Switcher UND Top-Level-Status-Chips; Inbox = **untriagiert** (`lifecycle_status='inbox'` AND `queue_position IS NULL`); Queueing triagiert (Status bleibt inbox, Item verlässt die Inbox), De-queue eines noch-inbox-Items **fällt zurück** in die Inbox; Ort-Filter (war Status-Chips) zieht in die Bibliothek; `view=reading` entfällt ersatzlos (Single-User); Tag-Leiste **Top-15 nach Count + „+N weitere" + Typeahead**; Tag-Bereinigung **nur CONVERTER-seitig**, Quasi-Duplikate bleiben manuell mergebar. **Phase 1 Backend** (`de44aee`): `view=inbox` (untriagiert) + Weiterlesen-Sektion-Daten (`reading_items`, vom alten `view=reading` übernommen: `0<%<95`, unqueued, nicht-archiviert, sort `updated_at desc`) nur in `view=queue` + `inbox_count` (untriagiert, global, in **jedem** View — Tab-Badge) + `available_tags` count-sortiert über die `conversion_tags`-Junction (tie-break alphabetisch, Top-N-ready); `view=reading`-Zweig entfernt ([app_pkg/library.py](app_pkg/library.py)). Neue `captured_templates`-Fixture (Flask `template_rendered`-Signal) für Kontext-Assertions auf Daten, die das Template erst ab Phase 2 rendert. **6 neue Tests** ([tests/test_library_ia.py](tests/test_library_ia.py) + Umbau der `view=reading`-Tests in [tests/test_reading_list.py](tests/test_reading_list.py)): Inbox zeigt nur Untriagiertes, De-queue-Rückfall, `inbox_count` filter-unabhängig, Tag-Counts. **Phase 2 Frontend** (`cb35c1d`): Karten-Markup als Jinja-**Macro** `conversion_card` (vier Render-Kontexte: Bibliothek/Inbox/Queue/Weiterlesen, explizite Parameter statt Context-Lookups); Tab-Leiste (Inbox+Badge/Lese-Liste/Bibliothek, `aria-current`); **Ort-Filter** nur in der Bibliothek als abgesetzte `.status-segmented`-Anchor-Gruppe mit Label „Ort:" (bewusst **nicht** Tag-Chip-Optik — war Teil der Verwirrung); Inbox-**Triage-Reihe** (Zur-Lese-Liste primär / Später / Archiv, Re-Komposition bestehender Aktionen); **Weiterlesen-Sektion** über der Queue (nur wenn nicht leer); vier Empty-States (DE, ≤3 Sätze). `library.js`-**Reload-Gates generalisiert** (Memory `reference_reorder_over_filtered_set`): Reload wenn die Karte die sichtbare Menge verlässt (queue+archive wie R2-D, **neu** inbox+Status≠inbox, queue-Toggle bei jedem queue-View-Toggle + inbox-add). **Phase 3 Tag-Leiste** (`e5107fe`): Top-15-Chips nach Count, **aktiver `?tag`-Chip immer sichtbar** (auch jenseits Top-15 + Phantom-Tag), „+N weitere"-Toggle (volle alphabetische Liste, collapsed Default, kein Persist, ehrliches N), native `<datalist>`-Typeahead (GET-Form, Hidden-Inputs replizieren die `pagination_args`-Semantik der Chips). **`[hidden]`-Guard** (`display:none !important`) — Live-Smoke-Fund: die `display:contents`-Chip-Listen überschrieben das UA-`hidden`-Attribut, beide Listen rendeten gleichzeitig (Bootstrap shippt denselben Reset). **Phase 4 Tag-Daten** (`cfa3b6c`): zentrale `Tag.normalize_name` ([models.py](models.py)) — **ein Gate für alle Pfade** (UI/Ingest/Script), R2-A-lowercase+trim plus Markdown-Artefakt-Strip (`*`/`` ` `` überall, `[`/`]` an den Rändern, Whitespace-Kollaps); `get_or_create` nutzt sie, Rückgabe-Kontrakt unverändert (None→skip/400). **One-off `scripts/cleanup_tags.py`** (dry-run-Default, `--apply` echt, idempotent): Bestand nachziehen — rename / merge (beide Junctions umhängen mit Duplikat-Paar-Abfang, Drain wie `api_delete_tag`) / delete (leer-normalisiert); dry-run durchläuft dieselben Pfade + `rollback()` → garantierte Apply-Treue. **Typeahead-Präfix-Falle gefixt** (Master-Finding): Auto-Submit `input`→`change` umgehängt — „spacex ipo" wurde am „spacex"-Präfix weggesnappt (echtes Mintbox-Paar). **Verify**: pytest **209/209** (199→+10); **Live-Browser-Smoke** (MacChrome, Container-Rebuild pro Template-Touch — Code gebaked, nicht gemountet) volle Matrix: drei Tabs, Triage-Roundtrip Inbox→Lese-Liste→De-queue→**zurück in Inbox** mit Badge 4→3→4, Weiterlesen-Dedup (gequeuetes Progress-Item doppelt nicht), Ort-Filter, vier Empty-States, Tag-Chip-Klick/Typeahead/„+N weitere"/Nicht-Top-15-Chip/Präfix-Paar, dark+light, keine Console-Errors. **Real-welt-verifiziert auf der Mintbox** (Master-Deploy 2026-06-13): `cleanup_tags.py --apply` **102→97** (5 Merges: `ai-agenten`/`ki-agenten`/`anthropic`/`ki-ethik`/`nvidia`, 2 Renames: `ki-modell-effizienz`/`apple intelligence`, alle `**`-Präfix-Artefakte, 0 Deletes), idempotenter Re-Lauf 97→0 — der erfolgreiche Lauf beweist nebenbei, dass `cfa3b6c` auf der Mintbox deployed ist (das Script importiert die Runtime-`normalize_name`). **Künftige Newsletter-Pushes (Cron Di/Fr) laufen jetzt durchs gehärtete Gate.** **Backup-Disziplin**: künftige Prod-Daten-Mutationen wie der Tag-Cleanup vorher mit `converter.db.bak-*` im `app_data`-Volume sichern (wie beim R2-C-Deploy) — diesmal durch bewiesene Logik + No-op-Re-Lauf abgesichert, aber als Standard festhalten. **Memory**: ein neuer `reference_*` — „Vokabular zentral im Gate härten + Bestand per dry-run-Default-Script nachziehen, das dieselbe Runtime-Normalisierung **importiert** (nicht reimplementiert) und via same-path-rollback Dry-run-Treue garantiert" (Geschwister zu `reference_data_migration_idempotency`). **Out-of-scope → Backlog**: Quasi-Duplikat-Merge per Heuristik (`ki-ethik`/`ai-ethik`, manuell via „Tags verwalten"); email-automation-Vokabular-Brief NL3; Inbox-Badge-In-Place-Update statt Reload-Konsistenz (XS-Polish); Static-Asset-Cache-Busting (Browser cacht alte JS/CSS nach Deploy → Hard-Reload nötig). **Master-Aktivität nächste**: **R2-F** (Reader-Abschluss-Leiste + Fortschritts-Klarheit, S) — Dispatch-Block steht bereit._

_Vorgänger: R2-D ☑ done 2026-06-04 → commits `d466ec1` (Backend) + `d7f5097` (Swap-Fix) + `4824a5c` (Frontend). **Lese-Liste / Priority-Shortlist (geordnet)** — eine manuell geordnete Prioritäts-Lese-Liste („Was lese ich als Nächstes, in welcher Reihenfolge?"), die **dritte orthogonale Reader-Achse**. **Die drei Achsen sind jetzt komplett getrennt: Ort (R2-C inbox/later/archive) · Lese-Zustand (R2-B `last_read_percent` + Weiterlesen-View) · Priorität (R2-D Lese-Liste/`queue_position`). Schließt den R2-Cluster A/B/C/D.** Plus billiger **Weiterlesen-View** (abgeleitet aus R2-B-Progress, kein neues Schema). Workshop-Entscheidungen (Master 2026-06-04, nicht neu diskutiert): **eigene Dimension** (Favoriten-Stern unangetastet, keine Migration); **eine Float-Spalte** `queue_position` (`NULL`=nicht drauf, **Float** fürs spätere Drag-Einsortieren *zwischen* zwei Items), **kein Backfill**; **Hoch/Runter-Buttons** v1, „move up"=`queue_position`-**Swap** mit dem direkten Listen-Nachbarn; Weiterlesen=`0<%<95` sort `updated_at desc`; queue-View zeigt nur **queued + nicht-archiviert** (**kein** Auto-Dequeue beim Archivieren — reine View-Filterung, un-archivieren bringt's an alter Position zurück). **Phase 1 Backend** (`d466ec1`): `queue_position` (Float, nullable, `index=True`) + `to_dict` ([models.py](models.py)); **4. idempotenter Inline-ALTER-Block** in `_run_pending_migrations` (kein Backfill, NULL=leere Liste; [app_pkg/__init__.py](app_pkg/__init__.py); `reference_inline_sqlite_migration.md`); **`POST /api/conversions/<id>/queue`** (`add`=`max(queue_position)+1.0` idempotent / `remove`=NULL / `up`+`down`=Nachbar-Swap unter den queued Items, **eine commit-Boundary**, Rand-no-op, Ownership-404, ungültige action 400); `library()` `?view=queue` (queued+nicht-archiviert, Sort `position asc`) / `?view=reading` (`0<%<95`, Sort `updated_at desc`) — beide **AND-en on top** der type/tag/status/search-Filter, **nicht** in `has_active_filter` (Modus, kein Filter); `pagination_args(…view='')` ([app_pkg/library.py](app_pkg/library.py)). **17 Tests** ([tests/test_reading_list.py](tests/test_reading_list.py)). **Swap-Fix** (`d7f5097`, **Master-Diff-Read-Finding**): der up/down-`queued`-Set hatte den Archiv-Ausschluss des Views **nicht** → „hoch" konnte mit einem **unsichtbaren archivierten** Item tauschen (scheinbarer No-op; erreichbar genau weil Decision #5 archivierte queued Items nicht auto-dequeued). Fix: `lifecycle_status != 'archive'` im Swap-Set angeglichen (matcht den View-Filter) + Regression-Test (rot `assert 2.0 < 1.0` gegen Buggy-Code bewiesen → grün). **Phase 2 Frontend** (`4824a5c`): **View-Switcher** Alle/Lese-Liste/Weiterlesen (Modus-Umschalter, Filter-/Status-/Tag-Chip-Rows nur im Alle-Modus sichtbar); **Queue-Flag-Toggle** pro Card (⚐/⚑ neben dem Favoriten-Stern) + labeled Toggle in der Detail-Sidebar (eigener 1-Arg-JS-Pfad, fixe `CONVERSION_ID`); queue-View **1-spaltig + nummeriert** + Hoch/Runter (Edge-disabled via `loop.first/last`+`has_prev/next`, **Reload nach Swap**); **zwei View-Empty-States** (DE, ≤3 Sätze); token-driven CSS (`.view-switcher`/`.queue-btn`/`.queue-reorder`/`.queue-toggle-btn`, kein Dark-Override nötig) + TOC. **Selbst-gefangener Konsistenz-Bug**: client-seitiges Card-Entfernen ließ `#Rang`+Edge-Arrows der Restliste **stale** → queue-View-**Membership-Änderungen reloaden** jetzt (wie der Reorder); **scoped** auf R2-Cs `setStatus`: Reload **nur** `inQueueView && status==='archive'`, sonst R2-C-In-Place erhalten (Master-genehmigter Cross-Sprint-Touch, kein R2-C-Regress). **Verify**: pytest **199/199** (181→+18); **Container-Restart-Smoke** (Rebuild, Python gebaked nicht gemountet): Migration-Log **einmalig** + idempotent (DB-Probe reale Mac-dev-DB: `queue_position FLOAT`, 9 Zeilen **alle NULL**, kein Backfill); **Live-Browser-Smoke** (MacChrome, HYG3-Mount) volle Matrix: add→queue-View in Add-Reihenfolge, Hoch/Runter ordnet um + **persistiert über Reload**, de-queue, **Archiv∩Liste** (archivieren→raus aus dem View, un-archivieren→**zurück an alter Position**), `?view=reading` nur `0<%<95`, View-Switcher-Aktiv-Zustände, **Queue-Empty-State light+dark**, Detail-Toggle (separater JS-Pfad add↔remove); **reale (smoke-)DB nach Smoke sauber** (alle `queue_position` NULL, un-archivierte Test-Items wieder `archive`, Theme zurück Dark). **Cross-Feature-Sanity**: `queue_position` koexistiert mit Lifecycle/Tag/Progress/Favorit auf einer Card (Smoke: Erklärbär gleichzeitig queued + inbox + ~40%-Progress + Tag). Reading-Empty-State nicht per UI ausgelöst (Smoke-DB hat genau **1** angefangenes Item, bewusst nicht mutiert) — statischer Geschwister-Text des bestätigten Queue-Empty-States + pytest-Filter decken's. **Memory**: ein neuer `reference_*` — „Reorder/Mutation muss über **dieselbe gefilterte Menge** laufen, die der View zeigt" (generalisierte Lehre aus dem Swap-Bug). **Out-of-scope → Backlog**: Drag-and-Drop-Reorder (P3, Polish falls Hoch/Runter zäh); Favoriten-Retire-Frage (falls nach R2-D redundant). **Master-Aktivität nächste**: gebündelter **Mintbox-Deploy R2-C + R2-D zusammen** (eine `git pull` + `up --build` zieht beide Migrationen); danach nächstes P1 **R3 Web-Article-Save** oder **R4-LEARN**._

_Vorgänger: R2-C ☑ done 2026-06-04 → commits `f29c9cd` (Backend) + `3350b89` (Frontend). **Lifecycle-Status (Inbox/Later/Archive)** — Reader-Triage-Workflow: jede Conversion bekommt einen **Lifecycle-Ort** `inbox`/`later`/`archive`, **orthogonal** zum R2-B-Reading-Progress („gelesen" bleibt der Progress, **kein** 4. Status). **Schließt den R2-Cluster (A/B/C komplett).** Workshop-Entscheidungen (Master 2026-06-03, nicht neu diskutiert): 3 Orte; **eine Spalte** `Conversion.lifecycle_status` (kein eigenes Table — YAGNI, keine Historie); einmaliger differenzierter Backfill `ai_newsletter→inbox`, Rest→`archive`; Neu-Default `'inbox'`; List-Default „alle zeigen" + Filter-Chips; DE-Labels Inbox/Später/Archiv (intern `inbox`/`later`/`archive`). **Phase 1 Backend** (`f29c9cd`): `Conversion.lifecycle_status` (String(20), `default='inbox'`, `index=True`) + `to_dict` ([models.py](models.py)); 3. idempotenter Inline-ALTER-TABLE-Block in `_run_pending_migrations` ([app_pkg/__init__.py](app_pkg/__init__.py), nach `last_read_percent`, vor CSV-Junction; `reference_inline_sqlite_migration.md`) mit **einmaligem differenziertem Backfill** (`UPDATE … WHERE conversion_type != 'ai_newsletter'` **innerhalb** des Spalten-Existenz-Guards → läuft genau beim Spalten-Add, **kein Re-Clobber** bei späterem manuellem Verschieben); `LIFECYCLE_STATUSES` + `?status`-Filter (nur bekannte Werte filtern, garbage→kein Filter, kombinierbar mit `?tag`, in `has_active_filter`) + `pagination_args(…, status='')` + **alle 5 `library.html`-Call-Sites** tragen `current_status`; `api_update_conversion` PUT `lifecycle_status` gegen `LIFECYCLE_STATUSES` validiert (400 „Ungültiger Lifecycle-Status.") ([app_pkg/library.py](app_pkg/library.py)). **P3 mit-gefixt** (eh im Filter-Bereich): `library.py`-Search-Branch von `ilike`+`re.sub` auf `.contains(search, autoescape=True)` (title/content/tags) — das alte Hand-Escaping war ein stiller No-op ([reference_sqlalchemy_like_escape.md](memory/reference_sqlalchemy_like_escape.md), `import re` raus). **10 Tests** ([tests/test_lifecycle.py](tests/test_lifecycle.py) — Migration/Backfill via DROP-COLUMN-Simulation, Idempotenz-no-reclobber, PUT setzt/lehnt-ab, `?status` filtert/aktiv/kombiniert-mit-tag/pagination-bewahrt, `to_dict`). **Container-Restart-Smoke** (rebuild, Python ist gebaked nicht gemountet): Migration-Log-Line **einmalig** (plain restart → kein Re-Run, R2-A/R2-B/R1-B-B-Guards greifen); **DB-Probe reale Mac-dev-DB**: Newsletter id=9 → `inbox`, 8 Tool-Outputs → `archive`, 0 Nulls (Spalte `VARCHAR(20) DEFAULT 'inbox'`). **Phase 2 Frontend** (`3350b89`): Status-Filter-Chip-Row (Inbox/Später/Archiv → `?status`, aktiver Chip `.c-tag--active`+`aria-current`, „× Status-Filter aufheben" scope-genau) + per-Card **Status-Badge** (Title-Case + Dot, drei dezente Töne rose/amber/slate, dark gespiegelt) + neutrales **Segmented-3-Wege-Control** ([templates/library.html](templates/library.html)); Detail „Status"-Sidebar-Card mit Full-Width-Segmented ([templates/library_detail.html](templates/library_detail.html)); `setStatus` PUTet `lifecycle_status` über den globalen CSRF-fetch-Wrapper (Erfolg still — Badge+aktives Segment = Feedback, Fehler `showToast`), `applyStatusToCard`/`applyStatusControl` spiegeln den Stand ([static/js/library.js](static/js/library.js) + [static/js/library_detail.js](static/js/library_detail.js)); `.status-badge`(+3 Töne+dark) + `.status-segmented` (pressed-Track + raised aktives Segment, token-driven → dark adaptiert automatisch) + `--full` + TOC ([static/css/style.css](static/css/style.css)). **Design-Calls**: Badge≠Type-Badge (Title-Case+Dot vs. UPPERCASE-Solid → zwei klar getrennte Familien); Badge farbig / Control neutral (keine Farb-Redundanz). **Verify**: pytest **181/181** (171→+10); `node --check` clean; **Struktur-Smoke** (In-Container-Test-Client gegen reale DB+Templates) ALL PASS (9 Cards je 1 Badge+Control, `?status` filtert, `?status×?tag` 9 Links tragen beide, Detail-Card aktiv = gespeicherter Stand); **Live-Browser-Smoke** (MacChrome, HYG3-Mount): Card-Toggle **inbox→später→archiv folgt live + persistiert über echten Reload**, Detail-Toggle (separater JS-Pfad), `?status×?tag` kombiniert (URL trägt beide, beide Chips aktiv, Schnitt korrekt), Empty-State unter Filter, **Dark+Light** Badge/Chips/Segmented lesbar; **reale DB nach Smoke restauriert** (id=9 `inbox`, 0 Abweichungen), Theme zurück auf Dark. **Mac-vs-Mintbox**: Mac-dev-DB hat nur **1** Newsletter (id=9) — die 11 NL-Bridge-Newsletter liegen auf der Mintbox; die Migration ist datenagnostisch, beim Mintbox-Deploy landen dort die 11 → `inbox`, Rest → `archive`. **Safety-Net**: Pre-Migration-Backup `converter.db.bak-r2c` im `app_data`-Volume (nach Mintbox-Verify löschbar). **Memory**: kein neuer Eintrag (etablierte Patterns — Inline-Migration, R2-B-Filter-Reuse, Badge-vs-Type-Trennung); `reference_sqlalchemy_like_escape.md` aktualisiert (library.py:66-Trap jetzt gefixt). **Master-Aktivität nächste**: nächstes P1 **offen** — **R3 Web-Article-Save** oder **R4-LEARN** (Spaced-Repetition/Lernkarten, Future-Cluster); Master entscheidet beim nächsten Burst._

_Vorgänger: NL1 ☑ done 2026-06-02 → commits `77f2d60` (Typ-Foundation) + `1fa1cee` (Ingestion-Endpoint) + `7634e65` (Dedup-Escaping-Fix). **AI-Newsletter-Ingestion, CONVERTER-Seite** (Push-Richtung, Master-Workshop 2026-06-01): neuer `conversion_type` `ai_newsletter` + erster nicht-Session-/token-authentifizierter Ingestion-Endpoint, gegen den NL2 (`email-automation`, anderes Repo auf Mintbox) später pusht. **Phase 1 Typ-Foundation** (`77f2d60`): `'ai_newsletter'` in `ALLOWED_CONVERSION_TYPES` ([app_pkg/library.py](app_pkg/library.py)), Filter-`<option>` + Card-Badge + Detail-Badge `{% elif %}`-Zweig ([templates/library.html](templates/library.html) + [templates/library_detail.html](templates/library_detail.html)), `.type-ai_newsletter` als **Teal-Token** (light `#115e59` / dark `#6fd4c0`, distinkt neben Blau/Grün/Lila/Amber) in [static/css/style.css](static/css/style.css); Test rendert eine echte `ai_newsletter`-Zeile in `/library` und sichert Badge-Label + `.type-ai_newsletter`-Hook (die bestehenden Library-Route-Tests rendern real — die CLAUDE.md-„Tests fangen keine Template-Bugs"-Notiz ist hier zu konservativ). **Phase 2 Ingestion-Endpoint** (`1fa1cee`, neues Modul [app_pkg/ingest.py](app_pkg/ingest.py) mit `register(app)`, eingehängt in [app.py](app.py)): `POST /api/ingest/conversion` — shared-secret Bearer gegen `INGEST_TOKEN` via `hmac.compare_digest` (constant-time), **fail-closed** 503 ohne Token, 401 bei fehlend/falsch, **CSRF-exempt für NUR diese View** (`app.extensions['csrf'].exempt`, erster nicht-Session-Endpoint des Projekts), Token nie geloggt (Auth-Fehlschläge loggen Grund + `remote_addr`). Generisch über `ALLOWED_CONVERSION_TYPES` (Token = Trust-Boundary); Ziel-User via `INGEST_USER` (set-but-missing → 503, kein stiller Fallback) sonst `User.query.first()`, kein User → 503. **Idempotent über `source_id` in `metadata_json`** (kein Schema-Touch): Re-POST → 200 deduped, keine zweite Zeile. `topics[]` → Tags via R2-A-Junction (`Tag.get_or_create`), `report_date` (ISO) → `created_at`. Body-Validierung spiegelt die `/api/conversions`-Create-Route. 18 Tests inkl. CSRF-exempt unter **erzwungenem** CSRF (conftest hat `WTF_CSRF_ENABLED=False`, daher im Test wieder angeschaltet) + struktureller „nur diese View exempt"-Check. **Sicherheits-Selbstreview clean** (constant-time, fail-closed, Token nie geloggt, nur diese View exempt, Ownership an `target.id`, Body-Cap via globales `MAX_CONTENT_LENGTH=500MB`, kein SQLi) — Master hat den Endpoint-Diff zusätzlich selbst grounded gelesen (sicherheits-sensibelste Änderung des Projekts). **Phase 3 Kontrakt + Fix** (`7634e65` Code + Doc-Wrap-up-Commit): Kontrakt-Doc [docs/ingest_contract.md](docs/ingest_contract.md) als NL2-Spec (Endpoint, Body-Schema, 201/200-deduped/400/401/503, Dedup-Semantik, Mappings, Envs, `source_id`-Format-Empfehlung), `.env.example` um `INGEST_TOKEN=`/`INGEST_USER=` ergänzt (kein echtes Secret), **plus Dedup-Escaping-Fix** (`7634e65`, Master-Diff-Read-Finding): die Dedup-LIKE-Query escapte `_`/`%` manuell via `re.sub` aber **ohne `ESCAPE`-Klausel** → ein `source_id` mit `_`/`%` matchte die gespeicherte Zeile nicht → stilles Duplikat beim Re-POST; Fix `Conversion.metadata_json.contains(source_id, autoescape=True)`, **Regression-Test mit `_`-`source_id`** (vor Fix rot: 2. POST = 201 = 2. Zeile / nach Fix grün: 200 deduped, 1 Zeile) — derselbe unescaped-`.like`-Trap steckt noch in `library.py:66` (R2-A-Search, als P3 geloggt, nicht hier gebündelt). **curl-Smoke im echten Container** (rebuild → recreate): 503 (kein Token, beweist auch neuer-Code-live) → 401 (falsch) → 201 (echter Newsletter) → 200 deduped, genau **1 Zeile**; DB-Beleg id=9 / user_id=1 (Single-User-Fallback) / `created_at=2026-05-30` (report_date) / tags `ki-agenten`+`code-generierung`+`multimodale modelle` / `source_id` in `metadata_json`; **CSRF-exempt auch in Prod-Config bestätigt** (curl ohne Session/CSRF → 201/200, stärker als der pytest-Flag-Flip). Sample-Newsletter (id=9) bleibt in der Live-DB als Olivers Eyeball-Ziel (Dropdown + Teal-Badge + `?tag=ki-agenten` in einer Runde). Pytest **171/171** (151 → +1 Typ-Render-Test +18 Ingest +1 Dedup-Regression). **Memory**: zwei `reference_*`-Einträge — `reference_token_auth_ingest_endpoint.md` (nicht-Session-Endpoint-Pattern) + `reference_sqlalchemy_like_escape.md` (der `.contains(autoescape)`-statt-no-op-`.like`-Trap). **NL-Bridge LIVE 2026-06-03**: NL1 auf die Mintbox-CONVERTER deployed (`git pull` 354eab3 → `f15ec41` + `INGEST_TOKEN` in `.env`, `env_file: .env` injiziert ihn), NL2 (`email-automation`-Push, anderes Repo) implementiert + scharf (`CONVERTER_PUSH_ENABLED=true`); **Backfill der 11 bestehenden Notion-Newsletter** via `scripts/backfill_to_converter.py` durchgelaufen + verifiziert → erscheinen in `/library` (Typ AI-Newsletter + Topic-Tags, Dedup hält). NL2-Backfill-Bug unterwegs gefixt (email-automation-seitig: notion-client 3.1.0 Data-Source-Modell, `databases.query` → `data_sources.query` mit DB-Retrieve→`data_sources[0].id`). Forward-Push (Cron Di/Fr 07:00) ab nächstem Lauf live. **Restbeobachtung** (email-automation-Domäne, nicht CONVERTER): ob `pages.create(parent={"database_id":…})` unter notion-client 3.1.0 noch anlegt — sonst kein `page_id` → kein Forward-Push (und kein Notion-Eintrag). **Master-Aktivität nächste**: R2-C Lifecycle (Inbox/Later/Archive) — jetzt mit echten Newslettern als Triage-Material; optional `library.py:66`-LIKE-Escaping als P3._

_Vorgänger: HYG3 ☑ done 2026-06-01 → commits `e7c9486` (Infra: pip-timeout + pyc-untrack) + `901edf1` (Dual-Reset-Microcopy). **Hygiene-Block**, vier disjunkte XS-Items, kein Feature-/Schema-/getesteter-Code-Touch (Pytest **151/151 unverändert** vor und nach jeder Phase). **Item 1 pip-timeout** (`e7c9486`, [Dockerfile](Dockerfile):23): `pip install --no-cache-dir -r requirements.txt` → `… --no-cache-dir --timeout=600 --retries=5 -r …` — Resilienz gegen den MAC1-ReadTimeout am 145-MB-NVIDIA-Wheel (schließt **MAC1-FOLLOWUP-A**), inspektions-verifiziert (Standard-pip-Flags, Olivers nächstes `up --build` exerziert es ohnehin). **Item 2 pyc-untrack** (`e7c9486`): `git rm -r --cached services/pdf_extraction/__pycache__/` entfernt 7 getrackte `.pyc`-Altlasten aus dem Index (Files bleiben auf Disk; die bestehende `.gitignore`-Regel `__pycache__/`+`*.pyc` greift jetzt — **kein** gitignore-Touch); behebt den dauerhaft-dirty Working-Tree, der echte Changes maskierte (in R2-B aufgefallen, nicht von R2-B verursacht). **Item 3 live-dev-mount** (lokal-only, **KEIN Commit** — `docker-compose.override.yml` ist gitignored, `git check-ignore` bestätigt): `volumes`-Block `./static:/app/static` + `./templates:/app/templates` unter `markdown-converter` → behebt den READER-FIX-B-`docker cp`-Schmerz (static-Edits live nach Browser-Reload, templates-Edits nach `docker compose restart markdown-converter` in Sekunden, kein `--build`). **Mount bidirektional bewiesen**: vor dem Recreate DB-Volume-Persistenz geprüft (`app_data` named volume hält die SQLite-DB — kein blindes `up -d`), nach `docker compose up -d` Bind-Mounts via `docker inspect` aktiv (worker/redis unberührt, kein Rebuild), Probe-String in `static/js/library.js` → `curl localhost:5656/static/js/library.js` zeigt ihn **live ohne Rebuild** (Count 0→1) → zurückgeändert → live weg (1→0), `library.js` git-clean. **Item 4 dual-reset** (`901edf1`, [templates/library.html](templates/library.html)): reiner Microcopy-Change, `href`/`pagination_args`/`url_for`-Logik byte-identisch — Chip-Row-Reset Z.58 „× Filter zurücksetzen" → **„× Tag-Filter aufheben"** (echter Scope: cleart nur den Tag; „aufheben" statt „entfernen", weil letzteres als Tag-vom-Eintrag-löschen fehllesbar wäre), Empty-State Z.144 „Filter zurücksetzen" → **„Alle Filter zurücksetzen"** (cleart alles). Behebt den R2-B-Dual-Reset-Befund (zwei gleich-gelabelte Affordances unterschiedlichen Scopes nebeneinander auf dem 0-Treffer-Screen). **Live-Smoke** über den neuen Mount (Container-Restart + Flask-Test-Client **im Container**, GET-only/read-only/echte Route+Template+DB — für eine reine Template-Änderung deterministischer als der Chrome-Hintergrund-Tab, kein Junk/kein Cleanup): drei Szenarien grün — `?tag=eins` (Treffer) zeigt das Chip-Row-Label, dessen href nur `tag` droppt; `?tag=zzz&type=markdown` (0 Treffer) zeigt **beide** Affordances mit distinkten Labels, der Chip-Row-href **behält `type=markdown`** (Beweis: nur der Tag fällt), der Empty-State-href = bare `/library` (cleart alles). Memory: **kein neuer Eintrag** (triviale/bekannte Patterns, nichts Unerwartetes). **Master-Aktivität nächste**: R2-C Lifecycle-Status bleibt nächstes P1; optional Voll-Template-Auto-Reload via `TEMPLATES_AUTO_RELOAD`-Env als P3 (falls `restart` zu umständlich)._

_Vorgänger: READER-FIX-B ☑ done 2026-05-31 → commits `59eb0cd` (Anker-Koordinaten-Fix) + `efde5ab` (Speicher-Feedback-Politur). **Hot-Fix vor R2-C** — User-Live-Test deckte auf, dass Text-Markieren über **Block-Grenzen** (Absatz/Liste/Überschrift/Blockquote) unsichtbar blieb: „Formatierungsgrenze"-Toast, kein Span, kein Hinscrollen. **Root-Cause** (Anker-Koordinatensystem-Mismatch, mit **echtem Maus-Drag empirisch belegt VOR dem Edit**): `saveCurrentSelection` speicherte `exact` aus `selection.toString()`, das an Block-Grenzen einen **doppelten** Separator-Newline `\n\n` einfügt, während `locateHighlightOffset` in `readerRawText` (purer `nodeValue`-Concat) mit **einem** `\n` sucht → `indexOf` findet nie → `applyHighlight` false → Cross-Format. Belegte Divergenz am P→P-Übergang: `toString()`-Codes `[115,46,10,10,87,97]` vs. Roh-Slice `[115,46,10,87,97]`, `indexOf(toString)=-1` vs. `indexOf(rawSlice)=2297`. (READER-FIX-A reparierte nur das *Wrappen* über **Inline**-Grenzen — `<strong>`/`<em>`, gleicher Block, kein `\n` in `toString()`; **Block**-Grenzen brachen eine Stufe früher beim *Finden* und blieben bis hier offen. Die READER-FIX-A-„Cross-Format-verschwunden"-Bewertung galt nur Inline-Grenzen und beruhte auf synthetischen DevTools-Ranges, die das `toString()`-Umbruch-Verhalten echter Maus-Drags nicht reproduzieren — die Methodik-Lücke, die diesen Bug überleben ließ. Forward-Korrektur, nicht revisionistisch: die READER-FIX-A-Erledigt-Zeile bleibt Zeit-Snapshot.) **Anker-Fix** (`59eb0cd`, `static/js/library_detail.js`, alleiniger Code-Touch +104/−41): neuer Helper `rawOffsetForPoint(reader, container, offset)` mappt einen DOM-Punkt auf den Char-Index in `readerRawText` (Text-Node-Fast-Path = alte `preLen`-Summierung generalisiert, `comparePoint`-Fallback für Element-Container z.B. Triple-Click-Block-Selections); `extractSelectionContext` liefert jetzt `{exact, prefix, suffix}` **alle aus `readerRawText` gesliced** (`exact = fullText.slice(rawStart, rawEnd)`), `saveCurrentSelection` bezieht `exact` daher statt aus `sel.toString()` → gespeicherter Such-Key byte-identisch zu dem was `locateHighlightOffset` durchsucht, EIN Koordinatensystem. Da `exact` per Konstruktion aus `readerRawText` stammt, **kann keine echte Auswahl mehr Cross-Format werden** (`crossFormatHighlightIds` real leer); der vorhandene READER-FIX-A-Multi-Node-Wrap rendert Block-übergreifende Auswahlen als N Spans. **Backward-Compat-Fallback KEPT** (nicht descoped): `locateHighlightOffset` gibt jetzt `{start, end}` zurück, Fast-Path `indexOf` exakt + Prefix/Suffix-Scoring, bei Miss `locateWhitespaceTolerant` (Whitespace-Runs→ein Space in `fullText` UND `exact`, Match im normalisierten Raum, Rückmapping auf Roh-Offsets via Index-Map) — **rettet Olivers Alt-Highlights live** (id 24→10 Spans, id 25→12 Spans, vorher 0 = unsichtbar). `applyHighlight` nutzt `loc.end` statt `start + exact.length` — kritisch für den Fallback, wo der Roh-Span breiter ist als das normalisierte `exact` (sonst verstümmelter Rescue). **1.4 Display**: Card zeigt `h.exact.replace(/\s+/g,' ')` (nur Anzeige, gespeicherter Such-Key unangetastet). **Speicher-Feedback** (`efde5ab`, +17/−2): `addTagToHighlight` toastet jetzt auch bei `200` (idempotenter Re-Add: „Tag bereits vorhanden.") statt still — vorher nur `201`; `renderHighlightList` bewahrt den `data-expanded='true'`-State offener Sidebar-Cards über den Re-Render (Snapshot vor `innerHTML=''`, nur Klasse wiederhergestellt, kein `scrollToHighlight`) → Tag-Add/-Remove klappt die aktive Card nicht mehr zu, der frische Tag „verschwindet" nicht optisch (genau Olivers Erlebnis). Item 4 (Cross-Format-Residual-Auto-Expand) **descoped** weil nach dem Anker-Fix unerreichbar; Warning-Toast-Pfad bleibt defensiv. **Live-Smoke ausschließlich mit echten CDP-Maus-Drags** (Methodik-Pflicht des Sprints, da synthetische Ranges genau diesen Bug maskieren): volle Matrix grün — Single-Node 1 Span, Inline-`<strong>` 3 Spans, **Absatz-Grenze 2 Spans** (DER FIX), Überschrift-Grenze 2 Spans, Listen-Items 4 Spans, Blockquote-Grenze 2 Spans, **Reload-Re-Apply alle 9 Highlights `zeroSpanIds:[]`** (der Locate-Pfad wo der Bug biss), Alt-Cross-Format vom Fallback gerettet, Degenerate-Whitespace collapsed graceful, Sidebar-Sprung mit Flash ohne Warning; plus vollständiger Mutations-Feedback-Audit (Tag-Add 201/200, Card-bleibt-offen, Tag-Remove, Note-Save, Delete — jeder mit sichtbarem Resultat). **Pytest 151/151 unverändert** (Frontend-only, kein Schema/Backend/Test-Touch). DB nach Smoke sauber auf Olivers [24,25,26]. **Caveat**: `static/` ist im Mac-Stack nicht volume-gemountet → Smoke lief via `docker cp` der JS in den Container (md5-verifiziert); reguläres `docker compose up --build` backt `efde5ab` byte-identisch ein (als P3-MAC-Dev-Reminder geloggt). **Master-Aktivität nächste**: R2-C Lifecycle-Status bleibt nächstes P1; plus Oliver-Vordergrund-Browser-Verifikation des Markierens über Absatz-/Listen-Grenzen empfohlen (Sub-Thread smokte auf gedrosseltem Hintergrund-Tab — synthetische Drags dort flaky wegen des 10ms-`setTimeout`-Race im mark-on-mouseup, kein Code-Problem)._

_Vorgänger: R2-B ☑ done 2026-05-29 → commits `4ff36a8` (Cluster A Reading-Progress) + `8b7e4f3` (Cluster B Tag-Filter). Zwei Cluster: schließt den explizit aufgeschobenen READER-MODE-Reading-Progress-Vorbehalt (last-read-Position) und baut die Tag-Filter-Chip-Row auf dem R2-A-`Conversion.tag_refs.any(...)`-Junction-Pfad auf. **Cluster A Reading-Progress-Persist** (`4ff36a8`): neue nullable `Conversion.last_read_percent`-Float-Spalte (Prozent 0–100, Workshop-Entscheidung #1 — robust gegen Content-Längen-Änderung, subsumiert gelesen/ungelesen) via dritten idempotenten Inline-ALTER-TABLE-Block in `_run_pending_migrations` ([app_pkg/__init__.py](app_pkg/__init__.py), vor dem CSV-Junction-Call, Pattern aus Memory `reference_inline_sqlite_migration.md` — Log-Line `R2-B: conversion.last_read_percent column added via ALTER TABLE` nur beim ersten Start, Container-Restart-Smoke bestätigt Count 1→1), plus `to_dict`-Erweiterung. Neue Route `PATCH /api/conversions/<id>/progress` in [app_pkg/library.py](app_pkg/library.py): Ownership via `get_owned_conversion`→404, Non-dict-Body→400, **Bool explizit abgelehnt** (`isinstance(True,int)` ist `True`), fehlt/None/nicht-numerisch→400 (atomarer Endpoint wie R1-B-B-Note-PATCH), out-of-range→**clamp 0–100** statt 400 (Fire-and-forget-Scroll-Signal, Rundungsartefakte wie 100.0001), 200 + `{success, last_read_percent}`. Frontend in `initReadingProgress` ([static/js/library_detail.js](static/js/library_detail.js)): `maxReached` aus `PageData.lastReadPercent` geseedet (furthest-read, Workshop #4 — Zurückscrollen senkt den Fortschritt nicht), Inline-Throttle ~2s (leading+trailing, **kein** `_utils.js`-Helper bei Single-Call-Site — Memory `feedback_helper_reuse_design_choice.md`), `persistProgress(p, useKeepalive)` über den globalen base.html-fetch-Wrapper (CSRF-Header automatisch, Fehler still geschluckt — kein Toast), keepalive-Flush bei `visibilitychange→hidden` (überlebt Tab-Close/Navigation; **nicht** `navigator.sendBeacon`, weil der den CSRF-Header nicht setzen kann — Workshop-Entscheidung), Resume-on-Open via `requestAnimationFrame` nur bei `1 < pct < 95` (`scrollTop = pct/100 * scrollable`; ≤1 = Top, ≥95 = oben als „gelesen" öffnen), `persistArmed`-Gate erst im Folge-rAF scharf, damit der programmatische Resume-Scroll sich nicht selbst persistiert. Card-Indikator in [templates/library.html](templates/library.html): 3px-Balken als letztes `.c-card`-Kind (von `overflow:hidden`+`border-radius` an den Ecken geclippt), versteckt bei null/0, Akzent-Gradient partiell, `.card-progress--read` (voller gedämpfter `--nm-text-muted`-Balken + „Gelesen"-Title) bei ≥95, sonst „X % gelesen"-Title; `.card-progress{,__fill,--read}`-CSS token-driven + TOC-Eintrag. **Cluster B Tag-Filter** (`8b7e4f3`): `?tag=<name>`-Filter in der `library()`-Route — `request.args.get('tag','').strip().lower()` (incoming auf den lowercase+trim-gespeicherten Namen normalisiert), `query.filter(Conversion.tag_refs.any(Tag.name == tag))` (derselbe Junction-Pfad wie der R2-A-Search-Branch, `==` statt `ilike`), `tag` in `has_active_filter`, `pagination_args(…, tag='')`-kwarg (droppt leeren Tag aus URL — Workshop #2: nur URL als Source, kein localStorage, bookmarkbar, frischer `/library`-Aufruf zeigt Default), `available_tags = Tag.query.filter(Tag.conversions.any(Conversion.user_id == …)).order_by(Tag.name)`. Chip-Row in `library.html` unter der Filter-Bar (nur wenn `available_tags`), jede `.c-tag`-Pille → `?tag=<name>` mit `page=1` + bewahrten Restfiltern, aktiver Chip `aria-current` + `.c-tag--active` (Akzent-Ring `inset 0 0 0 1px var(--nm-accent-hover)`) + `text-neo-text font-medium`, „× Filter zurücksetzen"-Link (clear tag, behält übrige Filter) wenn Tag aktiv; **alle 3 Pagination-Call-Sites** um `current_tag` erweitert (URL-Filter überlebt Seitenwechsel); bestehender `has_active_filter`-Empty-State deckt den 0-Treffer-Fall automatisch ab (kein neuer Empty-State). **Tests**: 20 neue → Pytest **151/151 grün** (131 → +20): 12 in [tests/test_conversion_progress.py](tests/test_conversion_progress.py) neu (PATCH valid/clamp-oben/clamp-unten/int/non-numeric/bool/missing/null/non-dict/foreign-404/to_dict/Migration-Idempotenz via DROP-COLUMN-then-ALTER + zweiter-Lauf-no-op), 8 in [tests/test_conversion_tags.py](tests/test_conversion_tags.py) erweitert (tag-surfaces-only-matching/empty-shows-all/case-normalize/type-combine/favorites-combine/unknown-tag-empty-state/pagination-preserve via Regex-page2-Link/chip-row-available-tags). **Live-Smoke** (Container neu gebaut, MacChrome): **Cluster B vollständig durchgeklickt** (server-gerendert, keine rAF-Abhängigkeit) — Chip-Row eins/ki/produkt sortiert, `?tag=ki`→nur doc1 + ki-Chip-aktiv + Reset, `?tag=produkt`→doc1+doc2, Typ∩Tag-Intersect (`ki&document_to_markdown`→0/Empty-State, `ki&markdown_input`→doc1), `?tag=gibtsnicht`→Empty-State „Keine Treffer mit aktuellen Filtern" + beide Reset-Affordances, Reset behält Typ, **Dark+Light** Filter-Chips (aktiv+inaktiv) **und** `.c-tag`-Card-Strip lesbar → **schließt den R2-A-Dark-Mode-Reminder**. **Cluster A** teilweise via Direkt-Invocation, weil der MCP-Tab `hidden` ist (rAF pausiert → automatisches Arming/Resume/Flush nicht live-klickbar, die OS-Chrome-Fokus-Freigabe lief in 300s-Timeout → computer-use disconnected): PATCH→200/CSRF-ok/**kein 400** (exakter fetch-Pfad durch den Wrapper), Fill-Bar folgt Scroll (39,9%), Card-Balken 40% partiell + 100% gedämpft „Gelesen" + null→kein-Balken (dark+light zoom-verifiziert), Resume-Daten-Fluss (`PageData.lastReadPercent===40` als `number` via `tojson`) + Formel + Scroll-Anwendung (→211px + Fill 40%), Container-Restart-Migration idempotent. **Cross-Cluster-Sanity**: doc1 zeigt in `?tag=produkt` den 40%-Balken **und** ist tag-gefiltert sichtbar (doc2 ohne Balken) — beide Cluster koexistieren konfliktfrei. **Files** 8 (Cluster A) + 4 (Cluster B, davon `app_pkg/library.py`+`templates/library.html`+`static/css/style.css` in beiden): `models.py`, `app_pkg/__init__.py`, `app_pkg/library.py`, `templates/library_detail.html`, `static/js/library_detail.js`, `templates/library.html`, `static/css/style.css`, `tests/test_conversion_progress.py` (neu), `tests/test_conversion_tags.py`. **Workshop-Entscheidungen** (Master 2026-05-29, nicht neu zu diskutieren): Prozent-0–100-Schema, nur-URL-Filter, furthest-read-Persist, throttle+keepalive-statt-`sendBeacon`. **Smoke-Residuen** (gewolltes Foundation-Recycling, Handoff-Gotcha §6 — nützliches R2-C-Seed): doc1=40% + doc5=97% `last_read_percent` (doc5 = natürlicher Archive-Kandidat), doc2 Tag `produkt`. **Master-Aktivität nächste**: R2-C Lifecycle-Status (Inbox/Later/Archive) als nächstes P1._

_Vorgänger: READER-FIX-A ☑ done 2026-05-26 → commit `7f331c1` (Multi-Node-Highlight-Wrap via Range-Walking + Sidebar-Card-Expand-on-Click). Zwei UX-Limitations aus dem R1-B-A/B-B-Cluster behoben die nach dem READER-MODE-Live-Test als alltägliche Reibung aufgefallen waren. **Phase 1.1 Multi-Node-Highlight-Wrap**: `rangeForOffsets` in [static/js/library_detail.js](static/js/library_detail.js) returnt jetzt nativ Multi-Node-Ranges (bail-Branch bei `startNode !== endNode` entfernt), neuer Helper `collectTextRangesInRange` walked Text-Nodes via TreeWalker mit `acceptNode → range.intersectsNode`-Filter (rooted auf `commonAncestorContainer` mit Text-Node-Fallback auf `.parentNode`, Whitespace-only Text-Nodes übersprungen), `wrapSelectionAsHighlight` wrappt jedes Sub-Range in einen eigenen `<span class="highlight" data-highlight-id="X">` in **Reverse-Order** (Ende → Anfang, damit frühere DOM-Mutationen die späteren Offsets nicht verschieben — Standard-Pattern Hypothes.is/Annotator.js), jedes Sub-Range ist single-text-node und `surroundContents` kann nicht mehr `InvalidStateError` werfen. `applyHighlight` refactored auf `wrapSelectionAsHighlight(range, highlight.id)` statt direkt `surroundContents`. `removeHighlightSpans(id)` aggregiert alle Spans pro `data-highlight-id` + `reader.normalize()` **einmal am Ende** statt N-mal pro Span (Cleanup-Symmetrie zum Multi-Span-Wrap). `crossFormatHighlightIds`-Set bleibt drin als Defense für absolute Edge-Cases (z.B. Selection nur in Whitespace-only Nodes), in der Praxis nach Phase 1.1 leer. **Phase 1.2 Sidebar-Card-Expansion**: Card kriegt `data-expanded="false"` als Default-State (per-doc-view ephemeral, nicht persistiert); Click toggelt das Attribut, scroll-to-Highlight + Flash nur beim **Expand** (Collapse soll User-Lese-Position nicht weg-bewegen) und nur bei Non-Cross-Format. Markup-Refactor: `__exact` (statt `__snippet`) clamped via `-webkit-line-clamp: 3`, `__note` auf 2 Zeilen, `[data-expanded="true"]`-Override hebt clamp auf (`unset` + `overflow: visible`). Tag-Visibility-Toggle: JS rendert **alle** Tags ins DOM (statt nur die ersten 3), CSS hidet `:nth-child(n+4):not(.highlight-tag-chip--more)` in collapsed und die `--more`-Pseudo-Chip in expanded. Edit-Button im Card-Markup mit `display: none` per Default, in expanded `inline-flex`, `evt.stopPropagation()` damit Click nicht die Card collapsed; öffnet `showHighlightActionPopover(card)` analog R1-B-C Cross-Format-Bridge — Card-Element ist der Anker (`getBoundingClientRect` + `dataset.highlightId` beide vorhanden), Popover positioniert sich unter der expanded Card und bleibt offen während Card im Hintergrund expanded bleibt. Cross-Format-Card-Verhalten: Click expandiert die Card (zeigt vollen Text + Warning-Icon + Edit-Button), kein Scroll, kein Toast (⚠ + border-left zeigen den Status schon); Edit-Button ist der Pfad zum Popover (statt vorher direkt-Popover-Bridge). 2-Click-Reibung vertretbar, weil Cross-Format nach Phase 1.1 fast nie auftritt. `truncateForCard`-Helper als dead-code entfernt (CSS clamp übernimmt). **Phase-2-Fix Tag-Render-Bug**: während Smoke 13 (Tag-Expand >3 Tags) gefunden — `renderSidebarCardTagChips` renderte nur die ersten 3 Tag-Chips ins DOM via `tags.slice(0, SIDEBAR_TAG_CHIPS_VISIBLE)`, aber das CSS-only-Hide-Pattern braucht alle Tags im DOM (`:nth-child(n+4)` greift nur wenn die Children da sind). Fix: alle Tags rendern, CSS regelt Sichtbarkeit. **Phase-2-Fix Tag-Chip-Click-Bubble**: zusammen mit dem Tag-Render-Fix als XS-Patch (Master-Disposition #3) — `chip.addEventListener('click', evt => evt.stopPropagation())` damit Tag-Chip-Click in expanded Card die Card nicht ungewollt collapsed. Verifiziert via direkten `tagChip.click()`-Test (Card bleibt expanded). **Lakmus-Test grün**: doc 4 id=4 cross-format-Highlight „HOST: Willkommen " ist nach Phase 1.1 als 2 DOM-Spans sichtbar („HOST:" parent=STRONG-span, " Willkommen " parent=SPAN), `crossFormatHighlightIds`-Set leer, Card in Sidebar normal (kein ⚠). **18 Smoke-Schritte alle grün** plus Smoke 17 Pygments-Class-Kollisions-Defense bestätigt (`pygmentsBlocksMatchUserSelector: 0` — `<div class="highlight">` matched NICHT `span.highlight[data-highlight-id]`). Multi-Node-Smoke-Beispiele: Smoke 3 (Multi-Strong neu) id=24 = 2 Spans, Smoke 4 (Heading-Boundary über H2) id=29 = 6 Spans inkl. H2-Parent, Smoke 5 (List-Item-Boundary) id=27 = 5 Spans über 2 `<li>` + STRONG/EM/S, Smoke 6 (Single-Node-Regression) id=25 = 1 Span (R1-B-A-Path nicht gebrochen), Smoke 8 (Delete Multi-Node aggregiert) entfernt beide Spans gleichzeitig + reader.normalize() sauber. Card-Smokes: Smoke 10/11 (Expand/Collapse Toggle), Smoke 12 (Note-Expand long note 36px clamp → 109px full), Smoke 13 (Tag-Expand 3+„+2" → alle 5), Smoke 14 (Edit-Button → Popover unter expanded Card). **Hover-Sync-Disposition** (Master-Eval #1): Hover bei id=4 zeigt nur den gehoverten Span dunkler (warning-tint), die anderen bleiben hellgelb — subjektive Beurteilung stört nicht (transient Geste, subtle Farbdifferenz, Spans direkt benachbart ohne sichtbares Gap), kein JS-Hover-Sync-Patch eingebaut. Falls später UX-Feedback → separater Polish, als P3-Reminder ergänzt. **Cross-Format-Card 2-Click-Pfad-Disposition** (Master-Eval #2): praktisch nicht testbar weil `crossFormatHighlightIds` empty bleibt; theoretisch Konsistenz mit normalen Cards rechtfertigt 2 Clicks bei Cross-Format als Edge-Case. **Files** 2 modifiziert + 1 neu (Sprint-Prompt-Doc): [static/js/library_detail.js](static/js/library_detail.js) (`collectTextRangesInRange` + `wrapSelectionAsHighlight` + `removeHighlightSpans` neu, `rangeForOffsets` + `applyHighlight` + `renderSidebarCardTagChips` + `renderHighlightList` + `deleteActiveHighlight` refactored), [static/css/style.css](static/css/style.css) (Card-Expand-Mechanik mit line-clamp + `[data-expanded]`-Overrides + `:nth-child(n+4)` Tag-Hide + Edit-Button `display: none → inline-flex` + `__snippet → __exact`-Rename in Cross-Format-Cascade), plus Sprint-Prompt-Doc [docs/archive/sprint-prompts/SPRINT_READER-FIX-A_multinode-highlight-card-expansion_2026-05-26.md](docs/archive/sprint-prompts/SPRINT_READER-FIX-A_multinode-highlight-card-expansion_2026-05-26.md). **Pytest 131/131 weiter grün** (Backend nicht touched, Frontend-only). **Memory-Eintrag** [reference_dom_range_walking.md](memory/reference_dom_range_walking.md) als verallgemeinerbares Pattern für künftige Selection-/Annotation-/Term-Highlight-Features. **Master-Aktivität nächste**: R2-B Filtered Views + Reading-Progress-Persistierung (last-read-Position-Schema, R2-B-Vorbehalt aus READER-MODE noch offen) oder R2-C Lifecycle-Status (Inbox/Later/Archive)._

_Vorgänger: READER-MODE ☑ done 2026-05-26 → commit `e255c3f` (distraction-free Sidebar-Toggles + Reading-Progress-Bar + Mark-on-Mouseup). Drei Frontend-only-Sub-Features im `library_detail`-Reader, kein Schema- oder Backend-Touch. **Distraction-Free-Toggles**: beide Sidebars (globale links via `base.html`, Detail-Sidebar rechts via `library_detail.html`) per Floater-Button am Bildschirmrand ein-/ausklappbar. State in localStorage via `loadViewState`/`saveViewState`-Helper aus [_utils.js](static/js/_utils.js) (Keys `reader.globalSidebar` + `reader.detailSidebar`). Body-Klassen `global-sidebar--collapsed` + `detail-sidebar--collapsed` triggern CSS-Grid-Track-Overrides mit 200ms ease-Transition; eingeklappte Sidebar zusätzlich `visibility: hidden; pointer-events: none` bzw `opacity: 0; overflow: hidden`. Floater als 22×56px `position: fixed` an linkem/rechtem Bildschirmrand mit `top: 50%; transform: translateY(-50%)`, Inline-SVG-Chevron der per JS zwischen Chevron-Left/Right rotiert (aria-expanded synchron). Desktop-only via `@media (min-width: 1024px)` — Mobile bleibt beim existing Hamburger-Pattern. **Reading-Progress-Bar**: 3px-sticky-Bar am Top des Viewports, Fill-Width entspricht Scroll-Fortschritt mit Linear-Gradient (`--nm-accent` → `--nm-accent-cta`). `findScrollAncestor`-Helper sucht nach overflow-Ancestor und fällt zurück auf `document.scrollingElement`/`window` — robust gegen Layout-Variation. Bar versteckt bei kurzen Docs via `MIN_SCROLLABLE_PX=30`-Threshold (fängt Sidebar-Mini-Overflow von ~5px wenn Detail-Sidebar geringfügig höher als Reader-Content). Resize-Listener triggert update, scrollable wird neu berechnet. **Persistierung der Scroll-Position bleibt R2-B-Vorbehalt** — hier nur visuelle Live-Anzeige. **Mark-on-Mouseup**: schwebender Markieren-Button (`#highlight-create-btn`) entfernt, dazu `hideHighlightCreateBtn` + `positionHighlightCreateBtn` + `selectionchange`-Listener aus [library_detail.js](static/js/library_detail.js) raus. Neuer `mouseup`-Listener am `.reader-view`: Cmd/Ctrl-Halten → bail (Copy-Pfad bleibt frei), Selection-`anchorNode`/`focusNode` müssen beide im Reader sein (Outside-Bail), `text.trim().length < MIN_HIGHLIGHT_LENGTH=3` → bail (Threshold gegen Accidental-Selections von Drift-Klicks), sonst direkter `saveCurrentSelection`-Call. 10ms-`setTimeout`-Defer wegen Selection-Propagation-Delay in Chrome. Normal-Save-Toast weggelassen (gelber Highlight-Span = sofortiges visuelles Feedback, Reader-Mode bleibt still), Cross-Format-Warning-Toast (`level: warning`, „Markierung gespeichert, Anzeige nicht möglich (Formatierungsgrenze).") bleibt unverändert weil dort kein Span sichtbar wird. Existing-Verhalten unverletzt: Click-auf-Highlight-Span → Popover (via Click-Handler ohne Drag, Selection collapsed), Cross-Format-Sidebar-Card-Click → Popover über Card (R1-B-C-Bridge), Triple-Click-Absatz-Selection → Save (legitime Geste). **Open-Page-Layout** (Master-Pivot mid-Sprint nach erstem Smoke-Bericht): `#content-body` `overflow-auto max-h-[calc(100vh-280px)]` weg → Reader-Container nimmt natural height; äußerer `<div class="flex-1 p-6 lg:p-8 overflow-auto">` ohne `overflow-auto` → window/HTML scrollt natürlich Readwise-Reader-Style. Beide Sidebars `lg:sticky lg:top-0` bleiben beim Page-Scroll am Viewport-Top kleben (verifiziert `leftSbPos: sticky, top: 0`, `detailSbPos: sticky, top: 0` bei scrollY=400) — globale via `lg:h-screen`, Detail via `lg:max-h-screen lg:overflow-y-auto lg:py-2` für internes Scrollen bei vielen Markierungen. **CSS-Grid-Quirk-Workaround** (kritisch — ohne den Patch greifen die Toggle-Track-Overrides NICHT): beide Sidebars bekommen `min-w-0` Tailwind-Klasse, sonst zwingt CSS-Grid-Track-Sizing-Algorithmus bei `min-width: auto` (Default) den Track auf min-content der Sidebar-Kinder zu honoring — `grid-template-columns: 0 1fr` wird ignoriert, Track bleibt bei 250px/300px. Mit `min-w-0` greift der Track-Collapse sauber (live verified `cols: 0px 1462px` / `cols: 1124px 0px`). **S3-CSS-Patch** Markdown-Converter-Edge-Case: `body.reader-active .sidebar-toggle-floater--global { display: none }` versteckt globalen Floater im Markdown-Converter-Reader-Mode (`reader-active` ist Editor/Preview-Stack im `markdown_converter.html`, Sidebar bereits dort via `body.reader-active #sidebar { display: none !important }` gehidden — Floater redundant, kleiner Background-Match-Edge-Case bestätigt + Patch akzeptiert). **Naming-Trennung** sauber: `body.reader-active` (Markdown-Converter Reader-Mode) vs. neue `body.global-sidebar--collapsed`/`body.detail-sidebar--collapsed` (library_detail Distraction-Free) koexistieren konfliktfrei. **Files**: 5 modifiziert + 1 neu — [templates/base.html](templates/base.html) (Floater + `min-w-0` + `lg:sticky lg:top-0 lg:h-screen` + `.layout-grid`-Klasse), [templates/library_detail.html](templates/library_detail.html) (Floater + Reading-Progress-Container + `min-w-0` + `lg:sticky lg:top-0 lg:max-h-screen lg:overflow-y-auto` + Markieren-Button-Removal + outer `overflow-auto` weg + `#content-body` overflow weg + `.detail-grid`-Klasse), [static/js/library_detail.js](static/js/library_detail.js) (`initMarkOnMouseup` + `initReadingProgress` mit `findScrollAncestor` + `initDetailSidebarToggle` + `MIN_HIGHLIGHT_LENGTH=3` + `MIN_SCROLLABLE_PX=30` + `applyDetailSidebarState` mit Chevron-Toggle + DOMContentLoaded-Hook erweitert + Markieren-Button-Code raus + Toast-Disposition), [static/js/base.js](static/js/base.js) (globaler Sidebar-Toggle IIFE mit `applyState`-Helper + Chevron-Toggle), [static/css/style.css](static/css/style.css) (neuer Block `===== READER MODE — TOGGLES =====` mit `.sidebar-toggle-floater{,--global,--detail}` + `.layout-grid`/`.detail-grid` Transitions + collapsed-State-Overrides für beide Grid-Cols und Sidebar-Items + `.reading-progress`/`.reading-progress__fill`/`.reading-progress--hidden` + `body.reader-active .sidebar-toggle-floater--global { display: none }` + alte `.highlight-create-btn`-Styles entfernt + `.highlight-action-popover`-Block konsolidiert + TOC-Eintrag ergänzt). Plus Sprint-Prompt-Doc [docs/archive/sprint-prompts/SPRINT_READER-MODE_distraction-free_2026-05-25.md](docs/archive/sprint-prompts/SPRINT_READER-MODE_distraction-free_2026-05-25.md). **Pytest 131/131 grün** (kein Backend-Touch, reines Frontend + Templates + CSS). **Live-Smoke 20 Schritte + 4 Master-Schwerpunkte alle abgehakt** — (S1) Mark-on-Mouseup-UX-Akzeptanz: 3-char-Threshold konservativ aber nicht strikt (fängt Drift, lässt API/TLS-Akronyme zu), Cmd-Bypass hält Standard-Copy frei, Empfehlung bleiben — bei häufigen Accidentals → 5 als nächste Stufe; (S2) Sidebar-Toggle-Smoothness: 200ms ease auf grid-template-columns + 150ms opacity fluide, kein Layout-Sprung **nach** dem `min-w-0`-Patch (vor dem Patch hätten Track-Werte zurückgesprungen — Layout-Bug); (S3) Markdown-Converter Floater-Edge-Case bestätigt + Patch akzeptiert; (S4) Cross-Format-Sidebar-Card-Click bei collapsed-Sidebar: `jsError: null`, JS-Click läuft sauber durch, Popover öffnet im DOM aber visuell hinter collapsed-Sidebar (User-Click ist eh blockiert via pointer-events:none — kein User-Impact). **Master-Aktivität nächste**: R2-B Filtered Views + Reading-Progress-Persistierung (last-read-Position-Schema, R2-B-Vorbehalt aus diesem Sprint zurückgestellt) oder R2-C Lifecycle-Status (Inbox/Later/Archive)._

_Vorgänger: R2-A ☑ done 2026-05-25 → `conversion_tags`-Junction + CSV-Migration + Tag-Picker statt CSV-Input + Library-Search Junction-Branch. **R2-Cluster nach R2-A**: R2-B (Filtered Views + Reading-Progress) und R2-C (Lifecycle-Status Inbox/Later/Archive) bleiben P1 offen. **Schema**: neue Junction-Tabelle `conversion_tags(conversion_id, tag_id)` mit `ondelete='CASCADE'` analog `highlight_tags` (R1-B-C), via `db.create_all()` automatisch angelegt; M:N-Relationship `Conversion.tag_refs = db.relationship('Tag', secondary=conversion_tags, lazy='joined', backref=db.backref('conversions', lazy='dynamic'))` damit `Conversion.to_dict()` die Tags ohne extra Query mitzieht; `Conversion.tags`-CSV-Spalte **bleibt als Dead-Column** (Kommentar im Model markiert, SQLite-DROP-COLUMN ist Table-Rebuild — separater Cleanup-Sprint möglich); **`Tag.get_or_create(user_id, name)`-Classmethod** als Single-Source-of-Truth-DRY-Anker mit `MAX_NAME_LEN=80` — drei Call-Sites (`POST /api/highlights/<id>/tags`, neue `POST /api/conversions/<id>/tags`, Migration-Helper) konsumieren dieselbe Normalisierungs-Logik (lowercase+trim + Empty/Oversize-Reject); Inline-`_normalize_tag_name`-Helper aus R1-B-C `app_pkg/tags.py` durch die Classmethod ersetzt. **Migration**: neuer Helper `_migrate_conversion_tags_csv_to_junction(app)` in [app_pkg/__init__.py:97](app_pkg/__init__.py:97) als zweiter Eintrag nach R1-B-B-Note-Migration in `_run_pending_migrations`, **idempotent via leerer-CSV-als-Marker**: Filter-Query `Conversion.tags != ''` findet beim zweiten Container-Start keine Candidates mehr weil `conv.tags = ''` nach jeder erfolgreichen Migration gesetzt wurde. **Live-verifiziert**: gesedete Conv 1 mit `tags = ' KI , Produkt , eins, eins '` → erster Restart Log-Line `R2-A: migrated 1 conversions from CSV to conversion_tags junction`, CSV danach `''`, Junction 3 Rows (`ki`, `produkt`, `eins` — Lowercase, Trim, Dedup); zweiter Restart **keine erneute Log-Line** (Idempotenz bestätigt). **Backend**: 2 neue API-Routes in [app_pkg/library.py:172](app_pkg/library.py:172) — `POST /api/conversions/<id>/tags` (find-or-create-attach + Idempotenz 201 bei neuer Junction, 200 bei bestehender — Pattern identisch zu R1-B-C-Highlight-POST) und `DELETE /api/conversions/<id>/tags/<tag_id>` (Detach, Tag bleibt erhalten); **PATCH-Handler-`tags`-Pfad entfernt** ([library.py:155](app_pkg/library.py:155) Dead-Path nach Migration — Frontend nutzt nichts mehr, sauberer Code); `GET /api/tags` ([app_pkg/tags.py:44](app_pkg/tags.py:44)) erweitert um **zweiten Outer-Join** auf `conversion_tags`-Subquery — Response-Payload pro Tag jetzt mit `highlight_count` + `conversion_count`; `DELETE /api/tags/<id>` cascadiert auf **beide** Junctions (`highlight_tags.delete().where(...)` + `conversion_tags.delete().where(...)` + `session.delete(tag)` in einer commit-Boundary). **Frontend-Migration**: [templates/library.html:76](templates/library.html:76) Card-Strip aus `conv.tag_refs` (Tag-Objects mit `.name`) statt CSV-Split (Z.78 `conv.tags.split(',')` → Z.78 `for tag in conv.tag_refs`); [templates/library_detail.html:155](templates/library_detail.html:155) Sidebar-Tags-Block ersetzt durch **Inline-Tag-Picker** analog R1-B-C-Pattern (`#conversion-tag-chips` Container + `#conversion-tag-input` mit `list="conversion-tag-suggestions"`-Datalist-Autocomplete + +-Add-Button + X-Detach), PageData-Erweiterung um `conversionTags`-Liste — **Backend übergibt** `conversion_tag_refs = [t.to_dict() for t in conversion.tag_refs]` ans Template via `library_detail`-Route, vermeidet fragile Jinja-`map(attribute='to_dict')`-Verkettung die nur Attribut-Access nicht Methoden-Call kann. [static/js/library_detail.js](static/js/library_detail.js) CSV-Mechanik **komplett raus** (4 CSV-Stellen + `AUTOSAVE_INPUTS.tags` + `parseTagsCsv` + `renderTagChips` + `setupTagChipSync` gelöscht), neue Funktionen `renderConversionTagChips` / `loadConversionTagSuggestions` / `addTagToConversion` / `removeTagFromConversion` / `initConversionTagPicker` 1:1 analog Highlight-Tag-Picker-Pattern aus R1-B-C; `conversionTagsState`-Array als Modul-Cache aus PageData seeded; `renderNotionFields`-Notion-Send-Pfad zieht Tags jetzt aus `conversionTagsState.map(t => t.name).join(', ')` statt aus dem entfernten CSV-Input. **Tag-Manager-Page** [static/js/tags.js:38](static/js/tags.js:38) zeigt nun beide Counts als ein-zeilige Mikrokopie `N Markierungen · M Dokumente` (Singular/Plural je Count). **CSS-Disposition B beibehalten** (Master-Empfehlung, keine Konsolidation in R2-A): dead `.tag-chip` / `.tag-chip-list` / `.tag-chip__remove` / `.tag-chip-empty`-Familie aus `style.css` entfernt (kein Caller mehr nach Migration); neue `.conversion-tag-section__chips` / `.conversion-tag-section__row` / `.conversion-tag-section__input` / `.conversion-tag-chips__empty` / `.conversion-tag-chip` / `.conversion-tag-chip__remove`-Familie eingeführt (visuell identisch zu `.highlight-tag-chip*` aus R1-B-C, semantisch getrennt — Konsolidation Polish-Sprint-Material); CSS-TOC um `CONVERSION TAG`-Sektion ergänzt, HIGHLIGHT-TAG-Header-Kommentar aktualisiert (alte CSV-Bezugnahme entfernt, Hinweis auf Polish-Sprint-Konsolidation dokumentiert). **Pre-Commit-Patch Library-Search Junction-Branch** (UX-Regressions-Schutz, Master-Entscheidung "JETZT statt R2-B"): Search-Filter in [library.py:71](app_pkg/library.py:71) ersetzt CSV-Branch `Conversion.tags.ilike(...)` durch Junction-Branch `Conversion.tag_refs.any(Tag.name.ilike(...))`. Begründung: nach Migration wäre die CSV-Spalte leer, User-Search „ki" hätte 0 Treffer ergeben trotz vorhandener Junction-Tags — echte UX-Regression. Live-verifiziert: `GET /library?search=ki` surfaced Conv 1 (Junction-Branch matched), Title/Content-Regression-Branches grün. **Tests**: neuer Modul `tests/test_conversion_tags.py` mit **24 Tests** (21 R2-A-Kern + 3 Search-Patch) — POST creates/idempotent/normalizes/rejects-empty-oversize/rejects-non-dict/foreign-404, DELETE detach + foreign-tag-404 + foreign-conversion-404, Cross-Domain-Reuse (gleicher Tag-Name via Highlight-API + Conversion-API → identische `tag.id`), to_dict-tag_refs-Field, Cascade-beide-Junctions, Migration-Helper Idempotenz × 2, Migration-Normalisierung (Lowercase+Trim), Migration-Filter-Empty/Whitespace, Migration-Dedup, Migration-Reuse-existing-Tag-Row (Highlight-Pfad legt Tag an, dann Migration zieht denselben Tag rein), Migration-No-Op-wenn-keine-CSV-Rows, `Tag.get_or_create` Blank/Oversize-Reject + Returns-Existing-Row-Reuse, Library-Search-Junction-Match + Dead-CSV-Non-Match + Title/Content-Regression. Plus 1 erweiterter Test in `tests/test_tags.py` (`GET /api/tags`-Test um `conversion_count`-Assertion) + 1 neuer Test (`test_api_list_tags_includes_conversion_count` für 2 Conversions mit gleichem Tag). Pytest **131/131 grün** im Container (106 baseline + 25 neu). **Live-HTTP-Smoke 3 Master-Schwerpunkte alle grün**: (1) **Cross-Domain-Tag-Reuse** — DB-Probe vor Smoke zeigte Tag id=2 ("produkt") parallel in `highlight_tags(highlight=9, tag_id=2)` (R1-B-C-Smoke-Artefakt) und `conversion_tags(conversion=1, tag_id=2)` (R2-A-Migration-Seed); Live-HTTP `POST /api/highlights/9/tags {name: "cascade-smoke"}` + `POST /api/conversions/1/tags {name: "cascade-smoke"}` lieferten **dieselbe tag.id=7** via Cross-Domain-Reuse-Pfad (`Tag.get_or_create` find-or-create teilt sich die Per-User-Tag-Tabelle). (2) **Tag-Manager-Cascade beide Junctions** — dual-attached `tag.id=7` via `DELETE /api/tags/7` gelöscht → DB-Probe danach `highlight_tags`-Rows = 0, `conversion_tags`-Rows = 0, `Tag.query.get(7) is None`; beide Junctions sauber cascadiert. (3) **Search-Patch** — `GET /library?search=ki` surfaced „Reading-View Smoke" (Conv 1 mit ki-Tag-Junction-Row) im Response-Body; Regression `GET /library?search=Quartalsbericht` surfaced Title-Match; Cross-Doc-Isolation: Conv 2 (Quartalsbericht, kein ki-Tag) nicht in `?search=ki`-Body. **Frontend-Render-Smoke** ebenfalls grün (HTTP-Roundtrip mit forced-session-cookie gegen Live-DB): Library-List `<span class="c-tag` Chips für ki/produkt/eins gerendert aus `conv.tag_refs`; Library-Detail neue DOM-IDs `conversion-tag-chips` / `conversion-tag-input` / `conversion-tag-suggestions` vorhanden, alte `tags-input` / `tag-chip-container`-IDs **weg**; `PageData.conversionTags` korrekt geseedet als list-of-dicts `[{id:5,name:'ki'},{id:2,name:'produkt'},{id:6,name:'eins'}]`; Tag-Manager-Page (`/tags`) lädt — Counts kommen client-side via `tags.js`. **Memory-Eintrag** [reference_data_migration_idempotency.md](memory/reference_data_migration_idempotency.md) für die "leere-Original-Spalte-als-Migration-Marker"-Mechanik als verallgemeinerbares Pattern (verlinkt mit `reference_inline_sqlite_migration.md` als Schwester-Pattern Schema vs. Daten). **Architektur-Memo** [docs/reader_architecture.md](docs/reader_architecture.md) Sprint-Schneidungs-Tabelle R2-Cluster ergänzt (R2-A done, R2-B + R2-C offen) + Decision-Log um drei 2026-05-25-Einträge erweitert (R2-A-Split, Tag.get_or_create-DRY-Anker, CSV-Migration-Idempotenz-Mechanik). **Dispositions-Bericht**: CSS-Konsolidation = B (keine Konsolidation, neue `.conversion-tag-*`-Familie statt Generic-Refactor); PATCH-`tags`-Pfad = entfernt (Default); Library-Search-Patch = Pre-Commit (Master-Entscheidung); Test-Count = 131 (von 106 → +25); 1 Conversion live-migriert (gesedete Smoke-Foundation). **Master-Aktivität nächste**: R2-B Filtered Views + Reading-Progress oder R2-C Lifecycle-Status (Inbox/Later/Archive) als nächster Sub-Sprint, plus R2-A-Dark-Mode-Browser-Smoke der `.conversion-tag-*`-Klassen als XS-Master-Aufgabe._

_Vorgänger: R1-B-C ☑ done 2026-05-25 → highlight-tags + tag-foundation + cross-format-popover-bridge. **R1-Cluster Reader-Core damit komplett geschlossen** (R1-A Reading-View + R1-B-A Highlight-Core + R1-B-B Notes/Sidebar + R1-B-C Tag-Foundation). **Schema**: neue Tabelle `Tag` mit `user_id`-FK + `unique(user_id, name)`-Constraint für Per-User-Tag-Namespace, neue Junction-Tabelle `highlight_tags` mit `(highlight_id, tag_id)` composite PK; beide via `db.create_all()` automatisch angelegt, **kein `_run_pending_migrations`-Touch nötig** (Memory `reference_inline_sqlite_migration.md` greift nur für Spalten-Adds an bestehender Tabelle). Plus M:N-Relationship am `Highlight`-Model `tags = db.relationship('Tag', secondary=highlight_tags, lazy='joined', backref='highlights')` damit `Highlight.to_dict()` die Tags ohne extra Query mitzieht. `Conversion.tags`-CSV-Spalte **komplett unangetastet** — R2-A erbt das Tag-Schema und migriert die CSV erst dann in eine `conversion_tags`-Junction. **Backend**: neues Modul [app_pkg/tags.py](app_pkg/tags.py) mit `register(app)`-Pattern + 4 Routes: `GET /api/tags` → sortierte Tag-Liste des current_user mit `highlight_count` per Outer-Join + GROUP BY, `POST /api/highlights/<id>/tags {name}` → find-or-create + Attach mit Idempotenz (existierende Junction → 200 no-op, neue Junction → 201), `DELETE /api/highlights/<id>/tags/<tag_id>` → Detach (Tag bleibt erhalten), `DELETE /api/tags/<id>` → komplett-Löschen mit Cascade via `highlight_tags.delete().where(tag_id == …)` + `session.delete(tag)` in einer commit-Boundary. Plus Page-Route `GET /tags` für Manager-Page. **Tag-Normalisierung**: `name.strip().lower()` beim Empfang — case-insensitive Disambiguation („KI", „ki", „KI " sind dasselbe), Tippfehler-Resilienz für Reader-Replacement-Workflow. Validierung: empty/whitespace-only → 400, oversized (>80) → 400 mit deutscher Microcopy „Tag-Name fehlt oder ist zu lang (max 80 Zeichen).". **Ownership-Scoping**: Foreign-Highlight via `get_owned_conversion`-Reuse → 404; Foreign-Tag-Delete → 404; Foreign-User-Tag in `GET /api/tags` unsichtbar (per-User-Query). **highlights.to_dict-Erweiterung**: `'tags': [t.to_dict() for t in self.tags]` damit `GET /api/conversions/<id>/highlights` direkt die Tag-Liste pro Highlight mitliefert ohne N+1. **Frontend Tag-Picker im Action-Popover**: neuer Block in [templates/library_detail.html](templates/library_detail.html) unter Note-Textarea via `<hr>`-Trenner — Tag-Chips-Container `#highlight-tag-chips` + Input `#highlight-tag-input` mit `list="tag-suggestions"`-Browser-native-Autocomplete + +-Add-Button + `<datalist id="tag-suggestions">`. JS-Erweiterung in [static/js/library_detail.js](static/js/library_detail.js): `loadTagSuggestions()` fetcht `GET /api/tags` und befüllt Datalist-Options, `addTagToHighlight()` POSTet + State-Update + Re-Render (Popover-Chips + Sidebar-Card-Chips), `removeTagFromHighlight(tagId)` DELETE + State-Update, `renderHighlightTagChips(highlight)` baut Popover-Chips mit X-Remove-Button (mousedown statt click damit Outside-Click-Handler nicht vorher schließt), `renderSidebarCardTagChips(card, tags)` baut Sidebar-Card-Chips Read-Only (kein X — Detach läuft nur über Popover, max 3 Chips sichtbar + „+N"-Marker bei Überlauf). **Tag-Manager-Page** [templates/tags.html](templates/tags.html): neue Page mit Heading „Tags", Liste `#tag-list` mit `.tag-manager-card`-Items (Name + „N Markierung(en)"-Count + Delete-Button), Empty-State „Noch keine Tags. In der Library-Detailansicht einen Highlight markieren und Tags hinzufügen.", Zurück-zur-Library-Link. JS in [static/js/tags.js](static/js/tags.js): `loadTags()` + `deleteTag(id)` mit Native-`confirm()`-Dialog „Tag <name> wirklich komplett löschen?". Library-List-Header bekommt „Tags verwalten"-Link via [templates/library.html](templates/library.html) neben Filter-Picker. **CSS** in [static/css/style.css](static/css/style.css): neue Blöcke `.highlight-tag-chip` (inline-flex Pill, `--nm-tint-accent`-Background, Token-driven Dark-Mode-Konsistenz), `.highlight-tag-chip__remove` (X-Button transparent + Hover-State), `.highlight-tag-chip--compact` (Sidebar-Card-Variante kleiner), `.highlight-tag-chip--more` („+N"-Marker), `.tag-manager-card` (Flex-Row mit `--nm-bg`-Background + `--nm-raised-sm`-Shadow + `--nm-radius-sm`), TOC-Eintrag aktualisiert. **Pre-Commit-Patch Cross-Format-Popover-Bridge**: R1-B-B-Sidebar zeigte Cross-Format-Highlights mit ⚠-Marker, Click triggerte aber nur Warning-Toast „im Text nicht direkt anspringbar" — R1-B-C-Tag-Picker hätte den User damit von Cross-Format-Tags ausgeschlossen. Patch in `library_detail.js`: Card-Click verzweigt jetzt `if (crossFormatHighlightIds.has(h.id)) showHighlightActionPopover(card)` (Card als Anker statt Span — `showHighlightActionPopover` Parameter umbenannt `spanEl → anchorEl`, beide tragen `dataset.highlightId` und `getBoundingClientRect`), Title-Microcopy auf „Markierung über Formatierungsgrenze. Klick: bearbeiten." aktualisiert. Outside-Click-Handler erweitert um `!evt.target.closest('.highlight-card--cross-format')` damit der Card-Click den frisch geöffneten Popover nicht durch document-Bubble sofort wieder schließt. Normale (nicht-cross-format) Card-Clicks bleiben `scrollToHighlight` + Flash — R1-B-B-Verhalten unverändert (Regression Smoke verifiziert: span existiert, highlight-flash gesetzt, popover bleibt zu). **Tests** [tests/test_tags.py](tests/test_tags.py): 16 neue Tests (GET-empty, GET-sorted-with-count, POST-new-tag, POST-existing-tag-reuse, POST-already-attached-idempotent-200, POST-normalisierung-strip-lowercase, POST-empty-400, POST-whitespace-only-400, POST-oversized-400, POST-foreign-highlight-404, DELETE-junction-tag-survives, DELETE-junction-foreign-404, DELETE-tag-cascade-junctions-gone, DELETE-tag-foreign-404, GET-tags-per-user-isolation, GET-highlights-includes-tags-field). Pytest **106/106 grün** im Container (90 baseline + 16 neu). **Live-Smoke 17 Punkte alle grün** inkl. Re-Smoke 18 nach Patch: Tag-Add/Reuse/Normalize/Idempotent/Detach/Cascade visuell + DB-Probe (`Tag.query.all() = [(2,'produkt',1)]`, `highlight_tags rows = [(9,2)]`), Datalist-Autocomplete browser-native bei Mac-Chrome korrekt rendered (Suggestion bei „k"-Tippen sichtbar, Tab-Navigation ergonomisch), Cross-Doc-Isolation (Tag „test" nur an doc 4, doc 2 zeigt ihn nicht; Per-User-Tag-Namespace via `GET /api/tags` zeigt beide weil Per-User-Scope wie spec'd), Cross-Format-Popover-Bridge via Sidebar-Card-Click → Popover öffnet → Tag-Attach → Sidebar-Chip live update, Sidebar-Truncation 3+„+N" verifiziert mit 5-Tag-Stress, Dark-Mode Token-driven (Chip-bg `rgba(123,138,189,0.15)` + Text `rgb(224,224,232)` lesbar), Edge-Cases Sonderzeichen/+/2026/Emoji/80-chars/81-chars-400 alle grün, normale Card-Click-Regression bestätigt unangetastet. **Subjektive Tag-Normalisierungs-Bewertung**: lowercase+trim wirkt pragmatisch für Reader-Replacement, etwas „weicher" / weniger Marketing-präsent als Case-preserving („KI" → „ki"), aber Tippfehler-Resilienz schlägt visuelles Idealbild — Akronyme leicht unschön aber nicht kritisch. Bleibt. **Master-Aktivität nächste**: R2-Cluster Library-Power planen (R2-A `conversion_tags`-Junction + CSV-Migration M-L, R2-B Filtered Views + Reading-Progress M)._

_Vorgänger: R1-B-B ☑ done 2026-05-25 → highlight-notes + sidebar. **Schema-Add via idempotenter ALTER-TABLE-Inline-Helper** `_run_pending_migrations(app)` in [app_pkg/__init__.py](app_pkg/__init__.py:74) direkt nach `db.create_all()`, prüft `inspect(db.engine).get_columns('highlight')` und ALTERt nur wenn `note` fehlt. Erste Container-Start emittierte einmal `R1-B-B: highlight.note column added via ALTER TABLE`, Restart-Smoke verifiziert keine zweite Line (idempotent). Pattern als Single-Source-of-Truth in Memory `reference_inline_sqlite_migration.md` verankert — R1-B-C neue Tabellen brauchen es nicht (laufen über `create_all()`), R2-A CSV-Migration der `Conversion.tags`-Spalte wird es wiederverwenden. **Models**: `Highlight.note = db.Column(db.Text, nullable=True)` plus `to_dict`-Erweiterung mit `'note': self.note`. **Backend**: neue Route `PATCH /api/highlights/<int:highlight_id>` in [app_pkg/highlights.py](app_pkg/highlights.py:74) mit Ownership-Check (`highlight.conversion.user_id == current_user.id` sonst 404), Body-Validierung **pflicht `note`-Key** (Body ohne `note` → 400 `Feld "note" fehlt im Request-Body.` — atomarer Endpoint ohne versteckte no-ops), Type-Check (string oder null, non-string non-null → 400), Length-Check `MAX_NOTE_LEN=2000` (oversized → 400), plus **Empty-String→NULL-Normalisierung** (leerer String wird als Lösch-Intent als NULL persistiert — DB-Konsistenz: keine Unterscheidung zwischen „leer" und „nie gesetzt"). **Frontend Sidebar-Highlights-Liste**: neuer Block in [templates/library_detail.html](templates/library_detail.html:60) **oben in der Sidebar** vor „An Notion senden" (Highlights als aktiver Reader-Workflow vor Notion/Details/Tags als statische Sekundär-Blöcke), `#highlight-list` Container plus `#highlight-count`-Span plus `#highlight-list-empty`-Default „Noch keine Markierungen.". JS-Erweiterung in [static/js/library_detail.js](static/js/library_detail.js:483): Modul-State `highlightsState = []` + `crossFormatHighlightIds = new Set()` als zentraler Cache, `loadHighlights` befüllt beide und triggert `renderHighlightList()` mit Cards (Snippet truncated 80 chars + Note-Preview truncated 60 chars wenn vorhanden + Cross-Format-Marker-Class wenn ID in Set), `scrollToHighlight(id)` findet Span via `CSS.escape`, scrollt smooth via `scrollIntoView({behavior, block: 'center'})`, fügt 1s-`.highlight-flash`-Class hinzu für Pulse-Cue (Selektor-Engführung `.reader-view span.highlight[data-highlight-id].highlight-flash` aus R1-B-A 1:1 übernommen — keine Pygments-Code-Block-Kollision). Action-Popover erweitert um Textarea `#highlight-note-input` (maxlength 2000) + Speichern + Löschen, Prefill beim Open mit `highlight.note || ''`, `saveHighlightNote()` PATCH-Call mit state-Update + Sidebar-Re-Render + Popover-Close + Toast „Notiz gespeichert.". `saveCurrentSelection` (POST-Pfad) erweitert um state-Push + Sidebar-Re-Render damit neue Highlights ohne Reload erscheinen, `deleteActiveHighlight` symmetrisch um state-Filter + Re-Render. **Cross-Format-UX-Lücken-Schluss aus R1-B-A** (R1-B-A-Fadenende): Cross-Format-Highlights waren in der DB persistiert aber im DOM unsichtbar, R1-B-B-Sidebar zeigt sie jetzt mit **4-fach gekoppeltem Marker** (opacity 0.7 + `border-left 3px solid var(--nm-text-muted)` + `⚠`-Glyph via `::before` in `var(--nm-warning)` + `title`-Attribut), Click triggert Warning-Toast „Markierung über Formatierungsgrenze, im Text nicht direkt anspringbar." statt Scroll. **CSS** in [static/css/style.css](static/css/style.css:1112): Action-Popover von `inline-flex` auf `flex` + `flex-direction: column` umgestellt (Textarea oben, Button-Row unten, `min-width 260px / max-width 320px`), neuer Block `.highlight-card{,_snippet,_note,--cross-format}` mit `--nm-bg`-Background + `--nm-raised-sm`-Shadow + Hover-State `--nm-tint-accent`, `@keyframes highlight-flash` mit 0%/50%/100%-Tween zwischen `--nm-tint-highlight` und `--nm-tint-warning`. **Tests**: 9 neue Tests in [tests/test_highlights.py](tests/test_highlights.py:153) (GET-Liste enthält note-Feld in Highlight-Dicts, PATCH valid note → 200 + Persistenz, PATCH null → NULL, PATCH empty string → NULL (Normalisierung), PATCH oversized > 2000 chars → 400, PATCH non-dict body → 400, PATCH missing note-Key → 400, PATCH non-string non-null → 400, PATCH fremden Highlight → 404). Pytest **90/90 grün** im Container (81 baseline + 9 neu). **15-Schritte-Live-Smoke alle grün**: 4 Sidebar-Cards bei doc 2 + Counter (DB-Drift 4 statt erwarteter 2 ist Test-Daten-Artefakt aus R1-B-A-Mehrfach-Smokes, kein Bug); Note add/edit/delete-via-empty-string verifiziert mit DB-Probe `h1.note=None` nach Lösch-Intent; Scroll-to-Highlight + Flash-Klasse via DevTools-JS-Eval als `SPAN` mit `isUserHighlight: true` matched (0 Pygments-Blocks im Test-Doc); doc 4 Cross-Format-Sidebar zeigt 1 Card mit ⚠-Marker, Click triggert Warning-Toast `toast-notification--warning show`, flashCount=0; doc 7 Multi-Anker disambiguation hält (Card 1 flash auf id=5 rect.left=448, Card 2 flash auf id=6 rect.left=800 — verschiedene DOM-Positionen); Delete via Popover Counter 5→4 + Span weg + Toast; live-Create text-drag-Selection → POST → Counter 4→5 + neue Card ohne Reload; Cross-Doc-Isolation doc 1 Counter (0) + Empty-State; Dark-Mode Card `rgb(42,45,58)` bg + `rgb(224,224,232)` text + Flash-Animation läuft; maxlength=2000 Browser-attr verifiziert; Doppel-Click-Race idempotent (1 Popover, kein Doppel-Render, Prefill korrekt). **Subjektive Bewertung Cross-Format-UX**: angemessen prominent ohne aufdringlich zu wirken — ⚠-Glyph und opacity-Dim sind die deutlichsten Signale, border-left im Light-Mode dezent (`--nm-text-muted` nahe `--nm-bg`), im Dark-Mode markanter. Defensive Redundanz bleibt drin, kein Trimm nötig. **Performance-Beobachtung**: 4–5 Cards renderten ohne wahrnehmbare Verzögerung; N>20 nicht getestet, `renderHighlightList` ist O(N) mit nativen DOM-Inserts — bei N>100 würde Virtualisierung relevant, aktuell out-of-scope. **Memory-Eintrag** [reference_inline_sqlite_migration.md](memory/reference_inline_sqlite_migration.md) als Single-Source-of-Truth-Anker für künftige Schema-Touches in diesem Stack ohne Migration-Framework angelegt. **Master-Aktivität nächste**: R1-B-C-Sub-Thread starten._

_Vorgänger: R1-B-A ☑ done 2026-05-25 → highlight-core Foundation. **Schema**: neue Tabelle `Highlight` mit FK auf `Conversion` plus Anker-Felder `exact`/`prefix`/`suffix` im W3C-Web-Annotation-Style, `cascade='all, delete-orphan'` für automatische Mit-Löschung beim Conversion-Delete. **Backend**: neues Modul `app_pkg/highlights.py` mit `register(app)`-Pattern und 3 Routes (POST + GET an `/api/conversions/<id>/highlights`, DELETE an `/api/highlights/<id>`), Ownership-Check via `get_owned_conversion` reused aus `library.py`, Foreign-Row-DELETE → 404 statt 403 (Existenz nicht leaken), Limits MAX_EXACT=5000 + MAX_CONTEXT=200. **Frontend-Selektion-UX**: schwebender „Markieren"-Button bei aktiver Selection, mousedown-Handler (statt click — sonst kollabiert Selection vor Read), `saveCurrentSelection` → POST → `applyHighlight` (Vanilla-JS TreeWalker + Range.surroundContents, keine externe Library wie mark.js). **Re-Apply beim Load**: `loadHighlights` → `locateHighlightOffset` mit Multi-Match-Disambiguation via Prefix/Suffix-Scoring (2 Punkte exakter Match, 1 Punkt für letzte/erste 8 Chars), bei Ambiguität erstes Vorkommen. **Delete-UX**: Click auf Span → Action-Popover mit „Löschen"-Button → DELETE-Call → DOM-Span unwrap + `parent.normalize()`. **CSS**: neuer Token `--nm-tint-highlight` in `:root` (light gelb-warm `rgba(255,220,0,0.4)`) und `[data-global-theme="dark"]` (dark amber-dimmed `rgba(232,176,64,0.35)`), `.reader-view span.highlight[data-highlight-id]`-Selector mit Hover-State auf `--nm-tint-warning`, plus `.highlight-create-btn` + `.highlight-action-popover` als absolut positionierte Floater. **Tests**: neuer Modul `tests/test_highlights.py` mit 10 Tests (POST valid/empty/oversized/non-dict-body/foreign-conversion, GET own/foreign, DELETE own/foreign-row, cascade-on-conversion-delete). Pytest **81/81 grün** im Container (71 baseline + 10 neu). **CSRF-Realitätsprüfung im Browser bestätigt**: globaler `fetch`-Wrapper in [templates/base.html:120-131](templates/base.html#L120) setzt automatisch `X-CSRFToken`-Header für POST/PUT/DELETE/PATCH; mein neuer Code triggert das ohne Boilerplate; alle Smoke-POSTs gaben 201, alle DELETEs 200, kein einziger 400. **Drei Phase-2-Bug-Fixes während Live-Smoke** alle reproduzierbar gefunden und lokal gefixt: (1) **Anchor-Koordinaten-Mismatch** — `extractSelectionContext` benutzte `preRange.toString()`-Länge (raw text-node concat), `locateHighlightOffset` und `rangeForOffsets` benutzten `reader.innerText` (rendered, mit Block-`\n`). Resultat: Spans landeten im falschen Offset (z.B. „iven abgeschlossen. Die Auslieferungs" statt „Auslieferungsquote lag bei 94 Prozent"). Fix: neuer Helper `readerRawText(reader)` walkt Text-Nodes und konkateniert `nodeValue`; `extractSelectionContext` + `locateHighlightOffset` jetzt auf demselben Koordinatensystem. (2) **`.highlight`-Class-Kollision mit Pygments-Code-Blocks** — Markdown-Renderer (markdown-it + Pygments + nh3) emittiert `<div class="highlight" style="background: #f8f8f8">` für jeden Code-Fence; mein generisches `.highlight`-CSS traf alle Code-Blöcke (gelbe Tönung) und Click-Handler öffnete Delete-Popover für sie. Fix: CSS verengt auf `.reader-view span.highlight[data-highlight-id]`, JS-Click-Selector auf `closest('span.highlight[data-highlight-id]')`. Pygments-Div bleibt unangetastet mit eigenem Inline-Background. (3) **Chrome `transition: background-color` + `var(--token)` Cache-Bug** — bei live Theme-Toggle (Attribut-Wechsel `data-global-theme=light` ↔ `dark`) cached Chromium den computed `background-color` auf bestehenden Spans; frische Spans (Clone, neue applyHighlight) bekamen den neuen Token sofort, alte stayen am alten Wert. WebKit/Chromium-Quirk. Fix: Transition auf `.reader-view span.highlight[data-highlight-id]` entfernt. Hover-Snap statt Fade, dafür korrekte Token-Cascade ohne Reload. **Cross-Format-Edge-Case-Pattern (Master-Entscheidung Variante a)**: Selektion über `</strong>`-Boundary wird gracefully gehandhabt — `rangeForOffsets` bailt bei multi-text-node Range, `applyHighlight` returnt false, Toast „Markierung gespeichert, Anzeige nicht möglich (Formatierungsgrenze)." (warning-Level). DB-row bleibt persistiert, R1-B-B-Sidebar schließt die UX-Lücke automatisch. **Multi-Occurrence-Disambiguation verifiziert**: Test-Doc `/library/7` mit 7 Vorkommen von „Anker" angelegt, 2 davon im selben Absatz ~20 Chars Abstand markiert, beide nach Reload exakt an der richtigen Stelle reproduziert (Prefix/Suffix-Scoring funktioniert auch bei räumlicher Nachbarschaft). **Memory-Eintrag** `feedback_css_class_collision_in_markdown_views.md` für die verallgemeinerte Render-Output-vs-User-Class-Lehre (zweites Vorkommnis nach R1-A `feedback_jinja_auto_escape_markup_filter.md`), verlinkt zu R1-A als verwandter „rendered content vs. our code"-Erkenntnis. **Smoke-Foundation-Artefakte in Container-DB**: doc 4 hält den Cross-Format-Highlight („HOST: Willkommen ") als Smoke-Artefakt für R1-B-B-Sidebar-Verifikation, doc 7 „Multi-Anker-Smoke" hält 2 disambiguierte Highlights als Multi-Occurrence-Demo, doc 2 „Quartalsbericht" hält 2 normale Highlights aus dem Reload-Smoke. Alle harmless — R1-B-B kann sie als Smoke-Foundation wiederverwenden oder vorab löschen. **Foundation für R1-B-B Highlight-Notes** steht — Schema kann via `ALTER TABLE Highlight ADD COLUMN note TEXT` erweitert werden, API-PATCH-Endpunkt + Sidebar-Notes-Liste füllen die R1-B-B-Lücke. **Master-Aktivität nächste**: R1-B-B-Sub-Thread starten._

_Vorgänger: R1-A ☑ done 2026-05-25 → commit `c84e469` (reading-view foundation: markdown-it+nh3-Renderer als `app_pkg/markdown_render.py` konsolidiert `markdown.py` + `library.py` auf einen Helper, `<pre>`-Block in `library_detail.html` durch `<article class="reader-view">` + hidden `<script type="text/markdown" id="content-source">` für raw-source-Side-Channel ersetzt, `script_safe`-Jinja-Filter mit `Markup`-Return gegen Auto-Escape im raw-text-Element plus Case-Insensitive `</script`-Patch, Reader-CSS-Block mit `--nm-*`-Token-Reuse Dark-Mode-frei plus Tailwind-Preflight-Bullets-Patch `list-style disc/decimal outside`). 4 Conversion-Types live gesmoked im Browser (markdown_input rich mit Headings/Listen/Code/Tabelle/Pygments/HR, document_to_markdown, audio_transcription, dialogue_formatting), Encoding-Smoke `</script>` clean (Source 3× escaped, Article-Render komplett, HTML-Body schließt sauber), JS-Reuse-Smoke per `textContent`-Inspektion zeigt byte-exakten Markdown für Copy/Download/Notion (id=1..4 src==db, id=5 reconstructed via reverse-escape == db). Empty-State „Kein Inhalt." angezeigt. Pytest **71/71 grün** im Container vor und nach den Phase-2-Fixes. **Phase-2-Fix 1 `script_safe` Markup-Return**: Filter gab erst `str` zurück, Flask escapte `<` zu `&lt;` im Script-Block, im raw-text-Element wird das nicht zurück-dekodiert → JS bekam HTML-Entitäten statt raw Markdown. Auf `markupsafe.Markup(...)` umgestellt mit Patch `</script` (case-insensitive) → `<\/script`. Memory-Eintrag `feedback_jinja_auto_escape_markup_filter.md` angelegt. **Phase-2-Fix 2 Listen-Bullets**: Tailwind-CDN-Preflight setzt `list-style: none` global; Reader-View-Listen sahen wie Absätze aus. 2-Zeilen-CSS-Patch `list-style: disc/decimal outside` im Reader-View-Block. **CSS-Variablen-Mapping**: die im Sprint-Prompt vermuteten `--neo-text`/`--neo-faint`/`--neo-border`/`--neo-surface` sind keine CSS-Variablen sondern Tailwind-Color-Aliase in `base.html`; echte Token sind `--nm-text`/`--nm-text-secondary`/`--nm-text-muted`/`--nm-bg`. Reader-View nutzt diese plus `--nm-font`/`--nm-font-mono`/`--nm-radius-sm`/`--nm-pressed-sm`/`--nm-tint-accent`, damit Dark-Mode automatisch greift. **Border-Substitute**: `var(--nm-text-muted)` für Blockquote-Linie + HR + Table-Borders, im Dark Mode visuell deutlich sichtbar, Light-Mode-Verifikation steht als P3-Reminder. **Master-Aktivität nächste**: READER-PLAN-Workshop für R1-B-Schema-Entscheidung (Highlight-Selektion + Speicherung + Notes + Tags)._

_READER-PLAN ☑ done 2026-05-25 (Master-Workshop, kein Code-Touch) → Architektur-Memo [docs/reader_architecture.md](docs/reader_architecture.md) persistiert. Vier Entscheidungs-Knoten geklärt: (1) Highlight-Schema = eigene Tabelle `Highlight` mit FK auf `Conversion` (leichter migrierbar als JSON-Spalte weil `create_all()` neue Tabellen automatisch anlegt, während `ALTER TABLE` manuell wäre); (2) Anker-Modell = Text-Quote + Prefix + Suffix im W3C-Web-Annotation-Style à la Hypothes.is (selbst-heilend, client-side, Industrie-Standard); (3) Notes = Single-`note`-Feld am Highlight (YAGNI gegenüber Multi-Note-Tabelle); (4) Tags = `Tag`-Tabelle plus zwei Junction-Tabellen `conversion_tags` + `highlight_tags` (eindeutiger Tag-Namespace pro User, beidseitige FK-Integrität). Bonus-Entscheidung: `Conversion`-Class-Name bleibt unverändert (YAGNI, Code-Churn ohne UI-Wert). R1-B-Sprint-Schneidung: L → drei Sub-Sprints **R1-B-A Highlight-Core** (M, Schema + API + Selektion-UX + Re-Apply, ohne Notes/Tags), **R1-B-B Highlight-Notes** (S), **R1-B-C Highlight-Tags + Tag-Foundation** (M, `conversion_tags`-Junction bleibt R2-A vorbehalten). R1-B-A Sprint-Prompt-Doc [docs/archive/sprint-prompts/SPRINT_R1BA_highlight-core_2026-05-25.md](docs/archive/sprint-prompts/SPRINT_R1BA_highlight-core_2026-05-25.md) bereit. **Master-Aktivität nächste**: R1-B-A-Sub-Thread starten._

_Vorgänger: MAC1 ☑ done 2026-05-25 (docker-compose.override.yml + mac-dev-setup.md fuer Apple-Silicon-Dev). Mac-Stack startet lokal via docker compose up --build, Markdown-Converter + Document-Converter + Library getestet. Playwright MD→PDF unter Rosetta M4: 3 Sekunden. Build-Zeit erster Run 6:53 min via Rosetta (Retry via Layer-Cache 5:27 min; pip ReadTimeout war Netzwerk-flaky, kein Rosetta-Bug). Image 4.82 GB durch NVIDIA-CUDA-Wheels als Torch-Transitives. Pytest 71/71 gruen im Container. Smoke-User smoke/smokepass123 in Container-DB angelegt fuer Phase-2-Smoke (kann fuer kuenftige Smokes wiederverwendet werden). Notion-Integration auf Mac off (kein lokaler notion-mcp-server). Server-Setup (Mintbox + converter.smallpieces.de) unangetastet. Zwei Follow-ups im BACKLOG (MAC1-FOLLOWUP-A pip-timeout XS, MAC1-FOLLOWUP-B Image-Slim M)._
_HYG2 ☑ done 2026-05-11 → drei Code-Commits `00969fb` (Sub-Batch A — library_detail Polish: BT7 textarea-escape mit inline-`escHtml`-Helper für alle 4 interpolierten User-/Suggestion-Werte in `renderNotionFields`, BT8 `noopener,noreferrer` im `sendToNotion`-Erfolgspfad, Opacity-Übergang 150 ms via `#notion-fields { transition: opacity 0.15s ease; }` plus `selectTarget`-Toggle nur bei `isSwitch=true` mit `offsetHeight`-Reflow analog `showToast`-Pattern in `_utils.js`), `766ebab` (Sub-Batch B — Cross-Feature-Hygiene: `showToast` Toast-DOM bekommt `role`+`aria-live` mit Level-Differenzierung `danger`/`warning` → `role=alert`+`aria-live=assertive` vs. `success`/`info` → `role=status`+`aria-live=polite` analog F-3.3 P8; 3 DE-Microcopy-Strings in `app_pkg/markdown.py:137/143/157` ohne „Error:"/„Warning:"-Präfix da flash-Level redundant), `c551d51` (Sub-Batch C — Audio-UX: `getUserMedia` aus `socket.onopen` herausgezogen und vor `new WebSocket(...)` gestellt in `connectToDeepgram`, Stream-Track-End-Handler mit `audioTrack.addEventListener('ended', ...)` für Mid-Recording-Mic-Abbruch zeigt warning-Banner und ruft `stopRecording()`, Stream-Release in `socket.onclose`/`socket.onerror` wenn WS vor `onopen` stirbt). **6 Aktive-Reminder-Items abgearbeitet** (Item 1 getUserMedia, Item 2 BT7 textarea-escape, Item 3 BT8 window.open-noopener, Item 4 Opacity-Übergang, Item 5 aria-live showToast, Item 6 3 DE-Strings markdown.py). Pytest-Erwartung 71/71 unverändert (Frontend-only Touches plus 3 Backend-Flash-String-Renames, Master prüft Container-Pytest via SSH zu Mintbox separat). **`escapeHtml`-Helper-Disposition**: lokal in `renderNotionFields` per Memory `feedback_helper_reuse_design_choice.md` weil alle 4 Call-Sites in derselben Funktion (single-call-site-Cluster). **Cross-Feature-`showToast`-Verifikation**: 8 Call-Sites in 4 Files (library_detail, library, audio_converter, document_converter), kein Call-Site bypasst Helper-Render-Logik — Helper-Touch reicht. **Master-Punch-Liste**: Item 1 (HYG2-Live-Mic-Smoke) plus Items 7-8 aus Sprint-Liste (P8-Master-Smoke für F5-IMPL plus F6-IMPL P2/P3/P11) bleiben Browser-Master-Aufgaben. **SMB-Permission-Flake** bei Phase-3-Doc-Commits notiert (Single-File-Commits brachen mit „insufficient permission for adding an object" ab, Multi-File-Bundles funktionierten; Code-Commits A/B/C waren nicht betroffen). Memory-Disposition keine neue Eintrag._
_Vorgänger: WAVE-CLOSE ☑ done 2026-05-11 → strukturelles Closing der Cleanup-Welle 2026-05. **Wellen-Zusammenfassung**: Cleanup-Stages 0–7 (2026-05-02/03) → Folge-Sprints aus Stage-4-Findings SEC + HYG + CVE-LOW + CVE-PDF + CVE-RQ + CVE-DG (2026-05-09) → UX-Cascade F-1 `document_converter` + F-2 `audio_converter` (2026-05-03) → F-3 `library_detail` + F-4 `podcast-flow` (2026-05-09/10) → F-5 `markdown_converter` (2026-05-10) → F-6 `library` List-View (2026-05-11). **Final-Test-Anzahl 71/71 grün im Container** (Stage-6-Baseline 36 → SEC +5 → HYG +5 → F3-IMPL +3 → F4-IMPL-B +5−2 Contract-Updates → F5-IMPL +1 → F6-IMPL +5). **BACKLOG-Hygiene-Verteilung**: 2 P3-Items entfernt (F-2.1-Doc-Korrektur Service-Gate in F-4.1 absorbiert, 2 EN-Strings in library.js in F-6-IMPL absorbiert), 8 behalten als Aktive Reminder, 3 deferred als F-N-Wellen (mermaid_converter und login neu ergänzt, Playwright bestand schon). BACKLOG umstrukturiert auf Steady-State (P0/P1-Inbox plus P3-Reminder). `OVERSEER_HANDOFF.md` via `git mv` nach `docs/archive/OVERSEER_HANDOFF_2026-05-03.md` archiviert plus Archiv-Marker. CLAUDE.md-„Historical"-Sektion mit UX-Cascade-F-3-bis-F-6-Methodik-Eintrag erweitert (Geschwister-/Schwester-Feature-Hebel-Methodik + Methodik-Lehren aus feedback-Memos + Output-Konvention). `docs/cleanup_plan.md`-Header auf „fully closed 2026-05-11" mit Wellen-Sequenz-Zusammenfassung und Test-Anzahl-Statistik. Kein Code-Touch — fünf Doc-Files. Memory-Disposition keine neue Eintrag — Master-Annotation 7 bestätigt._
_Vorgänger: F6-IMPL ☑ done 2026-05-11 → drei Commits `8049d3e` (Sub-Batch A — Silent-Failure-Familie + Cross-Feature-H4, P1/P2/P3/P4/P5/P6/P13), `741794a` (Sub-Batch B — List-View-State, P7/P8), `ca41270` (Sub-Batch C — Polish-Long-Tail, P9/P10/P11/P12/P13-bereits-in-A/P14-bereits-erfüllt). Pytest **71/71 grün** im Container (66 baseline + 5 neu: `test_api_update_conversion_404_for_other_users_conversion` P3-Backend-Contract, `test_library_empty_state_filter_aware` P7-Branch-Parität, `test_format_card_datetime_filter_renders_de_month_abbr` P11-Filter-Lokalisierung, `test_library_ignores_unknown_type_filter` P10-Allowlist, `test_library_ignores_unknown_per_page` P12-Allowlist). **14 Patterns implementiert + BT1/BT2/BT3/BT4 alle mit-gelöst** durch P3/P4/P2/P6-Apply. **1 neuer Jinja2-Filter** `format_card_datetime` in `app_pkg/__init__.py` (DE_MONTH_ABBR-Tuple Jan/Feb/Mär/Apr/Mai/Jun/Jul/Aug/Sep/Okt/Nov/Dez, container-locale-agnostisch analog `file_size`-Methodik-Pattern). **1 neuer Server-Helper** `pagination_args` in `app_pkg/library.py` (für P9 `favorites=''`-URL-Bereinigung + P12 `per_page=default`-URL-Bereinigung im selben Pfad). **`data-content`-Embed-Default-Wahl gefolgt** für P2 trotz avg=29 KB / max=178 KB (Grenzbereich: >10 KB Default-Threshold aber <50 KB Alternative-Threshold; Page mit 20 Items typisch ~580 KB HTML vertretbar für Single-User-LAN). **Drei 🔥🔥-Pflicht-Live-Master-Smoke-Patterns**: P11 (Card-Datum DE-Locale) Container-Smoke durch Filter-Unit-Test direkt verifiziert; P2 (Copy-Full-Content) + P3 (Auto-Save Failure-Banner) Code-evident verifiziert via `data-content`/`safeJSON`+`r.ok`+`showAlert`-Branches, Master-Live-Smoke ausstehend (lange Content + DevTools-Throttle). **P13 (aria-live) in Sub-Batch A mit-gefoldet** (nahegelegen beim Banner-Mountpoint-Touch — `<div id="library-action-status" aria-live="polite">` plus `announceCardRemoval`-Helper im Delete-Success-Branch + 404-Race-Branch). **P14 (Card-Hover-Lift) Code-Reading: bereits erfüllt** durch existierenden CSS-Stand (`.c-card { position: relative; transition: transform 0.15s ease; }` + `.c-card:hover { transform: translateY(-2px); }` = Pattern-Doc Option A Master-Default). **F-3.3-Übernahme-Disziplin durchgezogen**: `withServerSuffix(msg, status)` aus `library_detail.js` 1:1 in `library.js` repliziert, `SAVE_MESSAGES`-Konstanten-Pattern als top-level `FAVORITE_FAILURE_MSG`/`DELETE_FAILURE_MSG`/`DELETE_RACE_MSG`/`SESSION_EXPIRED_MSG` repliziert, `safeJSON(r).catch(()=>null).then()`-Pattern repliziert, `handleFetchError(err, fallbackMsg)` neu strukturiert mit Session-Expired-Erkennung via `error.message`-Regex. Drei-Commit-Aufteilung über temp-Backup → revert → stepwise-replay-Mechanik analog F-3-IMPL (67/67 grün nach A, 68/68 nach B, 71/71 nach C im Container). Push durch Auto-Mode-Classifier geblockt, Master pusht von Hand. **F-6 strukturell abgeschlossen** für `library` List-View. Verbleibende Sequenz: F-N… → WAVE-CLOSE._

_Vorgänger: F6-IMPL dispatched 2026-05-11 → Sprint-Prompt unter [docs/archive/sprint-prompts/SPRINT_F6-IMPL_library-list-implementation_2026-05-11.md](docs/archive/sprint-prompts/SPRINT_F6-IMPL_library-list-implementation_2026-05-11.md). **3 Sub-Batches mit A1/A2-Split-Fallback** (analog F-5-IMPL's Ca/Cb): A Silent-Failure-Familie + Cross-Feature-H4 (P1-P6 + P13-Synergie, 6+1 Patterns, 4 Smoke-Pflicht) → B List-View-State (P7+P8, 2 Patterns, 1 Smoke code-evident) → C Polish-Long-Tail (P9-P14, 6 Patterns, 3 Smoke-Pflicht). **Apply-Reihenfolge in Sub-Batch A**: P1 zuerst (Banner-Mountpoint-Vorbedingung) → P5 (DE-Microcopy-Foundation) → P3+P4 holistic (Failure-Banner aus F-3.3-Übernahme) → P6 (Toast-Level) → P2 (Copy-Full-Content mit data-content-Embed) → P13 wenn nahegelegen. **Drei 🔥🔥-Pflicht-Live-Master-Smoke**: P2 Copy-Paste / P3 DevTools-Throttle-Failure-Banner / P11 Card-Datum DE-Locale. **`format_card_datetime`-Jinja2-Filter-Disposition**: extrahieren wenn > 2-3 Zeilen Inline ODER Methodik-Parität mit `file_size`-Pattern (single-call-site OK weil analog F3-IMPL-Pattern); F6-IMPL entscheidet beim Apply. **`data-content`-Embed-Pre-Flight-Check**: Sub-Thread inspiziert typische `ConversionHistory.content`-Größe vor P2-Apply (Default-Wahl Embed wenn typisches < 10 KB, sonst Backend-Endpoint-Alternative). **BT-Folde alle 4 mit-gelöst** durch Patterns (BT1↔P3, BT2↔P4, BT3↔P2, BT4↔P6). **List-View-State-Default-Wahl-Validierung** beim Apply mit Bericht bei Abweichung (server-side has_active_filter-Empty-State + Submit-Required-mit-DE-Hint). Erwartete Test-Anzahl 66→67-70. Größe L (XS-lastig durch Geschwister-Feature-Übernahme aus F-3.3, strukturell vergleichbar mit F-5-IMPL)._
_Vorgänger: F6-PATTERNS ☑ done 2026-05-11 → commit `d920822`. Patterns-Doc unter [docs/ui_patterns_library_list_2026-05.md](docs/ui_patterns_library_list_2026-05.md). **14 Patterns** (XS: 8, S: 5, M: 1, L: 0) aus 17 Findings + 4 BTs (alle Finding-linked, BT1↔P3, BT2↔P4, BT3↔P2, BT4↔P6). **Konsolidierungs-Quote 18%** mit drei Konsolidaten (F2+F3 → P3 Auto-Save Favorite-Toggle, F4+F5 → P4 Delete, F9+F11 → P8 Search Submit-Required mit DE-Hint) — im Master-Erwartungsband 15-25%, zwischen F-5.3's 13% und F-3.3's 21%. **Geschwister-Feature-Übernahme aus F-3.3 durchgezogen** analog F-5.3's F-1.3-Disziplin: 5 direkt-übertragbare F-3.3-Patterns (P1↔F-3.3 P15 Banner-Mountpoint, P3↔F-3.3 P1+P14 Auto-Save-Banner+safeJSON, P4↔F-3.3 P3+P14 Delete-Banner+safeJSON, P5↔F-3.3 P6 DE-Microcopy-Sweep, P6↔F-3.3 P8 Toast-Level) plus 1 teil-übertragbar (P11↔F-3.3 P5 Card-Datum DE-Lokalisierung mit Mechanik-Verschiebung Server-side Custom Jinja2-Filter statt JS-Helper) — **F-3-Konvergenz-Quote 36%** (5 direkt + 1 teil von 14 F-6.3-Patterns abdeckend 8 von 17 Cross-Feature-H4-Findings, im Erwartungsband 30-50%). **3 Sub-Batches vorgeschlagen** für 1-Sprint-mit-Sub-Batches-Default analog F-5-IMPL: Sub-Batch A Silent-Failure-Familie + Cross-Feature-H4-Welle 6 Patterns (P1 Banner-Mountpoint XS, P2 Copy-Full-Content S Pflicht-Live-Master-Smoke, P3 Auto-Save Banner S Pflicht-Live-Master-Smoke, P4 Delete S Smoke-code-evident, P5 DE-Microcopy S, P6 Toast-Level XS) plus A1/A2-Split-Fallback (Foundation Sweep P1+P5+P6 vs. Silent-Failure-Banner P2+P3+P4) wenn Sub-Batch A als zu groß empfunden / Sub-Batch B List-View-State-Visibility 2 Patterns (P7 Empty-State filter-aware S Master-Default-Wahl server-side has_active_filter-Boolean + zwei Empty-State-Varianten, P8 Search Submit-Required mit DE-Hint XS Master-Default-Wahl konsistent mit URL-Persistierung) / Sub-Batch C List-Polish-Long-Tail 6 Patterns (P9 favorites=''-URL XS, P10 Type-Filter-Validation XS, P11 Card-Datum DE-Lokalisierung S Pflicht-Live-Master-Smoke, P12 Per-Page-Size-Toggle M, P13 aria-live XS Smoke-code-evident Synergie-Hinweis Sub-Batch-A-Mit-Nahme weil Verzahnung mit P4 Delete-Pfad, P14 Card-Hover-Lift XS Smoke-code-evident). **Top-5-Quick-Wins-Score-Range 7.5–10.0** mit P6 Toast-Level 10.0 + P8 Search Submit-Required 10.0 + P2 Copy-Full-Content 7.5 (BT3) + P3 Auto-Save Banner 7.5 (BT1) + P4 Delete Banner 7.5 (BT2). **8× 🔥 Smoke-Pflicht für F6-IMPL** (P2, P3, P4, P6, P8, P11, P13, P14) **davon 3× 🔥🔥 Pflicht-Live-Master-Smoke** (Master-Annotation 3): P2 Copy-Full-Content via Copy-Paste-Verifikation mit langem Content >200 char / P3 Auto-Save Favorite-Toggle via DevTools-Network-Throttle Offline + Banner-Sichtbarkeit / P11 Card-Datum DE-Lokalisierung via Browser-Inspektion „Mai" statt „May". Andere 5× code-evident verifiziert im Container (P4 Delete, P6 Toast-Level, P8 Search Submit, P13 aria-live, P14 Card-Hover). **List-View-State-Default-Wahl konkret pro Pattern** statt Variante-A/B/C-Diskussion (Master-Annotation 5): P7 Empty-State server-side has_active_filter-Boolean mit zwei Microcopy-Varianten (Filter-aktiv „Keine Treffer mit aktuellen Filtern" + „Filter zurücksetzen"-Btn vs. wirklich-leer „Noch keine gespeicherten Einträge"); P8 Search Submit-Required mit DE-Hint im Placeholder „Titel, Inhalt, Tags suchen … (Enter)" begründet durch URL-Persistierung-Design-Wahl. **Befund-1-Pattern P2** mit Master-Default-Wahl `data-content`-Attribut-Embed im Template-Render (statt neuer Backend-Endpoint) plus dokumentiertem Skalierungs-Trade-off (HTML-Bloat 100-400 KB pro Page bei 20 Items × 5-20 KB Content) und Endpoint-Alternative für F6-IMPL-Pragmatik. **Helper-Disposition: keine neuen `_utils.js`-Helper** still angelegt, Memory `feedback_helper_reuse_design_choice.md`-Geist gewahrt; ein Server-side Vorschlag dokumentiert (`format_card_datetime`-Jinja2-Filter analog `file_size` aus F-3-IMPL für P11) als Hinweis ohne Anlage. **Cross-Feature-H4-Sektion** mit Helper-Reuse-Reflexion aus F-6.2 1:1 übernommen (3 begründete Design-Wahl-Stellen: saveViewState/loadViewState URL-Persistierung, confirmInPlace kein Bulk-Delete, confirmIfLong semantisch unpassend) + 3 bereits-konvergente F-3-Patterns als positives Inventar (P9 Tag-Chips Inversions-Hinweis List-View war voraus, P11 Sidebar-Active via base.html path-Match, P12 file_size n/a für List-View) + 4 nicht-anwendbare F-3-Patterns (P4/P7/P10 Notion-only, P13 Title-Edit-only). Phase-2-Konsistenz-Check ergab keine Korrekturen — Vollständigkeit aller 17 Findings + 4 BTs adressiert, F-3-Übernahme-Disziplin durchgezogen, Live-Verifikation-Status zwischen Pattern-Tags und Smoke-Pflicht-Übersicht konsistent, Drei-Pflicht-Live-Master-Smoke-Marker pro Master-Annotation 3, Microcopy-Stichprobe regelkonform (Banner ≤2 Sätze, Buttons ≤3 Wörter, Empty-State ≤3 Sätze, keine Emojis bei Fehlern), Impact-Score-Konsistenz Top-5-Tabelle vs. Pattern-Block-Scores, keine konkreten Code-Diffs/Bug-Fix/Implementations-Sequenzen. Verbleibende Sequenz: F6-IMPL → F-N… → WAVE-CLOSE._
_Vorgänger: F6-REVIEW ☑ done 2026-05-10 → Findings-Doc unter [docs/ui_findings_library_list_2026-05.md](docs/ui_findings_library_list_2026-05.md). **17 Heuristik-Findings + 4 Bug-Tickets** (alle Bug-Tickets Finding-linked, keine Pure-Bugs ohne H-Aspekt — Disposition-Abweichung von F-3.2/F-5.2 begründet). Severity-Verteilung: Sev 4: 0 / Sev 3: 6 (F1 Copy-200char-Preview H9, F2/F3 toggleFavorite-silent H1+H9, F4/F5 deleteConversion-silent H1+H9, F6 EN-Strings H4) / Sev 2: 3 (F7 Toast-Level H4, F8 Empty-State filter-aware H9, F9 Search-not-auto-submit H4) / Sev 1: 8 (F10 Banner-Mountpoint H4, F11 Search-no-live H6, F12 favorites='' URL H4, F13 Type-Filter-Validation H9, F14 %b-Locale H4, F15 Per-Page-Size H6, F16 aria-live H1, F17 Card-Hover-Lift H6). **Cross-Feature-H4-Finding-Quote 47%** (8 von 17: F2/F3 + F4/F5 + F6 + F7 + F10 + F14) — exakt im Master-Erwartungsband 35-50%, höher als F-3.2's 35%, niedriger als F-5.2's 86% (Geschwister-Feature-Inversion bestätigt). **Befund 1 (Copy-Btn) als F1+BT3 mit H9 Sev 3 disponiert** (Master-Annotation 3 1:1 übernommen — Sev 4 geprüft und abgelehnt, weil Full-Content via Detail-View erreichbar und User merkt es beim Paste). **Befunde 2+3 (silent-fail toggleFavorite/deleteConversion) als F-3-P1/P3+P14-Korrespondenz mit H1+H9 Sev 3 übernommen** (Master-Annotation 4) — split into F2+F3 / F4+F5 wie F-3.2 F1+F2 / F4+F5. **Befund 16 (Helper-Reuse-Meta) in Cross-Feature-H4-Sektion absorbiert** als Helper-Reuse-Reflexion (Master-Annotation 5) — methodische Lehre als Memory-Eintrag verankert: `feedback_helper_reuse_design_choice.md` (Helper-Reuse-Drift mit begründeter Design-Wahl ≠ H4-Verletzung; Präzedenzfall F-6.2). **Eine begründete Severity-Abweichung von F-3.2**: F14 (%b-Locale) Sev 3→1 und H1→H4, weil Server-side `strftime` im Display-Pfad statt JS-Pre-Population in Submit-Pfad (kein Daten-Fehler in Zielsystem-Quelle). **Eine Disposition-Abweichung von F-6.1-Pre-Disposition**: Befund 9 (favorites='' URL) bleibt Finding F12 statt Bug-only, weil URL user-sichtbar (Adressleiste, Bookmark-Sharing). **10 ⚠️ code-only-markierte Findings** (F2/F3/F4/F5/F7/F9/F11/F14/F16/F17) — Master-Walkthrough-Empfehlung optional vor F6-PATTERNS, sonst 🔥 Smoke-Pflicht in F6-IMPL. **Schwerpunkt-Cluster (4)**: Silent-Failure-Familie / Cross-Feature-H4-Helper-Reuse zu F-3 / List-View-State-Visibility und Empty-State-Recovery / List-Polish-Long-Tail. **Phase-2-Konsistenz-Check ergab drei kleine Zähl-Korrekturen** (⚠️-Marker-Zähler 9→10, Cluster-1-Header Sev-Spannweite, Cluster-4-Header F14 ergänzt). Verbleibende Sequenz: F6-PATTERNS → F6-IMPL → F-N… → WAVE-CLOSE._
_Vorgänger: F6-PICK ☑ done 2026-05-10 → Inventur-Doc unter [docs/ui_inventory_library_list_2026-05.md](docs/ui_inventory_library_list_2026-05.md). **21 echte interaktive Elemente in 30 Tabellenzeilen** (Layout 9 + Filter-Bar 5 + Per-Card 4 + Pagination 3 plus 9 dekorativ/state-system) — im erwarteten Sprint-Bereich 20-40 zwischen F-3.1's 21 und F-5.1's 32. **F-3-Korrespondenz-Verteilung 6 direkt / 2 teil / 3 bereits-erfüllt / 4 nicht-anwendbar** (53% direkt+teil bei 15 F-3-Patterns, leicht über Master-Erwartung 30-50% — Begründung „Helper-Reuse-Konvergenz dominiert": 5 von 6 direkt-übertragbaren Patterns hängen am `showAlert`/`safeJSON`/Toast-Level/Banner-Mountpoint-Helper-Reuse, nur P6 ist reine Microcopy-Welle); direkt: P1 Auto-Save-silent-fail auf `toggleFavorite` / P3 Delete-silent-fail auf `deleteConversion` / P6 DE-Microcopy-Sweep / P8 Toast-Level für Copy-Fail / P14 `safeJSON`-Wrap für PUT+DELETE / P15 Banner-Mountpoint-Container; teil: P2 Pending-State (andere Stelle, Favorite-Toggle statt Title/Tags-Edit) / P5 Datum-Lokalisierung (Server-strftime statt JS-Helper); bereits-erfüllt: P9 Tag-Chip-Rendering schon Server-side mit `.c-tag`-Klasse (interessante Inversion: List-View ist hier voraus zur Detail-View) / P11 Sidebar-Active via `base.html:84` path-Match deckt List+Detail beide ab / P12 `file_size`-Filter existiert (aber n/a für List-View mangels File-Size-Display); nicht-anwendbar P4/P7/P10 Notion-only und P13 Title-Edit-only. **List-View-States 6 kartiert** (Sortierung/Filter-Type+Favorites/Suche im URL-Query, Bulk-Selektion existiert nicht, Pagination 20/Page hardcoded URL-getrieben, Empty-State nicht filter-aware) plus Page-Load/Initial-State. **Helper-Reuse-Master-Fokus-Antwort**: `saveViewState`/`loadViewState` **zweite Call-Site: nein** mit expliziter Begründung — List-View persistiert State **vollständig über URL-Query-Params** (bookmarkable, sharable, browser-back-restoriert); `confirmInPlace` aus F-4-IMPL **zweite Call-Site: nein** mit expliziter Begründung — List-View hat **keinen Bulk-Delete-Pfad** und per-Card-Delete ist semantisch näher zu F-3 #17 als zu F-4 Cancel-State-Machine. Beide Helper bleiben single-call-site, Helper-Disziplin gewahrt. **10 fehlende States** (Filter-Loading bei Auto-Submit, Live-Search/Debouncing, Favorite-Loading+Error+focus-visible, Delete-Loading+Error, Toast-Level-Failure, aria-live-Card-Remove, Empty-Filter-Awareness, Pagination-URL-Artifact, Banner-Mountpoint-Vorbedingung). **4 Code↔live-Divergenzen** (Locale-`%b` im Card-Datum, Card-Hover-Lift vs. Sub-Element-Klick, Multibyte-`content[:200]`-Cut, Copy-Btn-Preview-Only). **16 separate Befunde** mit Code-Ankern (4 Finding+Bug-Ticket-Kandidat: #1 Copy-Btn 200-char-Preview, #2 toggleFavorite-silent-fail, #3 deleteConversion-silent-fail, #4 Toast-Level-Failure; 10 nur Finding inkl. #5 Banner-Mountpoint-Vorbedingung, #14 aria-live-Card-Remove, #15 Card-Hover-vs-Click; 1 Sammel-Befund EN-Strings mit Disposition F6-PATTERNS DE-Microcopy-Folde; #16 Helper-Reuse-Beobachtung). **Akut-flag-Kandidaten: keine** — Befund 1 ist funktional-irreführend (Copy-Btn kopiert nur 200-char-Preview statt Full-Content), aber kein Crash und kein silent-Datenverlust; Befunde 2/3 sind identisch zur F-3-Welle-Familie und werden in F6-IMPL Sub-Batch nach F-3-Muster geschlossen. **9 Live-Walkthrough-Lücken** dokumentiert (Locale-Inspektion Card-Datum, Card-Hover-Animation-Robustheit, CSRF-Verhalten der API-Calls, Empty-State-Render-bei-Filter-mit-leerem-Treffer auf Page-N). **Phase-2-Konsistenz-Check ergab zwei kleine Korrekturen**: P14 in Element-Zeilen #19/#24 als sekundäre Pattern-Identität ergänzt (Übersicht hatte P14 als direkt-übertragbar gelistet, Element-Zeilen erwähnten nur P1/P3) und Befund 8 (Search-vs-Filter-Auto-Submit-Inkonsistenz) mit Code-Ankern auf die `onchange`-Stellen geschärft. Verbleibende Sequenz: F6-REVIEW → F6-PATTERNS → F6-IMPL → F-N… → WAVE-CLOSE._
_Vorgänger: F5-IMPL ☑ done 2026-05-10 → Sub-Batches A+B Master-bundled in commit `07e9aa6` (P1 Save-Failure-Banner mit `showAlert`+`safeJSON` + drei `alert()`-Calls raus, P2 Auto-Dismiss-Hook für Server-Flash-Banner via lokalem `attachAutoDismissToServerBanners`, P3 File-Info-Display nach File-Auswahl mit `formatFileSize`, P4 Iframe-a11y-Annotations `tabindex`+`role`+DE-`aria-label`, P5 Reader-Mode-State-Persistenz mit `prefs.modeOn`+Init-Rehydrate+`updateWidthButtons` immer beim Init, P6 Esc-Key-`activeElement.tagName`-Check für Form-Controls, P7 Two-Dark-Modes-Konsolidierung mit Reader-aware `isDarkActive` Reader-Scope-Sieg über Theme-Dark + BT3 mit-gefoldet (zwei `updateStyle()`-Init-Calls auf einen) + BT5 mit-gefoldet (toter `<link id="preview-style">` aus Template entfernt) + Helper-Extraktion `saveViewState`/`loadViewState` nach `_utils.js`); Sub-Batch C in commit `9e6999c` (P8 PDF-Gen-Error-Recovery via `flash`+`redirect` statt `render_template` mit BT1 strukturell mit-aufgelöst + Backend-Test `test_convert_markdown_pdf_gen_error_redirects_with_flash`, P9 Sample-Markdown-Template-Bug-Fix via `{% if not markdown_text %}…{% endif %}`-Wrap mit BT2 mit-aufgelöst, P10 Theme-CSS-Fetch-Recovery mit `showAlert`-Warning + Theme-Name-Interpolation + `currentThemeCSS=''`-Fallback, P11 markdown-it-CDN-Fallback-DE-Microcopy mit Recovery-Hinweis, P12 Empty-Submit-Frontend-Pre-Check mit `showAlert`-Warning + Textarea-Fokus, P13 Submit-Loading-Persistenz via Restore-Zeile-Entfernung + DE-Loading-Microcopy „Wird vorbereitet …" + Submit-Btn-Default-Label DE „PDF erstellen"). Pytest **66/66 grün** im Container (65 baseline + 1 für P8). **Live-Master-Smoke clean** für P5+P7 (Reader-Mode-Reload-Persistenz + Two-Dark-Modes-Sequenzen 1+2). P8-Master-Smoke (PDF-Gen-Error forcieren) noch ausstehend — empfohlen via Theme-Datei-Manipulation oder Playwright-Stop. **13 Patterns implementiert + BT1/BT2 mit-gelöst + BT3/BT5 mit-gefoldet + BT4 out-of-scope** (Inline-`<style>` separater Hygiene-Pass) + **1 neuer Helper in `_utils.js`** (`saveViewState`/`loadViewState` als generischer JSON-LocalStorage-Wrapper mit Try-Catch+Default-Fallback) + **1 lokaler Helper** (`attachAutoDismissToServerBanners` in `markdown_converter.js`, single-call-site). **Master-Default-Wahl gefolgt für P5/P6** (existierender `localStorage.readerPrefs`-Key + `activeElement.tagName`-Check), **pragmatische Abweichung in P7 dokumentiert** (`isDarkActive` Reader-aware statt pure-OR weil Smoke-Sequenz-1 das verlangte). **F-5 strukturell abgeschlossen** für `markdown_converter`. Verbleibende Sequenz: F-N… (Master-Pick zwischen `library` List-View / `mermaid_converter` / `login`) → WAVE-CLOSE._

_F5-IMPL dispatched 2026-05-10 → Sprint-Prompt unter [docs/archive/sprint-prompts/SPRINT_F5-IMPL_markdown-converter-implementation_2026-05-10.md](docs/archive/sprint-prompts/SPRINT_F5-IMPL_markdown-converter-implementation_2026-05-10.md). **3 Sub-Batches mit Ca/Cb-Split-Fallback** (gestützt auf F-3-IMPL-15-in-3-Lehre und F-4-IMPL-B-10-in-1-Lehre): A Cross-Feature-H4-Konvergenz (P1-P4, helper-reuse, kein Smoke) → B Reader-Mode-State (P5-P7, alle 3 Smoke-Pflicht) → C Error-Recovery+Async-Pre-Check (P8-P13, 5 von 6 Smoke-Pflicht, Ca=P8+P9 / Cb=P10-P13 als Fallback). **Apply-Reihenfolge-Empfehlung in Sub-Batch C**: P8 zuerst (Befund-16-Quick-Win Score 15.0), dann P9 (BT2-isoliert), dann P12+P13 (gemeinsamer Submit-Handler), dann P10+P11. **Smoke-Pflicht-Kalibrierung**: 8 Smoke-Patterns runter auf **Drei-Pflicht-Live-Master-Smoke** (P5 Reader-Mode-Persistenz / P7 Two-Dark-Modes-Reihenfolge / P8 PDF-Gen-Error forcieren), die anderen 5 als Container-Smoke / Code-Reading. **Helper-Vorschlags-Disposition**: `saveViewState/loadViewState` extrahieren nach `_utils.js` (zweite Call-Site library-View-State absehbar) / `attachAutoDismissToServerBanners` lokal in `markdown_converter.js` (single-call-site). **BT-Folde-Disposition**: BT1↔P8 + BT2↔P9 mit-gelöst durch Pattern-Apply / BT3 mit-folden in P7-Init-Touch / BT5 mit-folden in P5-Init-Touch / BT4 explizit out-of-scope (Inline-`<style>` reine HTML-Reorganisation, separater Hygiene-Pass). **Reader-Mode-Default-Wahl-Validierung**: F-5.3-Master-Annotation-4-Defaults (gemeinsamer `localStorage.markdown.viewState`-Key, `activeElement.tagName`-Esc-Check, Reader-Dark-lokaler-Scope-Sieg) sind im Pattern verankert; Sub-Thread kann beim Apply abweichen mit Begründungs-Bericht. Erwartete Test-Anzahl 65→66-69. Größe L (XS-lastig durch Schwester-Feature-Übernahme, weniger als F-3-IMPL-XL)._
_Vorgänger: F5-PATTERNS ☑ done 2026-05-10 → commit `06f74b5`. Patterns-Doc unter [docs/ui_patterns_markdown_converter_2026-05.md](docs/ui_patterns_markdown_converter_2026-05.md). **13 Patterns** (XS: 8, S: 4, M: 1, L: 0) aus 15 Findings + 5 BTs (BT1↔P8, BT2↔P9 finding-linked; BT3/BT4/BT5 pure out-of-scope mit Verzahnung-Hinweisen für P5/P7-Init-Touches).
_Vorgänger: F5-PATTERNS ☑ done 2026-05-10 → commit `<hash>`. Patterns-Doc unter [docs/ui_patterns_markdown_converter_2026-05.md](docs/ui_patterns_markdown_converter_2026-05.md). **13 Patterns** (XS: 8, S: 4, M: 1, L: 0) aus 15 Findings + 5 BTs (BT1↔P8, BT2↔P9 finding-linked; BT3/BT4/BT5 pure out-of-scope mit Verzahnung-Hinweisen für P5/P7-Init-Touches). Konsolidierungs-Quote 13% mit zwei Konsolidaten (P1 F1+F2 Save-`alert()`-Familie identische Code-Pfad-Wurzel, P5 F9+F11 Reader-Mode-State-gemeinsame-`localStorage`-Mechanik) — strukturell niedriger als F-2.3 50% / F-3.3 21% / F-4.3 25%, weil Schwester-Feature-Übernahme-Disziplin „F-1-Pattern-Identität bleibt erkennbar" 1:1 statt zu konsolidieren bedeutet. **3 Sub-Batches vorgeschlagen** für 1-Sprint-mit-Sub-Batches-Default (gestützt auf F-3-IMPL-15-in-3-Lehre und F-4-IMPL-B-10-in-1-Lehre): Sub-Batch A Cross-Feature-H4-Konvergenz zu F-1 4 Patterns (P1 Save-`alert()`-Banner S, P2 Auto-Dismiss-Hook XS, P3 File-Info-Display S, P4 Iframe-a11y XS) helper-reuse-dominiert kein Smoke / Sub-Batch B Reader-Mode-State und Visual-Layout 3 Patterns (P5 Reader-Mode-Persistenz M Smoke-Pflicht-für-F11-Teil, P6 Esc-Key-Scope XS Smoke-Pflicht, P7 Two-Dark-Modes-Konsolidierung S Smoke-Pflicht) alle drei mit Master-Default-Wahl-Marker / Sub-Batch C Error-Recovery + Async-Pre-Check 6 Patterns (P8 PDF-Gen-Error-Recovery+BT1 XS Smoke-Pflicht, P9 Sample-Merge+BT2 XS Smoke-Pflicht, P10 Theme-CSS-Fetch-Recovery S Smoke-Pflicht, P11 markdown-it-CDN-Fallback-DE XS Smoke-Pflicht, P12 Empty-Submit-Pre-Check XS, P13 Submit-Loading-Persistenz XS Smoke-Pflicht) plus Zwei-Sub-Batch-Split-Empfehlung Ca/Cb (Backend+Template-Error-Recovery vs. Frontend-Error-Recovery+Submit-Pfad) als Fallback wenn Sub-Batch C als zu groß empfunden wird. **Top-5-Quick-Wins-Score-Range 7.5–15.0** mit P8 PDF-Gen-Error-Recovery 15.0 (Sev 3 + XS, Befund-16-Pattern als #1-Quick-Win — pragmatische Befund-16-Folding-Entscheidung wird damit quasi-Hot-Fix mit der ersten Sub-Batch-Welle in F5-IMPL), P9 Sample-Merge-Template-Bug 10.0, P12 Empty-Submit-Pre-Check 10.0, P13 Submit-Loading-Persistenz 10.0, P1 Save-Failure-Banner 7.5. **8× 🔥 Smoke-Pflicht in F5-IMPL** (P5 für F11-Teil, P6, P7, P8, P9, P10, P11, P13) adressieren alle 8 ⚠️ code-only-Findings aus F-5.2 (F3, F5, F6, F7, F8, F11, F12, F15) mit expliziter Smoke-Mechanik pro Pattern (DevTools-Network-Block für Theme-CSS und CDN; Reload-Sequenz für Reader-Mode-State; Theme-Toggle-Reihenfolge für Two-Dark-Modes; Esc + Textarea-Fokus-Test; Network-Throttle für Submit-Loading; PDF-Gen-Failure-Forcierung für PDF-Gen-Error-Recovery; Empty-Page-Load für Sample-Merge). **F-1-Übernahme-Anzahl 6 Patterns aus 7 Cross-Feature-H4-Findings** (47%-Quote aus F-5.2 strukturell repräsentiert): 4 direkt-übertragbare F-1.3-Patterns (P4→P1 Save-`alert()`, P7→P2 Auto-Dismiss, P12→P3 File-Info-Adaption Aufwand-Hochstufung XS→S weil Display insgesamt fehlt nicht nur Unit-Bug, P13→P4 Iframe-a11y) plus 2 teil-übertragbare (P1→P12 Empty-Submit Sev-Anpassung 4→2 plus Aufwand-Senkung S→XS weil keine Drop-Zone, P9→P13 Submit-Loading andere Mechanik wegen native form.submit() plus Aufwand-Senkung M→XS weil Restore-Zeile-Entfernung statt Drop-Zone-Loading-Skeleton). 5 bereits-konvergente F-1-Patterns (P3 Save-Reset, P6 accept-SoT, P8 Frontend-Vorab-Check mit DE-Microcopy bereits-da, P10 Result-Scroll durch Split-Pane, P11 Keyboard-Pfad durch native Input) als positives Inventar gelistet, 2 nicht-anwendbare (P2 Result-Area, P5 Drag-Active-Highlight) erklärt — Konvergenz-Quote 86% aus F-5.2 1:1 reproduziert. **Reader-Mode-Default-Wahl-Hinweis** (Master-Annotation 4): konkrete pragmatische Defaults pro Pattern statt Variante-A/B/C — P5 gemeinsamer `localStorage.readerPrefs`-Key (existiert) + Mode-On/Off-Persistenz + `updateWidthButtons` immer beim Init, P6 `activeElement.tagName`-Check im Esc-Handler statt Container-Scope-Listener-Refactor, P7 Reader-Dark als lokaler Scope-Sieg über Theme-Dark wenn Reader aktiv mit sauberem Scope-Abfall beim Exit. **Helper-Vorschläge** (nicht still mit-angelegt, F5-IMPL entscheidet beim Cluster-Schnitt): `saveViewState`/`loadViewState` aus P5 plus `attachAutoDismissToServerBanners` aus P2 in dedizierter Sektion am Doc-Ende. Verbleibende Sequenz: F5-IMPL → F-N… → WAVE-CLOSE._
_Vorgänger: F5-REVIEW ☑ done 2026-05-10 → Findings-Doc unter [docs/ui_findings_markdown_converter_2026-05.md](docs/ui_findings_markdown_converter_2026-05.md). 15 Findings (Sev 4: 0, Sev 3: 3, Sev 2: 6, Sev 1: 6) plus 5 Bug-Tickets (BT1–BT2 finding-linked zu F3 und F6, BT3–BT5 pure ohne UX-H-Aspekt). **Pattern-Konvergenz-Quote 86%** (12 von 14 anwendbaren F-1-Patterns) — Schwester-Feature-Inversion zu F-4.2 bestätigt, im Master-Erwartungs-Bereich 80-90%. **Cross-Feature-H4-Findings 47%** (7 von 15) auf Finding-Ebene — höher als F-2.2 (~41%) und F-3.2 (~35%) und F-4.2 (0%) wegen Schwester-Feature-Status. **Heuristik-Verteilung 5-2-4-4 H1/H4/H6/H9** — H1 dominiert wegen Visibility-Lücken (Empty-Submit-Roundtrip, File-Info-fehlt, Submit-Loading-kurz, Sample-Merge), H6 deutlich wegen Reader-Mode-Lifecycle-Lücken, H9 deutlich wegen Error-Pfade (PDF-Gen, Theme-Fetch, CDN-Fail, Save-Recovery). **Befund 16 PDF-Gen-Error-Re-Render als Finding + Bug-Ticket BT1 für F5-IMPL-Mit-Fix disponiert** (1:1 Master-Annotation gefolgt: H9 Sev 3 ⚠️ code-only, kein Hot-Fix-Sprint, kein Code-Fix in der Doc; F5-PATTERNS greift "PDF-Gen-Error-Recovery" als eigenes Pattern auf). **Vier Schwerpunkt-Cluster**: (1) Cross-Feature-H4-Helper-Reuse zu F-1 mit F1+F2+F14+F13+F10 — günstigster Konvergenz-Pfad mit existing-helper-reuse XS-S Aufwand; (2) Reader-Mode-State und Visual-Layout F9+F11+F12+F8 — H6/H4-dominiert, Daily-Usage-Schmerz mittel-hoch wegen F9 Reader-Mode-On/Off-Persistenz; (3) Error-Recovery-Pfade F3+F5+F6+F2 — H9-dominiert alle ⚠️ code-only mit Smoke-Pflicht in F5-IMPL; (4) Async-Pre-Check und Loading-Visibility F4+F7. **8 Findings mit `⚠️ code-only`-Marker** (F3, F5, F6, F7, F8, F11, F12, F15) — Master-Walkthrough vor F5-PATTERNS empfohlen für Sev-3-Sev-2-`⚠️`-Findings: F3 PDF-Gen-Error-Trigger forcieren, F8 Theme-Toggle ↔ Reader-Mode-Dark-Reihenfolge, F6 post-BT1-Smoke-pflichtig, F5 DevTools-404-Block, F9 code-evident kurze Verifikation günstig. **Severity-Abweichungen begründet** (Daily-Usage-Schmerz-Gewichtung): F4 (P1 Empty-Submit) F-1.2 Sev 4 → Sev 2 weil Server-Flash-Roundtrip Feedback liefert; F14 (P7 Auto-Dismiss) F-1.2 Sev 2 → Sev 1 weil Close-`×` schon im Template inline da; F10 (P12 File-Info) H4 Sev 1 → H1 Sev 1 weil Display insgesamt fehlt nicht nur Konvention-Bruch. **5 bereits konvergente F-1-Patterns** (P3 Save-Reset durch setTimeout, P6 accept durch SEC-Sprint-F-006, P8 Frontend-Vorab-Check mit DE-Microcopy, P10 Result-Scroll durch Layout, P11 Keyboard-Pfad durch native Input) als positives Inventar gelistet — kein Finding nötig, P10 und P11 von F-5.1's „teil-bzw-bereits-erfüllt" auf „bereits konvergent" verschärft mit Architektur-Begründung. **3 Disposition-Abweichungen von F-5.1**: Befunde 6/14/15 zu Bug-only (kein UX-H-Aspekt; analog F-3.2 BT7+BT8 und F-4.2 BT4); Befund 5 H4 → H1 umklassifiziert weil Display insgesamt fehlt. **2 Inventur-Befunde aus F-5.2 ausgenommen** (Befund 8 EN-Flash-Strings für DE-Pass-Welle in F5-PATTERNS; Befund 19 konformer Codestyle window.PageData ohne UX-H). Verbleibende Sequenz: F5-PATTERNS → F5-IMPL → F-N… → WAVE-CLOSE._
_Vorgänger: F5-PICK ☑ done 2026-05-10 → Inventur-Doc unter [docs/ui_inventory_markdown_converter_2026-05.md](docs/ui_inventory_markdown_converter_2026-05.md). 32 echte interaktive Elemente in 44 Tabellenzeilen, F-1-Korrespondenz-Verteilung 4 direkt übertragbar (P4, P7, P13, P14) / 8 teil-übertragbar bzw. bereits-erfüllt (P1, P3, P6, P8, P9, P10, P11, P12) / 2 nicht-anwendbar (P2, P5) → ≈86% Cross-Feature-H4-Quote bestätigt Sprint-Erwartung 80%+; 10 markdown-spezifische Befunde ohne F-1-Korrespondent (Befunde 4, 6, 9, 10, 11, 14, 15, 16, 17, 18); ~5 fehlende States (Iframe-Loading, Theme-CSS-Fetch-Error, Iframe-`tabindex`/`role`, Submit-Loading nach CSRF-Refresh, kein File-Info-Display); 6 Code↔live-Divergenzen; 19 separate Befunde (Befund 19 ist konformer-Codestyle-Notiz); **Befund 16 akut-flag-Kandidat** (PDF-Generation-Error-Pfad re-rendert Template ohne `themes`/`accepted_extensions`-Context → secondary 500-Error statt flash-Banner; Behebung trivial via `redirect()` statt `render_template`, Master entscheidet ob Hot-Fix-Sprint vorgezogen wird); 7 Live-Walkthrough-Lücken (Iframe-Flicker, Reader-Visual, Width-Buttons-Initial-Active, PDF-Download-Verhalten, Browser-Validation-Popover, Theme-CSS-Fetch-Latenz, CSRF-Refresh-Race); Helper-Reuse-Spuren-Sektion neu eingeführt (zeigt `showAlert` ✓ im Submit-Handler-Extension-Pfad mit DE-Microcopy als einziger DE-String der Page, drei Browser-`alert()`-Calls in `saveMarkdownToLibrary` ✗, kein `safeJSON`/`showToast`/`formatFileSize` ✗ trotz vorhandener Helper); F-006/F-002/F-007/F-008/F-011 nur als Header-Verweis dokumentiert nicht in Befunden; Code-only-Inventur (kein Live-Walkthrough — Schwester-Feature-Effizienz aus F-1.1 ausgenutzt). Verbleibende Sequenz: F5-REVIEW → F5-PATTERNS → F5-IMPL → F-N… → WAVE-CLOSE._

_Vorgänger F4-IMPL-B ☑ done 2026-05-10 → commit `3ef7f9e`. **P3-P12 implementiert** (10 Patterns Cluster II + Cluster III in einem Sweep, kein Sub-Batch-Split). Pytest **65/65 grün** im Container (60 baseline + 5 neu inklusive 2 Contract-Updates auf queued/started). **P3-Mechanik: Plan A (`job.meta` + `job.save_meta()`)** — keine RQ-Worker-Subprocess-Sync-Latenz beim Live-Smoke aufgetaucht, Plan-B-Redis-Key bleibt als dokumentierter Fallback ungenutzt. Helper-Layout: `update_job_stage` in `tasks.py` für Worker-Top-Level-Calls (`finalizing`), Inline-Duplikat `_set_stage` in `services/gemini/tts.py` (~12 LOC) wegen Import-Cycle `tasks → services → tts → tasks`. Stage-Marker an: `filtering` / `chunking` / `tts_chunk` mit chunk_current+chunk_total / `concatenating` / `finalizing`. Frontend rendert Sub-Caption + Linear-Progress-Bar (nur bei `tts_chunk`-Stage), aria-live=polite. Backend `/podcast-status`: P5 differenziert `queued`/`started`, reicht meta.stage durch (P3), behandelt `deferred`/`scheduled` explizit (P8). P9 Allowlist-Konvergenz analog F-013 für `/generate-gemini-podcast` (dialogue/language/tts_model) plus `MAX_RAW_TEXT_CHARS=50000` in `app_pkg/config.py` für `/format-dialogue-with-llm`. Frontend P9: Live-Char-Counter unter Source-Textarea mit grau→gelb→rot je nach Schwellwert, plus `maxlength`-Attribut. P4 Browser-Reload-Recovery: `localStorage.podcast.activeJobId` plus IIFE `reattachActiveJobOnLoad` auf `DOMContentLoaded` mit sofortigem In-Flight-UI-Render statt 2-Sek-Tick-Wait. P7 Loading-State-Konvergenz: einheitlicher `setBtnLoading`-Helper plus `.is-loading`-Klasse triggert F16-Spinner-Animation auf existierendem Generate-Btn-Icon (Reuse `mic-button-spin` Keyframes 0.8 s linear). P8 Polling-Robustheit: dedizierter `pollPodcastStatus(jobId)` mit `r.ok`-Check, 404-Branch (Job-TTL), 5xx-Retry mit Counter (max 3) → Danger-Banner, Status-Allowlist `POLLING_KNOWN_STATES` Set, Frontend-Timeout 10 min mit persistentem Info-Banner. P11 Skript-Textarea-Readonly: `setPodcastScriptReadonly` analog F-2.3 P10 plus Hint-Zeile. P10 Skript-Format-Hilfe: Hint-Zeile expandiert (Format + Sprecher-Map + Skip-Branch) plus `preParseScriptForHint()` auf blur-Event mit Zeilen-Hinweis-Banner bei Empty-`dialogue[]`. P12 Tab-Disabled: aria-disabled-Guard im Click-Handler + CSS `pointer-events: none` plus `opacity: 0.5`. P6 Download-Toast-Guard: `showToast` raus, info-`showAlert` mit Pop-Up-Blocker-Hinweis. **BT4 Audio-Blob-URL-Revoke gefolded** (Patterns-Doc empfahl P2-Verzahnung, hier nahegelegen via Download-Pfad-Touch): `lastPodcastBlobUrl` getrackt, revoke auf `pagehide` plus vor jedem `URL.createObjectURL`-Aufruf. **`confirmInPlace`-Helper-Disposition**: blieb lokal in `audio_converter.js` (Cancel-Confirm-State-Machine bleibt einzige Call-Site, F4-IMPL-B fand keine zweite). **Live-Smoke clean** für alle drei 🔥-Pflicht-Patterns (P4 Browser-Reload-Recovery + P7 Spinner-Animations-Vergleich + P8 Network-Drop-Throttle) plus Cluster-I-Regression-Smoke (Cancel SIGKILLt Worker weiter, kein orphan WAV) plus P3 Stage-Progress-Smoke (saubere Übergänge filtering→chunking→tts_chunk_X_of_N→concatenating→finalizing). 9 Files geändert, 744/-64 Zeilen Diff. **F-4 strukturell abgeschlossen** für `podcast-flow`. Verbleibende Sequenz: F-N… → WAVE-CLOSE._

_Vorgänger F4-IMPL-A ☑ done 2026-05-10 → commit `e6e9443`. **P1 Cancel-Mechanik + P2 Orphan-Cleanup atomic**, ein Commit. Pytest **60/60 grün** im Container (51 baseline + 9 neu). RQ-Cancel-Option: **Option B** (`send_stop_job_command` aus `rq.command`). rq-2.8.0-Source-Reading bestätigt: PubSub-publish → Worker-subscriber ruft `worker.kill_horse()` → `os.killpg(getpgid(horse_pid), SIGKILL)` auf das Horse-Subprozess-Group, also echte mid-execution-SIGKILL-Mechanik kein Status-Flip. Cooperative Redis-Key-Polls (Option A) übersprungen weil mid-TTS-Call (30+s pro Chunk) sowieso ohne Wirkung — und Sprint-Prompt-Regel "Option B wenn rq tatsächlich SIGINT/SIGTERM-Mechanik liefert" griff. `confirmInPlace`-Helper: **lokal** in `audio_converter.js` (inline State-Machine `idle → confirm-pending → cancelling` mit 5s Timeout-Reset) — Patterns-Doc empfahl explizit das Helper-Vorschlag-Pattern bis zur zweiten Call-Site, dort ist dies der erste Call-Site. Worker-Stop greift code-evident: neuer `POST /podcast-cancel/<job_id>`-Endpoint in `app_pkg/podcasts.py` mit Status-Branch (`started` → `send_stop_job_command`, `queued`/`deferred`/`scheduled` → `Job.cancel()`, `finished`/`failed`/`stopped`/`canceled` → terminal-no-op) plus `meta['cancelled_by_user']`-Flag plus deterministischer `OUTPUT_DIR/{job.id}.wav` aus `tasks.py` via `rq.get_current_job()` plus Path.is_relative_to-guarded Cleanup. `/podcast-status` collapses `stopped`/`canceled`/`failed-mit-meta` → `cancelled` für saubere Frontend-Terminal-Branchsicht. Frontend-Lüge raus: `podcastCancelRequested`-Short-Circuit entfernt, Polling läuft weiter mit `Wird abgebrochen …`-Zwischenstate, Banner erst nach Worker-confirmed `cancelled` aus `/podcast-status`. **Live-Smoke Cancel-Disk-Forensik 2026-05-10 07:19:46 (Job `00fa3dbd-b701-4ff0-93b9-97ff6c3182d2`)**: Worker-Stop-Latenz **15 Sekunden** (vs. **1:12 Min** vor Sprint = 4.8x Reduktion); Disk-Output **0 Bytes** (vs. 11.7 MB orphan WAV); TTS-Token-Verbrauch praktisch 0 (vs. voller 1:12 Min Pipeline-Burn); Worker-Logs zeigen `killed horse pid 587` plus `moving job to FailedJobRegistry` — Option-B-Mechanik wirkt out-of-the-box ohne Cooperative-Cancel-Checks. Push durch Auto-Mode-Classifier geblockt (analog F-3-IMPL), Master pushed von Hand. **9 neue Tests**: cancel-endpoint-Contracts (started/queued/finished/foreign/no-such/other-exception), Orphan-WAV-Unlink mit echtem `tmp_path`, Status-cancelled-Mapping (rq stopped + failed-with-meta). 4 Files geändert, 316/-28 Zeilen Diff. Verbleibende Sequenz: F4-IMPL-B (P3-P12, 10 Patterns Cluster II Async-State-Visibility + Cluster III Polish/Defensiv) → F-N… → WAVE-CLOSE._

_Vorgänger F4-PATTERNS ☑ done 2026-05-10 → Patterns-Doc unter [docs/ui_patterns_podcast_flow_2026-05.md](docs/ui_patterns_podcast_flow_2026-05.md). **12 Patterns** (XS: 4, S: 4, M: 3, L: 1) aus 16 Findings + 3 finding-linked BTs (BT1↔P1, BT2↔P3, BT3↔P2). Konsolidierungs-Quote 25% mit vier Konsolidaten (P1 F1+F2 Cancel-Familie, P7 F7+F16 Loading-State-Konvergenz, P8 F8+F15 Polling-Robustheit, P9 F13+F14 Backend-Validation) — im Master-Erwartungs-Bereich 15–25%. **3 Cluster vorgeschlagen** mit Drei-Cluster-Default plus Zwei-Sprint-Split-Empfehlung als Fallback wegen P1 L-Aufwand: Cluster I Cancel-und-Cleanup-Recovery 2 Patterns mit ✅ live-verifiziert (P1 Cancel-Mechanik mit RQ-cooperative-cancel-Recherche-Punkt + Worker-Stop + Frontend-Lüge-Fix + Confirmation-Dialog L, P2 File-TTL-plus-Re-Download M) / Cluster II Async-State-Visibility 5 Patterns (P3 Stage-Progress-via-job.meta-Hebel mit Plan-B-Redis-Key bei Subprocess-Sync-Risiko M, P4 Browser-Reload-LocalStorage S Smoke-Pflicht, P5 queued-vs-started-Backend-Differentiation XS, P6 Download-Toast-Guard XS, P7 Loading-State-Konvergenz S Smoke-Pflicht-für-F16-Teil) / Cluster III Defensiv-Robustheit-und-Skript-Hilfe 5 Patterns (P8 Polling-Robustheit S Smoke-Pflicht, P9 Backend-Validation-Konvergenz S, P10 Skript-Format-Hilfe M, P11 Skript-Textarea-Readonly XS, P12 Tab-Disabled-Konsistenz XS). **Top-5-Quick-Wins-Score-Range 5.0–10.0** — strukturell niedriger als F-3.3 (7.5–15.0) weil P1 Cancel-Mechanik (Sev 4) auf L-Aufwand liegt, also keine Sev-4-XS-Wins möglich; P5 / P6 / P12 alle 10.0 mit XS-Aufwand, P4 7.5, P11 5.0. **2 Live-verifiziert** (P1 F1+F2, P2 F4 — Master-Walkthrough 2026-05-10 Cancel-Disk-Forensik Job 9bf48e0a) **+ 3 Smoke-Pflicht** (P4 F5 Browser-Reload, P7 F16 Spinner-Animation, P8 F8 Polling-Network-Drop) für die verbleibenden code-only-Findings. Helper-Reuse: bestehende `_utils.js`-Helper genutzt (`showAlert`, `safeJSON`); 1 Helper-Vorschlag `confirmInPlace(triggerEl, options)` aus P1 Cancel-Confirmation am Doc-Ende dokumentiert (nicht still mit-angelegt — F4-IMPL entscheidet). Architektur-Hebel-Disziplin: P3 nutzt explizit Worker-Logger-Lines→job.meta-Hebel mit Plan-B-Redis-Key-Sackgassen-Vermerk; P1 RQ-cooperative-cancel-Recherche transparent an F4-IMPL delegiert. BT4 Audio-Blob-URL-Revoke explizit out-of-scope notiert mit Hinweis auf P2-Verzahnung beim Re-Download-Pfad. Verbleibende Sequenz: F4-IMPL → F-N… → WAVE-CLOSE._

_Vorgänger F4-REVIEW ☑ done 2026-05-10 → Findings-Doc unter [docs/ui_findings_podcast_flow_2026-05.md](docs/ui_findings_podcast_flow_2026-05.md). 16 Findings (Sev 4: 2 / Sev 3: 3 / Sev 2: 6 / Sev 1: 5) plus 4 Bug-Tickets (3 finding-linked BT1–BT3 + 1 pure BT4 Blob-URL-Revoke ohne UX-H-Aspekt). **Cross-Feature-H4-Quote 0%** (drastisch unter Master-Erwartung 15-25% wegen sauberer F-2-Helper-Konvergenz im audio_converter.js-Podcast-Block; F-4.1 Befund 20 bestätigt — keine künstlichen H4-Findings konstruiert). **Heuristik-Verteilung gleichgewichtig 6-2-2-6 H1/H4/H6/H9** wie Master vorhergesagt — H1+H9 dominieren wegen 5/6 unzureichender Async-State-Klassen. **Async-spezifisch 10 von 16 Findings (62.5%)** — Async-State-Klassen-Mapping-Sub-Sektion strukturiert F4-PATTERNS. **Vier Schwerpunkt-Cluster**: (1) Cancel-und-Cleanup-Recovery F1+F2+F4 Sev 3-4 mit Daily-Usage-Schmerz hoch — Cancel-Lüge ist Strukturanalogon zur F-2.1 Drag-Drop-Lüge; (2) Async-State-Visibility F3+F5+F6+F10+F16 H1-dominiert — Stage-Progress-Fehlen zentraler Daily-Usage-Schmerz mit Architektur-Hebel auf existierende Worker-Logger-Lines; (3) Polling-und-Defensiv-Robustheit F8+F13+F14+F15 H9-dominiert kleine Defensiv-Lücken; (4) Speaker-Format-Hilfe-und-Edit-Verhalten F11+F12 H6 Skript-Parser-Fragilität plus Skript-Textarea-readonly-Pattern aus F-2. **6 Findings mit `⚠️ code-only`-Marker** (F1, F2, F4, F5, F8, F16) mit priorisierter Test-Anleitung-Sequenz aus F-4.1 für Master-Walkthrough-Empfehlung — Cancel-Disk-Forensik > Re-Download-Browser-Block > Browser-Reload > Multi-Chunk > Network-Drop. **Disposition-Abweichung von F-4.1**: Befund 11 Blob-URL-Revoke von „nur Finding" zu „nur Bug-Ticket" verschoben (kein UX-H-Aspekt; analog F-3.2 BT7/BT8); Befund 17 redundant in BT4 absorbiert; Befund 20 Helper-Reuse-Meta in Cross-Feature-Sektion absorbiert; Befund 2 (F-2.1-Doc-Korrektur) und Befund 3 (Legacy /generate-podcast Dead-Code) explizit „aus F-4.2 ausgenommen" katalogisiert (gehören zu Hygiene-/Doc-Wellen). Verbleibende Sequenz: F4-PATTERNS → F4-IMPL → F-N… → WAVE-CLOSE._

_Vorgänger F4-PICK ☑ done 2026-05-10 → Inventur-Doc unter [docs/ui_inventory_podcast_flow_2026-05.md](docs/ui_inventory_podcast_flow_2026-05.md). 17 echte interaktive Elemente + 3 Async-State-Pseudo-Elemente in 30 Tabellen-Zeilen, Async-State-Coverage 5/6 unzureichend (queued+started konflatiert, stage-progress fehlt komplett, cancelled ist Frontend-Lüge), 5 Code↔live-Divergenz-Verdachts-Items, 19 Befunde mit Disposition 3 Bug-Ticket-Kandidaten (Befund 4 Stage-Progress-job.meta + Befund 9 Cancel-Lüge + Befund 18 File-Cleanup-vs-Re-Download) / 15 nur-Findings / 1 Pre-Existing-Erwähnung (Befund 3 Legacy `/generate-podcast` Dead-Code-Kandidat); Async-Pipeline-Mapping vollständig als ASCII-Sequenz-Diagramm Frontend→Backend→Worker→Polling→Download plus Cancel-Pfad plus Re-Download-Pfad; 7 separate Live-Walkthrough-Test-Anleitungen für die Async-Übergänge (kurzer Single-Chunk-Job / Multi-Chunk-Job / Cancel-mid / Browser-Reload / Network-Drop / Service-Gate / Re-Download); Helper-Reuse aus F-2 sauber durchgezogen — kein `alert()`-Pfad, keine drei konkurrierenden Error-UI-Patterns wie in der ursprünglichen F-2.1; F-2.1-Doc-Korrektur (Service-Gate-Banner-Verhalten veraltet) als P3-Item in BACKLOG nachgepflegt. Verbleibende Sequenz: F4-REVIEW → F4-PATTERNS → F4-IMPL → F-N… → WAVE-CLOSE._

_Vorgänger F3-IMPL ☑ done 2026-05-09 → drei Commits `843574b` (Sub-Batch A — Foundation + Silent-Failure, P15/P1/P3/P6/P7/P8/P14), `40dd02e` (Sub-Batch B — Notion-Form-Stability, P4/P5), `b3e666a` (Sub-Batch C — Polish + a11y, P2/P9/P10/P11/P12/P13). Pytest 51/51 grün im Mintbox-Container (48 baseline + 3 neu: `test_api_update_conversion_rejects_non_dict_body` und `test_api_delete_conversion_404_for_other_users_conversion` für die P1/P3-Failure-Banner-Backend-Contracts plus `test_file_size_filter_matches_js_helper` für die P12-JS↔Python-Parität bei 222 B / 4,6 KB / 1,2 MB). **15 Patterns implementiert** (14 mit Code-Touch + P11 als „bereits durch base.html:84-Path-Match erfüllt, kein Code-Touch" mit Code-Anker dokumentiert — Code-only-Inventur-Befund F14/BT6 war eine Fehl-Annahme über die `base.html`-Mechanik) + **6 finding-linked Bug-Tickets BT1–BT6 mitgefoldet** (BT7 textarea-escape und BT8 window.open-noopener bleiben als pure Bug-Tickets ohne UX-H-Komponente liegen, kommen in Sammel-Bug-Pass). **1 neuer Helper** in `_utils.js` (`formatDatetimeLocalNow` für P5 — vom Helper-Vorschlag in F-3.3-Doc übernommen); zweiter Vorschlag `renderTagChips` blieb bewusst lokal in `library_detail.js` (single-call-site, kein Cross-Feature-Reuse-Anlass heute) — Helper-Disziplin gewahrt. Server-side `file_size`-Jinja-Filter in `app_pkg/__init__.py` spiegelt `formatFileSize` aus `_utils.js` (DE-Komma, B/KB/MB) — Sub-MB-Dateien zeigen jetzt z.B. „4,6 KB" statt „0,0 MB". Smoke-Pflicht-Patterns P1/P3/P4/P5: code-evident verifiziert vor Apply (kein .catch/r.ok-else; UTC-`toISOString`; `innerHTML=...`-Wipe-Pfad), Container-side Backend-Smoke clean (Filter-Roundtrip + Jinja2-Parse + Render mit allen P15/P10/P9/P12-Markern verifiziert). **Live-Browser-Smoke per DevTools-Network-Throttle / Notion-Re-Toggle-Sequenz / Berlin-Datum-Inspektion stays Master-Aufgabe** vor Production-Deploy — `docker compose up --build` notwendig weil Source per `COPY . .` ins Image gebakened. Drei-Commit-Aufteilung über temp-Backup → revert → stepwise-replay → restore-final, weil `library_detail.js` Holistic-Rewrite mit Cross-Sub-Batch-Hunks war (50/50 nach A, 50/50 nach B, 51/51 nach C im Container). Push durch Auto-Mode-Classifier geblockt, Master pushed von Hand. **F-3 strukturell abgeschlossen für `library_detail`**. Verbleibende Sequenz: F-N… → WAVE-CLOSE._

## Steady-State
Cleanup-Welle 2026-05 abgeschlossen mit WAVE-CLOSE 2026-05-11. BACKLOG ist auf Steady-State umgestellt (P0/P1-Inbox plus P3-Reminder, keine sequenzielle Roadmap mehr — siehe [BACKLOG.md](BACKLOG.md)). Neue Sprints werden als Sprint-Prompt-Doc unter [docs/archive/sprint-prompts/](docs/archive/sprint-prompts/) angelegt und als frische Sub-Session ausgeführt. Master macht keine Code-Edits.

## Zuletzt durch

- **R1-A** (Reading-View Foundation `library_detail`, commit `c84e469` — `<pre>`-Monospace-Block ersetzt durch server-rendertem `<article class="reader-view">` mit lesefreundlicher Typografie, neuer Helper `app_pkg/markdown_render.py` als Single Source of Truth für MarkdownIt+pygments+nh3, hidden `<script type="text/markdown" id="content-source">` als Raw-Source-Side-Channel für die 4 JS-Lese-Stellen in `library_detail.js` (Copy/Download/Notion-Send). `script_safe`-Jinja-Filter mit `markupsafe.Markup`-Return gegen Auto-Escape im raw-text-Element plus case-insensitiver `</script`-Patch. Reader-View-CSS-Block nutzt `--nm-*`-Token (Dark-Mode-frei) plus Tailwind-Preflight-Bullets-Patch. 4 Conversion-Types live im Browser gesmoked, Encoding-Smoke clean, JS-Reuse-Smoke byte-exakt verifiziert, Empty-State sauber. Pytest **71/71 grün**. Memory-Eintrag `feedback_jinja_auto_escape_markup_filter.md` angelegt. Light-Mode-Verifikation steht als P3 in BACKLOG.) — 2026-05-25.
- **MAC1** (Mac-Dev-Setup für Apple Silicon — `docker-compose.override.yml` mit `platform: linux/amd64` für `markdown-converter` und `worker`, `notion-mcp-net` von external auf lokale Bridge, `restart: no`; `docs/archive/sprint-prompts/mac-dev-setup.md` mit Wiederherstellungs-Anleitung. Build-Zeit Rosetta 6:53 min / Retry via Layer-Cache 5:27 min. Playwright MD→PDF unter Rosetta M4 in 3 Sekunden. Pytest **71/71 grün** im Container. Server-Setup auf Mintbox bit-identisch (Override-File gitignored). Zwei Follow-ups in P1: MAC1-FOLLOWUP-A pip-timeout XS + MAC1-FOLLOWUP-B Image-Slim M.) — 2026-05-25.
- **HYG2** (Hygiene-Welle 2 Sammel-Pass, 6 Aktive-Reminder-Items in drei Sub-Batches A/B/C — drei Code-Commits `00969fb` library_detail Polish (BT7 textarea-escape + BT8 window.open-noopener + Opacity-Übergang), `766ebab` Cross-Feature-Hygiene (`showToast` aria-live + 3 DE-Microcopy-Strings in markdown.py), `c551d51` Audio-UX (`getUserMedia` vor WS-Handshake plus Stream-Track-End-Handler plus Stream-Release-Cleanup in `socket.onclose`/`socket.onerror`). Pytest-Erwartung 71/71 unverändert, Master prüft Container-Pytest via SSH. `escapeHtml`-Helper lokal in `renderNotionFields` per Memory `feedback_helper_reuse_design_choice.md`. Cross-Feature-`showToast`-Verifikation 8 Call-Sites in 4 Files alle Helper-konform. Toast-Level-Differenzierung analog F-3.3 P8. HYG2-Live-Mic-Master-Smoke bleibt als XS Master-Aufgabe in „Aktive Reminder" (drei Sequenzen S1 Permission-Grant / S2 Permission-Block / S3 Mid-Recording-Track-End). Items 7-8 aus Sprint-Liste (P8-F5-Smoke + F6-IMPL P2/P3/P11) out-of-scope per Sprint-Annotation 2 — bleiben Master-Punch-Liste. SMB-Permission-Flake bei Phase-3-Doc-Commits (Single-File-Commits failing mit „insufficient permission for adding an object", Multi-File-Bundles funktionierten). Memory-Disposition keine neue Eintrag — Permission-Prompt-vor-async-State-Init ist Audio-Live-Tab-spezifisch, keine übertragbare Pattern-Lehre) — 2026-05-11.
- **WAVE-CLOSE** (Strukturelles Closing der Cleanup-Welle — `docs/cleanup_plan.md`-Header auf „fully closed 2026-05-11" mit Wellen-Sequenz-Zusammenfassung und Test-Anzahl-Statistik 36→71/71 grün, `OVERSEER_HANDOFF.md` via `git mv` nach `docs/archive/OVERSEER_HANDOFF_2026-05-03.md` archiviert plus Archiv-Marker, CLAUDE.md-„Historical"-Sektion mit UX-Cascade-F-3-bis-F-6-Methodik-Eintrag erweitert (Geschwister-/Schwester-Feature-Hebel-Methodik + Methodik-Lehren aus feedback-Memos + Output-Konvention) plus Pfad-Update OVERSEER_HANDOFF auf neuen Archiv-Pfad, BACKLOG-Strukturwechsel von „In Sequenz" zu „Steady-State" mit zwei P3-Sub-Sektionen (Aktive Reminder 8 Items vs. Deferred F-N-Wellen 3 Items), STATUS.md Sequenz-Plan zu Steady-State umetikettiert. P3-Hygiene: entfernt 2 (F-2.1-Doc-Korrektur in F-4.1 absorbiert, 2 EN-Strings in library.js in F-6-IMPL absorbiert), behalten 8, deferred 3 (mermaid_converter und login neu, Playwright bestand). Kein Code-Touch, kein Test-Touch — fünf Doc-Files) — 2026-05-11.
- **F6-IMPL** (F-6 Implementation `library` List-View, 14 Patterns in 3 Sub-Batches A/B/C mit BT1/BT2/BT3/BT4 alle mit-gelöst — drei Commits `8049d3e` Sub-Batch A Silent-Failure-Familie + Cross-Feature-H4 P1–P6 + P13, `741794a` Sub-Batch B List-View-State P7/P8, `ca41270` Sub-Batch C Polish-Long-Tail P9–P14. Pytest **71/71 grün** im Container (66 baseline + 5 neu). 1 neuer Jinja2-Filter `format_card_datetime` in `app_pkg/__init__.py`, 1 neuer Server-Helper `pagination_args` in `app_pkg/library.py`. `data-content`-Embed-Default-Wahl gefolgt für P2 trotz Grenzbereich-Content-Größen. Drei 🔥🔥-Pflicht-Live-Master-Smoke-Patterns Master-ausstehend (P2/P3/P11). F-3.3-Übernahme-Disziplin durchgezogen, F-6 strukturell abgeschlossen für `library` List-View) — 2026-05-11.
- **F6-PATTERNS** (Patterns + Microcopy `library` List-View, Stufe 3 der Duan-Kaskade analog F-1.3 / F-2.3 / F-3.3 / F-4.3 / F-5.3 mit Geschwister-Feature-Pattern-Übernahme aus F-3.3; 14 Pattern-Blöcke P1–P14 aus 17 Findings + 4 BTs, Konsolidierungs-Quote 18% mit drei Konsolidaten, F-3-Konvergenz-Quote 36%, Aufwand-Verteilung XS 8 / S 5 / M 1 / L 0, 3 Sub-Batches A/B/C vorgeschlagen, 8× 🔥 Smoke-Pflicht davon 3× 🔥🔥 Pflicht-Live-Master-Smoke; Patterns-Doc [docs/ui_patterns_library_list_2026-05.md](docs/ui_patterns_library_list_2026-05.md), commit `d920822`) — 2026-05-11.
- **F6-REVIEW** (UX-Heuristik-Review `library` List-View, Stufe 2 mit Geschwister-Feature-Hebel zu F-3 `library_detail`; 17 Heuristik-Findings F1–F17 plus 4 finding-linked Bug-Tickets, Sev-Verteilung 0/6/3/8, Cross-Feature-H4-Finding-Quote 47%, methodische Lehre `feedback_helper_reuse_design_choice.md` als Memory-Eintrag verankert; Findings-Doc [docs/ui_findings_library_list_2026-05.md](docs/ui_findings_library_list_2026-05.md)) — 2026-05-10.
- **F6-PICK** (UX-Inventur `library` List-View, Stufe 1; 21 echte interaktive Elemente in 30 Tabellenzeilen, F-3-Korrespondenz-Verteilung 6 direkt / 2 teil / 3 bereits-erfüllt / 4 nicht-anwendbar, Helper-Reuse-Master-Fokus-Antwort `saveViewState`/`confirmInPlace` beide single-call-site, 16 separate Befunde mit Code-Ankern; Inventur-Doc [docs/ui_inventory_library_list_2026-05.md](docs/ui_inventory_library_list_2026-05.md)) — 2026-05-10.
- **F5-IMPL** (F-5 Implementation `markdown_converter`, 13 Patterns in Sub-Batches A+B Master-bundled commit `07e9aa6` und Sub-Batch C commit `9e6999c`, BT1/BT2 mit-gelöst plus BT3/BT5 mit-gefoldet plus BT4 out-of-scope, 1 neuer Helper `saveViewState`/`loadViewState` in `_utils.js`, Pytest **66/66 grün** im Container, Live-Master-Smoke clean für P5+P7, P8-Master-Smoke ausstehend, F-5 strukturell abgeschlossen für `markdown_converter`) — 2026-05-10.
- **F5-PATTERNS** (Patterns + Microcopy `markdown_converter`, Stufe 3 mit Schwester-Feature-Pattern-Übernahme aus F-1.3; 13 Patterns P1–P13, Konsolidierungs-Quote 13%, 3 Sub-Batches A/B/C, 8× 🔥 Smoke-Pflicht davon 3× 🔥🔥 Pflicht-Live-Master-Smoke; Patterns-Doc [docs/ui_patterns_markdown_converter_2026-05.md](docs/ui_patterns_markdown_converter_2026-05.md), commit `06f74b5`) — 2026-05-10.
- **F5-REVIEW** (Heuristik-Review `markdown_converter`, Stufe 2 der Duan-Kaskade — analog F-1.2 / F-2.2 / F-3.2 / F-4.2; **Schwester-Feature zu `document_converter`** also F-1.2 als primäre Heuristik-Klassifikations-Quelle; 15 Findings mit Sev-Verteilung 0/3/6/6 plus 5 Bug-Tickets BT1–BT5 (BT1 PDF-Gen-Re-Render-Context ↔ F3, BT2 Sample-Merge-Template ↔ F6, BT3 updateStyle-doppelt pure, BT4 Inline-`<style>` pure, BT5 Tot-`<link>` pure); **Pattern-Konvergenz-Quote 86%** (12 von 14 anwendbaren F-1-Patterns; 2 nicht-anwendbar P2 Result-Area + P5 Drag-Active-Highlight) — Schwester-Feature-Inversion zu F-4.2's 0% bestätigt im Master-Erwartungs-Bereich 80-90%; **Cross-Feature-H4-Findings 47%** (7 von 15 mit F-1-Pattern-Korrespondenz: F1+F2 P4-Save-`alert()`, F4 P1-Empty-Submit-teil, F7 P9-Submit-Loading-teil, F10 P12-File-Info-teil, F13 P13-Iframe-a11y, F14 P7-Auto-Dismiss); **Heuristik-Verteilung 5-2-4-4 H1/H4/H6/H9** mit H1 dominiert wegen Visibility-Lücken (Empty-Submit-Roundtrip, File-Info-fehlt, Submit-Loading-kurz, Sample-Merge), H6 deutlich wegen Reader-Mode-Lifecycle-Lücken (F9 Reader-Mode-Persistenz, F11 Width-Buttons-Initial, F12 Esc-Document-global, F13 Iframe-a11y), H9 deutlich wegen Error-Pfade (F2 Save-Recovery, F3 PDF-Gen, F5 Theme-Fetch, F15 markdown-it-CDN); **Befund 16 PDF-Gen-Error-Re-Render als Finding + Bug-Ticket BT1 für F5-IMPL-Mit-Fix disponiert** mit primärer Heuristik H9 und Severity 3 ⚠️ code-only — 1:1 Master-Annotation gefolgt, kein Hot-Fix-Sprint, kein Code-Fix in der Doc, F5-PATTERNS greift "PDF-Gen-Error-Recovery" als eigenes Pattern mit Microcopy-Aspekt auf; **vier Schwerpunkt-Cluster** (1) Cross-Feature-H4-Helper-Reuse zu F-1 mit F1+F2+F14+F13+F10 — günstigster Konvergenz-Pfad mit existing-helper-reuse XS-S Aufwand und Daily-Usage-Schmerz mittel weil Save-Library-Pfad häufiger Endpoint, (2) Reader-Mode-State und Visual-Layout F9+F11+F12+F8 — H6/H4-dominiert mit Daily-Usage-Schmerz mittel-hoch wegen F9 (Reader-Mode-On/Off-Persistenz täglich getroffen wenn Oliver in Reader-Mode liest), (3) Error-Recovery-Pfade F3+F5+F6+F2 — H9-dominiert alle ⚠️ code-only mit F5-IMPL-Smoke-Pflicht analog F-3-IMPL und F-4-IMPL, (4) Async-Pre-Check und Loading-Visibility F4+F7 — H1 niedriger Daily-Usage-Schmerz; **8 Findings mit `⚠️ code-only`-Marker** F3/F5/F6/F7/F8/F11/F12/F15 mit Master-Walkthrough-Empfehlung 15-30 Min vor F5-PATTERNS für die Sev-3-Sev-2-`⚠️`-Findings — F3 PDF-Gen-Error-Trigger forcieren via injizierten Style-Crash für secondary-500-Verifikation, F8 globaler Theme-Toggle ↔ Reader-Mode-Dark-Toggle Reihenfolge-Test mit DevTools-Element-Inspector für `data-global-theme`/`data-theme`, F6 nur sichtbar nach BT1-Fix daher post-F5-IMPL-Smoke-pflichtig, F5 Theme-CSS-Fetch via DevTools-Network-404-Block 5-Min-Verifikation, F9 code-evident kurze Verifikation des Reload-startet-immer-in-non-Reader-Verhaltens günstig; **Severity-Abweichungen begründet** mit Daily-Usage-Schmerz-Gewichtung: F4 (P1 Empty-Submit) F-1.2 Sev 4 → Sev 2 weil Server-Flash-Roundtrip Feedback liefert (kein silent fail wie F-1's `display:none`-Anker), F14 (P7 Auto-Dismiss) F-1.2 Sev 2 → Sev 1 weil Close-`×` schon im Template inline da nur Auto-Dismiss-Timer fehlt, F10 (P12 File-Info) F-1.2 H4 Sev 1 → H1 Sev 1 weil Display insgesamt fehlt (Schmerz primär System-Status-fehlt nicht Konvention-Bruch); **5 bereits konvergente F-1-Patterns** als positives Inventar gelistet kein Finding-Bedarf — P3 Save-Btn-`.saved`-Reset durch setTimeout-Reset-Routine ([static/js/markdown_converter.js:135-139](static/js/markdown_converter.js#L135-L139)), P6 Format-Label-Single-Source-of-Truth durch SEC-Sprint-F-006 plus Mini-P6.1-Hinweis für sichtbares Label-Format-Hint in F5-PATTERNS, P8 Frontend-Vorab-Check unsupported file mit `fileExtensionAllowed()`+`showAlert(... 'danger', 'Dateiformat nicht unterstützt. Erlaubt: .md, .markdown.')` (DE-Microcopy bereits da als einziger DE-String der Page!), P10 Result-Area-scrollIntoView durch Architektur-Konvergenz (Split-Pane-Layout statt Result-Area+Scroll), P11 Drop-Zone-Keyboard-Pfad durch Architektur-Konvergenz (visible native `<input type="file">` out-of-the-box keyboard-erreichbar) — F-5.1's „teil-bzw-bereits-erfüllt" für P10 und P11 auf „bereits konvergent" verschärft weil markdown_converter-Architektur das F-1-Problem strukturell vermeidet kein UX-Schmerz mehr verbleibend; **Disposition-Abweichungen von F-5.1 begründet**: Befund 6 (`updateStyle()`-doppelt) zu Bug-only BT3 weil zweiter Fetch vom Browser-Cache absorbiert reine Code-Hygiene, Befund 14 (Inline-`<style>`) zu Bug-only BT4 weil reine Architektur-Drift CSP-strict im Single-User-LAN-Setup nicht aktiv, Befund 15 (Tot-`<link>`) zu Bug-only BT5 weil leeres Tag keinen Request auslöst reine Code-Aufräum, Befund 5 (Kein File-Info-Display) H4 → H1 umklassifiziert weil Display insgesamt fehlt nicht MB-Konvention-Bruch wie F-1.2 F17; **2 Inventur-Befunde aus F-5.2 ausgenommen** Befund 8 (EN-Flash-Strings) für DE-Pass-Welle in F5-PATTERNS sprint-konstitutiver Hinweis, Befund 19 (konformer Codestyle window.PageData) ohne UX-H-Aspekt CLAUDE.md-Pattern eingehalten; Phase-2-Konsistenz-Check ergab eine kleine Korrektur (zwei konkrete deutsche Microcopy-Strings in Cluster-3- und Cluster-4-Fix-Pfaden entfernt analog F-3.2-Disziplin); Findings-Doc [docs/ui_findings_markdown_converter_2026-05.md](docs/ui_findings_markdown_converter_2026-05.md)) — 2026-05-10.
- **F5-PICK** (UX-Inventur `markdown_converter`, Stufe 1 der Duan-Kaskade — analog F-1.1 / F-2.1 / F-3.1 / F-4.1; **Schwester-Feature zu `document_converter`** also F-1.1 als primäre Strukturvorlage; 32 echte interaktive Elemente in 44 Tabellenzeilen; **F-1-Korrespondenz-Spalte neu eingeführt** mit Übersicht am Doc-Ende — 4 F-1-Patterns direkt übertragbar (P4 Save-Failure-`alert()`, P7 Alert-Auto-Dismiss, P13 a11y-Annotations Iframe, P14 Download-Success-Toast) / 8 teil-übertragbar oder bereits-erfüllt (P1 Empty-Submit, P3 Save-Btn-Stale-Saved bereits-erfüllt durch setTimeout-Reset, P6 Format-Label durch SEC-Sprint, P8 Frontend-Vorab-Check durch SEC-Sprint mit DE-Microcopy, P9 Submit-Loading bereits-erfüllt-teils, P10 Result-Scroll, P11 Drop-Zone-Keyboard bereits-erfüllt durch native visible File-Input, P12 KB/MB-Filename-Fallback) / 2 nicht-anwendbar (P2 Result-Area, P5 Drag-Active-Highlight) → **86% Cross-Feature-H4-Quote bestätigt Sprint-Erwartung 80%+**; 10 markdown-spezifische Befunde ohne F-1-Korrespondent (Befund 4 Sample-Text-Merge-Bug im Template, Befund 6 `updateStyle()` doppelt beim Page-Load, Befund 9 Reader-Mode-On/Off-State nicht persistiert, Befund 10 Width-Buttons-Initial-Active-State, Befund 11 zwei voneinander unabhängige Dark-Modes global vs. Reader-only, Befund 14 Inline-`<style>` im Template, Befund 15 totes `<link id="preview-style" href="">`, Befund 16 PDF-Gen-Error-Re-Render unvollständiger Context **akut**, Befund 17 Esc-Key-Listener Document-global, Befund 18 markdown-it CDN-Dependency); ~5 fehlende States (Iframe-Loading-State während markdown-it-Render und Theme-CSS-Fetch, Iframe `tabindex`/`role` für a11y, Submit-Loading nach CSRF-Refresh-Roundtrip nur kurz sichtbar weil Text-Restore vor `form.submit()`, Theme-CSS-Fetch-Failure silent mit nur `console.error`, kein File-Info-Display nach File-Auswahl); 6 Code↔live-Divergenzen-Verdachts-Items; **19 separate Befunde** (Befund 19 ist konformer-Codestyle-Notiz `window.PageData`); **Befund 16 als akut-flag-Kandidat** flag im Bericht — `convert_markdown`-Error-Branch [app_pkg/markdown.py:246](app_pkg/markdown.py#L246) ruft `render_template('markdown_converter.html', markdown_text=markdown_text)` ohne `themes`/`accepted_extensions`/`accepted_extensions_accept`-Context, Template iteriert `{% for theme in themes %}` → bei Flask's Default-`Undefined` `UndefinedError` → secondary 500-Page statt sichtbarem flash-Banner; einziger real auftretender Crash-Pfad ist PDF-Gen-Failure (Empty-Markdown- und Invalid-Filename-Pfade machen `redirect()`); kein Datenverlust-Risiko aber freundliches Error-Banner verschwindet hinter generischer 500-Page; Behebung trivial (`redirect(url_for('markdown_converter'))` statt `render_template` im Error-Branch), Master entscheidet ob Hot-Fix-Sprint vorgezogen wird; 7 Live-Walkthrough-Lücken (Iframe-Flicker zwischen `srcdoc`-Updates, Reader-Mode-Visual-Layout-Wechsel, Width-Buttons-Initial-Active vor erstem Reader-Toggle, PDF-Download-Verhalten nach Form-Submit ob Page stehen bleibt, Browser-Native-Validation-Popover für leeres `output_filename`, Theme-CSS-Fetch-Latenz-Sichtbarkeit, CSRF-Refresh-vs-Re-Submit-Race); **Helper-Reuse-Spuren-Sektion neu eingeführt** als Phase-2-Konsistenz-Check-Item — `showAlert` ✓ wird im Submit-Handler-File-Extension-Pfad genutzt mit DE-Microcopy als einzigem DE-String der Page (alle anderen User-Strings sind EN, DE-Pass kommt in F5-PATTERNS), drei Browser-`alert()`-Calls in `saveMarkdownToLibrary` ✗ statt In-Page-Banner (siehe Befund 1, identisch zu F-1-Pattern P4), `safeJSON` ✗ nicht genutzt (Login-Redirect-Detection würde fehlen), `showToast` ✗ nicht genutzt, `formatFileSize` ✗ nicht genutzt mangels File-Info-Display; F-006/F-002/F-007/F-008/F-011 als Header-Verweis dokumentiert nicht in Befunden (Bereits-durch-frühere-Sprints-erfüllt-Disziplin); **Code-only-Inventur ohne Live-Walkthrough** weil Schwester-Feature-Effizienz aus F-1.1 ausgenutzt; Phase-2-Konsistenz-Check ergab drei Disziplin-Verstöße zu beheben (Severity-Bewertungen in Befunden 10/11/12/13/14/15/17 entfernt, Pattern-Vorschlags-Sprache in Befunden 1/2/5 zu „Disposition: F5-REVIEW" zurückgekürzt, Code-Anker in Befunden 2/5/13 ergänzt) plus CDN-Dependency als Befund 18 ergänzt; Inventur-Doc [docs/ui_inventory_markdown_converter_2026-05.md](docs/ui_inventory_markdown_converter_2026-05.md)) — 2026-05-10.
- **F4-IMPL-B** (F-4 Implementation `podcast-flow` Cluster II + III, 10 Patterns P3–P12 in einem Sweep ohne Sub-Batch-Split — JS/HTML/CSS-Touches im Podcast-Block waren stark verkoppelt (Stage-Indicator + Polling-Loop + Reattach + Loading-State teilen Helpers); Apply-Reihenfolge Backend-zuerst (P5 Status-Mapping in `app_pkg/podcasts.py`, P9 Allowlist plus `MAX_RAW_TEXT_CHARS=50000`-Konstante in `app_pkg/config.py`, P3 `update_job_stage` in `tasks.py` + Inline-Duplikat `_set_stage` in `services/gemini/tts.py` wegen Import-Cycle `tasks → services → tts → tasks`) → CSS plus HTML-Markup (P3 stage-indicator-Mountpoint, P7 spinner-spans + `.is-loading`-Klasse + Reuse `mic-button-spin`-Keyframes 0.8s linear, P9 char-counter mit grau→gelb→rot, P11 readonly-Style, P12 `aria-disabled`-Tab-CSS) → Frontend-JS (alle 10 Touches in `static/js/audio_converter.js`) → Tests (5 neu plus 2 Contract-Updates auf queued/started) — Pytest 60→**65 grün** im Mintbox-Container exakt im Sprint-Zielband 63-65; **P3-Mechanik Plan A `job.meta` + `job.save_meta()`** gewählt — Live-Smoke zeigte keine RQ-Worker-Subprocess-Sync-Latenz, Plan-B-Redis-Key bleibt als dokumentierter Fallback ungenutzt; Stage-Marker an: `filtering` / `chunking` / `tts_chunk` mit `chunk_current`+`chunk_total` / `concatenating` / `finalizing`; Backend `/podcast-status` reicht meta.stage durch wenn started, P5 differenziert `queued` vs. `started`, P8 behandelt `deferred`/`scheduled` explizit; Frontend rendert Sub-Caption + Linear-Progress-Bar (nur bei `tts_chunk`) mit aria-live=polite; **P4 Browser-Reload-Recovery** via `localStorage.podcast.activeJobId` plus IIFE `reattachActiveJobOnLoad` auf `DOMContentLoaded` mit sofortigem In-Flight-UI-Render (kein 2-Sek-Tick-Wait) — Probe `/podcast-status` und je Status entweder Polling re-attachen mit Info-Banner `Laufende Generierung wiederhergestellt.` (auto-dismiss 4 s) oder finaler State-Banner; **P7 Loading-State-Konvergenz** via `setBtnLoading`-Helper + `.is-loading`-Klasse triggert F16-Spinner-Animation auf existierendem Generate-Btn-Icon (Reuse statt neue Keyframes); **P8 Polling-Robustheit** via dedizierter `pollPodcastStatus(jobId)` mit `r.ok`-Check vor `safeJSON`, 404-Branch (Job-TTL `Job-Status nicht mehr verfügbar`), 5xx-Retry mit Counter (max 3) → Danger-Banner `Status-Server reagiert nicht`, Status-Allowlist `POLLING_KNOWN_STATES` Set guards future RQ-Drift, Frontend-Timeout 10 min mit persistentem Info-Banner; **P11 Skript-Textarea-Readonly** via `setPodcastScriptReadonly` analog F-2.3 P10 plus Status-Hint-Zeile; **P10 Skript-Format-Hilfe** via expandierte Hint-Zeile (Format + Sprecher-Map Kate/Max/Default-Kore + Skip-Branch `#`/`**`) plus `preParseScriptForHint()` auf blur-Event mit Zeilen-Hinweis-Banner bei Empty-`dialogue[]` (8 s autoDismiss); **P12 Tab-Disabled** via aria-disabled-Guard im Click-Handler + CSS `pointer-events: none` plus `opacity: 0.5` plus info-Toast `Service nicht konfiguriert`; **P6 Download-Toast-Guard** via `showToast` raus zugunsten info-`showAlert` `Download gestartet. Falls nichts passiert, Pop-Up-Blocker prüfen.`; **BT4 Audio-Blob-URL-Revoke gefolded** weil Download-Pfad-Touch nahegelegen (Patterns-Doc empfahl explizit P2-Verzahnung) — `lastPodcastBlobUrl` getrackt, `URL.revokeObjectURL` auf `pagehide` plus vor jedem `URL.createObjectURL`-Aufruf; **`confirmInPlace`-Helper-Disposition** lokal in `audio_converter.js` belassen weil Cancel-Confirm-State-Machine bleibt einzige Call-Site (F4-IMPL-B fand keine zweite trotz Suche); **Live-Smoke clean** für alle drei 🔥-Pflicht-Patterns — P4 Browser-Reload-Recovery via F5 mid-Job (Polling-Loop reattacht, Stage-Indicator läuft weiter, kein Leer-Tick), P7 Spinner-Animations-Vergleich (Generate-Btn-Icons rotieren mit gleichem Tempo wie Mic-Spinner), P8 Network-Drop via DevTools-Offline-Throttle (Danger-Banner nach 3-Retry-Budget); **Cluster-I-Regression-Smoke** (Cancel SIGKILLt Worker weiter analog `killed horse pid …`, kein orphan WAV im OUTPUT_DIR); **P3 Stage-Progress-Smoke** (Multi-Chunk-Job mit sauberen Übergängen `Skript wird gefiltert …` → `Wird aufgeteilt …` → `Wird gesprochen … (Chunk X/N)` mit Progress-Bar-Füllung → `Audio wird zusammengefügt …` → `Datei wird abgeschlossen …`); 9 Files geändert (`app_pkg/audio.py` + `app_pkg/config.py` + `app_pkg/podcasts.py` + `services/gemini/tts.py` + `static/css/style.css` + `static/js/audio_converter.js` + `tasks.py` + `templates/audio_converter.html` + `tests/test_podcasts.py`), 744/-64 Zeilen Diff; Ein Commit für F4-IMPL-B, direkt auf `main`, Push erfolgreich (kein Auto-Mode-Classifier-Block diesmal); **F-4 strukturell abgeschlossen** für `podcast-flow`, Verbleibende Sequenz F-N… → WAVE-CLOSE) — 2026-05-10, commit `3ef7f9e`.
- **F4-IMPL-A** (F-4 Implementation `podcast-flow` Cluster I, P1 Cancel-Mechanik + P2 Orphan-File-Cleanup atomic in einem Commit; **RQ-Cancel-Option B** gewählt nach `rq.command`-Source-Reading rq 2.8.0 — `send_stop_job_command` publisht via PubSub plus Worker-Subscriber ruft `worker.kill_horse()` ruft `os.killpg(getpgid(horse_pid), SIGKILL)` auf das Horse-Subprozess-Group, also echte mid-execution-SIGKILL-Mechanik kein Status-Flip; Cooperative-Redis-Key-Polls (Option A) übersprungen weil mid-TTS-Call (30+s pro Chunk) sowieso ohne Wirkung; deterministischer Output-Filename `OUTPUT_DIR/{job.id}.wav` aus `tasks.py` via `rq.get_current_job()`; neuer `POST /podcast-cancel/<job_id>`-Endpoint mit Status-Branch und Path.is_relative_to-guarded Cleanup; `/podcast-status` collapses `stopped`/`canceled`/`failed-mit-cancelled-meta` → `cancelled`; Frontend-Lüge raus mit `confirmInPlace`-Inline-State-Machine `idle → confirm-pending → cancelling`; **Live-Smoke Cancel-Disk-Forensik 2026-05-10 07:19:46 (Job `00fa3dbd-b701-4ff0-93b9-97ff6c3182d2`)** — Worker-Stop-Latenz 15 s vs. 1:12 Min vorher = 4.8x Reduktion, Disk-Output 0 Bytes vs. 11.7 MB, TTS-Token-Verbrauch praktisch 0; +9 Tests, pytest 60/60 grün; Push durch Auto-Mode-Classifier geblockt, Master pushed von Hand) — 2026-05-10, commit `e6e9443`.
- **F4-PATTERNS** (Patterns + Microcopy `podcast-flow`, Stufe 3 der Duan-Kaskade — analog F-1.3 / F-2.3 / F-3.3; 12 Pattern-Blöcke P1–P12 aus 16 Findings + 3 finding-linked Bug-Tickets BT1–BT3 (BT4 explizit out-of-scope mit P2-Verzahnungs-Hinweis); Konsolidierungs-Quote 25% mit vier Konsolidaten (P1 F1+F2 Cancel-Familie, P7 F7+F16 Loading-State-Konvergenz, P8 F8+F15 Polling-Robustheit, P9 F13+F14 Backend-Validation) — im Master-Erwartungs-Bereich 15–25%; Aufwand-Verteilung XS 4 / S 4 / M 3 / L 1 — höchster Aufwand L (P1 Cancel-Mechanik mit RQ-cooperative-cancel-Recherche-Punkt + Worker-Stop + Frontend-Lüge-Fix + Confirmation-Dialog; einziger L-Pattern in der Cleanup-Sequenz bisher); drei Cluster vorgeschlagen Cluster I Cancel-und-Cleanup-Recovery 2 Patterns ✅ live-verifiziert / Cluster II Async-State-Visibility 5 Patterns mit zwei Smoke-Pflicht / Cluster III Defensiv-Robustheit-und-Skript-Hilfe 5 Patterns mit einem Smoke-Pflicht plus Zwei-Sprint-Split-Empfehlung als Fallback wegen P1 L-Aufwand und RQ-Recherche-Risiko (Cluster I als eigener F4-IMPL-A-Sprint, Cluster II+III als F4-IMPL-B-Sprint); Top-5-Quick-Wins-Score-Range 5.0–10.0 — strukturell niedriger als F-3.3 (7.5–15.0) weil P1 Sev 4 auf L-Aufwand liegt also keine Sev-4-XS-Wins möglich, P5 queued-vs-started + P6 Download-Toast-Guard + P12 Tab-Disabled-Konsistenz alle 10.0 mit XS-Aufwand, P4 Browser-Reload-LocalStorage 7.5 mit S-Aufwand und Smoke-Pflicht, P11 Skript-Textarea-Readonly 5.0; 2 Live-verifizierte Patterns (P1 F1+F2 Cancel-Lüge, P2 F4 File-Cleanup) durch Master-Walkthrough 2026-05-10 Cancel-Disk-Forensik (Job 9bf48e0a — Worker lief 1:12 Min nach Cancel weiter, WAV 11.7 MB im Volume, TTS-Token vollständig verbraucht); 3 Smoke-Pflicht-Patterns (P4 F5 Browser-Reload mit F-4.1 Test-Anleitung 4, P7 F16 Spinner-Animation visueller Vergleich Mic-Spinner vs. Generate-Btn-Icon, P8 F8 Polling-Robustheit mit F-4.1 Test-Anleitung 5 Network-Drop) für die verbleibenden ⚠️ code-only-Findings; Helper-Reuse-Disziplin gewahrt mit existierenden `_utils.js`-Helpern (showAlert/safeJSON/showToast) und einem Helper-Vorschlag `confirmInPlace(triggerEl, options)` aus P1 Cancel-Confirmation in dedizierter Sektion am Doc-Ende — nicht still mit-angelegt, F4-IMPL-Sub-Thread entscheidet beim Cluster-Schnitt; Architektur-Hebel-Disziplin: P3 Stage-Progress nutzt explizit den Worker-Logger-Lines→job.meta-Hebel aus F-4.1 Befund 4 (50+ existierende Logger-Lines in services/gemini/synthesis.py + services/gemini/tts.py per-chunk-Loop ohne neue Telemetrie-Investition) mit Plan-B-Sackgassen-Vermerk auf Redis-Key `podcast:stage:{job_id}` falls RQ-Worker-Subprocess-Sync-Latenz Plan A scheitern lässt; P1 RQ-cooperative-cancel-Recherche-Punkt (rq 2.x cooperative cancel via redis-key-poll vs. `Worker.kill_horse()`-Emulation) transparent an F4-IMPL delegiert in eigener Architektur-Hebel-Bedenken-Sektion; Patterns-Doc [docs/ui_patterns_podcast_flow_2026-05.md](docs/ui_patterns_podcast_flow_2026-05.md)) — 2026-05-10.
- **F4-REVIEW** (Heuristik-Review `podcast-flow`, Stufe 2 der Duan-Kaskade — analog F-1.2 / F-2.2 / F-3.2; 16 Findings mit Sev-Verteilung 2/3/6/5 plus 4 Bug-Tickets BT1–BT4 davon 3 finding-linked und 1 pure ohne UX-H-Aspekt (BT4 Audio-Blob-URL-Revoke); **Cross-Feature-H4-Quote 0%** drastisch unter Master-Erwartung 15-25% mit ehrlicher Begründung in eigener Sektion (F-4.1 Befund 20 dokumentiert saubere F-2-Helper-Konvergenz im audio_converter.js-Podcast-Block — kein alert()-Pfad, keine drei konkurrierenden Error-UI-Patterns; verbleibende H4-Findings F7/F9/F16 sind interner Natur nicht Cross-Feature; Sub-Thread hat **keine künstlichen H4-Findings konstruiert** wie Master-Annotation explizit verlangt); **Heuristik-Verteilung gleichgewichtig 6-2-2-6 H1/H4/H6/H9** wie Master vorhergesagt — H1 plus H9 dominieren wegen 5/6 unzureichender Async-State-Klassen mit Cancel-Lüge-Finding-Paar F1+F2 das die H1+H9-Doppel-Achse spannt; **Async-spezifisch 10 von 16 Findings 62.5%** — eigene Async-Heuristik-Sub-Sektion mit State-Klassen-Mapping (cancelled/stage-progress/queued+started/Polling-Edge-Cases/Browser-Reload/File-Cleanup/Download-Toast/Loading-Pattern) hilft F4-PATTERNS bei der Pattern-Strukturierung nach Async-State-Achse; **vier Schwerpunkt-Cluster** (1) Cancel-und-Cleanup-Recovery F1+F2+F4 Sev 3-4 mit Daily-Usage-Schmerz hoch — Cancel-Lüge ist Strukturanalogon zur F-2.1 Drag-Drop-Lüge identische Schmerz-Klasse, (2) Async-State-Visibility F3+F5+F6+F10+F16 H1-dominiert — Stage-Progress-Fehlen zentraler Daily-Usage-Schmerz mit Architektur-Hebel auf existierende Worker-Logger-Lines aus services/gemini/synthesis.py 50+ Lines plus services/gemini/tts.py per-chunk-Loop ohne neue Telemetrie-Investition, (3) Polling-und-Defensiv-Robustheit F8+F13+F14+F15 H9-dominiert kleine Defensiv-Lücken inkl. F-013-Allowlist-Konsistenz-Gap auf /generate-gemini-podcast, (4) Speaker-Format-Hilfe-und-Edit-Verhalten F11+F12 H6 mit Skript-Parser-Fragilität plus Skript-Textarea-readonly-Pattern analog setLiveTextareaReadonly aus F-2; **6 Findings mit `⚠️ code-only`-Marker** F1/F2/F4/F5/F8/F16 mit priorisierter Test-Anleitung-Sequenz aus F-4.1 für Master-Walkthrough-Empfehlung — Cancel-Disk-Forensik via docker logs converter-worker -f Test-Anleitung 3 höchste Priorität wegen Sev-4-Schmerz und BT1-Implementation-Kalibrierung > Re-Download-Pop-Up-Blocker Test-Anleitung 7 für BT3-Fix-Pfad-Wahl > Browser-Reload Test-Anleitung 4 > Multi-Chunk Test-Anleitung 2 für Stage-Indicator-UX-Designentscheidung > Network-Drop Test-Anleitung 5; **Disposition-Abweichungen von F-4.1 begründet**: Befund 11 Blob-URL-Revoke von „nur Finding" zu „nur Bug-Ticket BT4" verschoben weil keine UX-H-Komponente analog F-3.2 BT7+BT8, Befund 17 redundant in BT4 absorbiert (F-4.1 hatte transparent „doppelt aufgenommen" markiert), Befund 20 Helper-Reuse-Meta-Beobachtung in Cross-Feature-H4-Sektion absorbiert als positive Begründung der 0%-Quote, Befund 2 F-2.1-Doc-Korrektur Service-Gate und Befund 3 Legacy /generate-podcast Dead-Code explizit „aus F-4.2 ausgenommen" katalogisiert mit Begründung Hygiene-/Doc-Welle; Findings-Doc [docs/ui_findings_podcast_flow_2026-05.md](docs/ui_findings_podcast_flow_2026-05.md)) — 2026-05-10.
- **F4-PICK** (UX-Inventur `podcast-flow`, Stufe 1 der Duan-Kaskade — analog F-1.1 / F-2.1 / F-3.1; cross-cutting Async-Pipeline-Inventur Frontend (`#podcast-pane` in `audio_converter.html`) + JS (`audio_converter.js`-Podcast-Block) + Backend (`app_pkg/podcasts.py` 6 Routes) + Worker (`tasks.generate_podcast_task` + `services/gemini/tts.py` Chunking-Pipeline); 17 echte interaktive Elemente plus 3 Async-State-Pseudo-Elemente in 30 Tabellen-Zeilen, Async-State-Klasse als 6 separate Spalten queued/started/stage-progress/finished/failed/cancelled; 19 Befunde mit Code-Ankern Disposition 3 Bug-Ticket-Kandidaten + 15 nur-Findings + 1 Pre-Existing-Erwähnung; **Async-State-Coverage 5/6 unzureichend** als strukturelles Hauptproblem (queued+started konflatiert zu `processing`, stage-progress fehlt komplett weil Worker kein job.meta pflegt, cancelled ist Frontend-only); **Befund 9 Cancel-Lüge** strukturanalogon zur F-2.1-Drag-Drop-Lüge (Cancel-Btn stoppt nichts, Worker brennt CPU+Token weiter); **Befund 18 File-Cleanup-vs-Re-Download** als zweites strukturelles Issue (`os.unlink` direkt nach erstem Download macht Re-Download unmöglich); Async-Pipeline-Mapping als ASCII-Sequenz-Diagramm Frontend→Backend→Worker→Polling→Download plus Cancel-Pfad plus Re-Download-Pfad vollständig am Doc-Anfang; 7 separate Live-Walkthrough-Test-Anleitungen für die Async-Übergänge zur Master-Nachreichung in F4-REVIEW (Code-only-Coverage geschätzt 65% wegen Async-Komplexität); Helper-Reuse-Status aus F-2 sauber durchgezogen — `audio_converter.js`-Podcast-Block nutzt safeJSON+showAlert+showToast+confirmIfLong durchgehend, kein `alert()`-Pfad, keine drei konkurrierenden Error-UI-Patterns wie in der ursprünglichen F-2.1; Out-of-scope-Hinweis Architektur-Hebel für F4-PATTERNS: `services/gemini/synthesis.py` hat 50+ Logger-Lines mit Safety-Ratings+Token-Counts die als job.meta-Update-Source für Stage-Progress dienen könnten ohne neue Telemetrie-Investition; Output [docs/ui_inventory_podcast_flow_2026-05.md](docs/ui_inventory_podcast_flow_2026-05.md)) — 2026-05-10.
- **F3-IMPL** (F-3 Implementation `library_detail`, 15 Patterns in 3 Sub-Batches — bewusst aggressiv geschnitten über F-2-Cluster-I-Schmerzgrenze 12, mit Sub-Batch-Mechanik als Schutz; Sub-Batch A 7 Patterns Foundation+Silent-Failure inkl. 2 Smoke-Pflicht (P1/P3) mit DE-Microcopy-Sweep und showAlert-statt-Toast-Konvergenz auf F-1-Pattern, Sub-Batch B 2 Patterns Notion-Form-Stability beide Smoke-Pflicht (P4 State-Preservation mit NOTION_BODY_POOL-Cross-Target-Mapping summary↔description und Snapshot/Restore-um-loadSuggestions plus selectTarget-Switch / P5 lokales Datum via neuer formatDatetimeLocalNow-Helper in _utils.js), Sub-Batch C 6 Patterns Polish+a11y (P2 dirty-indicator mit visibilitychange+beforeunload Flush-on-Hide, P9 Tag-Chip-Visualisierung mit Empty-State, P10 aria-expanded+aria-controls plus gefoldet aus B aria-live-Target-Switch-Hint und Notion-Ziel-wechseln-Tooltip plus neue .sr-only-Utility, P11 als bereits-erfüllt durch base.html:84 dokumentiert kein Code-Touch, P12 server-side file_size Jinja-Filter spiegelt formatFileSize, P13 document.title-Update mit en-dash Library-Suffix); 6 finding-linked BTs gefoldet (BT1 updateField/toggleFavorite, BT2 deleteConversion, BT3 Notion-Form-State, BT4 Datum-lokal, BT5 Toast-Level-pro-Call-Site, BT6 Sidebar-Active conditional); 1 neuer Helper formatDatetimeLocalNow in _utils.js plus 1 neuer Jinja-Filter file_size in app_pkg/__init__.py; +3 Tests (Update-non-dict-body 400 für P1, Delete-non-owned 404 für P3, file_size-Filter-Parität für P12) → pytest 51/51 grün im Mintbox-Container; Smoke-Pflicht-Patterns code-evident verifiziert vor Apply, Live-Browser-Smoke wartet auf Master nach docker-compose-up-build; drei Commits einer pro Sub-Batch via temp-Backup→revert→stepwise-replay→restore-final wegen Cross-Sub-Batch-Hunks in library_detail.js; F-3 strukturell abgeschlossen für library_detail) — 2026-05-09, commits `843574b` / `40dd02e` / `b3e666a`.
- **F3-PATTERNS** (Patterns + Microcopy `library_detail`, Stufe 3 der Duan-Kaskade — analog F-1.3 / F-2.3; 14 Pattern-Blöcke P1–P14 plus struktureller Vorbedingung-Block P15 aus 17 Findings + 6 finding-linked Bug-Tickets; Konsolidierungs-Quote 17,6% mit drei Konsolidaten P1 Auto-Save / P3 Delete / P4 Notion-Wipe — niedriger als F-2.3's ~50%, näher F-1.3's ~26%; Aufwand-Verteilung XS 7 plus P15 = 8 / S 5 / M 2 / L 0; drei Cluster vorgeschlagen Cluster I Foundation+Silent-Failure 7 Patterns / Cluster II Notion-Stability 2 Patterns / Cluster III Polish+a11y 6 Patterns plus Zwei-Cluster-Fallback für Cluster I; Top-5-Quick-Wins-Score-Range 7,5–15,0 mit P5 UTC-Datum 15,0 als höchster Score; 4 Smoke-Pflicht-Patterns P1/P3/P4/P5 für Cluster 1 Silent-Failure und Cluster 2 Notion-Wipe+UTC mit Smoke-Mechanik dokumentiert pro Pattern; Helper-Reuse-Disziplin gewahrt mit zwei Helper-Vorschlägen formatDatetimeLocalNow und renderTagChips in dedizierter Sektion; BT7 textarea-escape und BT8 window.open-noopener als pure Bug-Tickets explizit aus F-3.3-Scope; Patterns-Doc [docs/ui_patterns_library_detail_2026-05.md](docs/ui_patterns_library_detail_2026-05.md)) — 2026-05-09, commit `f010cc7`.
- **F3-REVIEW** (Heuristik-Review `library_detail`, Stufe 2 der Duan-Kaskade — analog F-1.2 / F-2.2; 17 Findings mit Sev-Verteilung 0/8/4/5 plus 8 Bug-Tickets; Cross-Feature-H4-Quote 35% — strukturell niedriger als F-2.2's 41%, weil library_detail keine Drop-Zone / keine `.saved`-Klasse / keine Backend-Whitelist hat; drei Schwerpunkt-Cluster Silent-Failure-Familie + Notion-Side-State-Wipe-und-UTC + Cross-Feature-Helper-Drift; 9 Findings mit ⚠️ code-only-Marker, weil zwischen F3-PICK und F3-REVIEW kein Live-Walkthrough nachgereicht — Master-Walkthrough vor F3-PATTERNS empfohlen für F6/F11/F8/F14, plus 5-Min DevTools-Throttle-Verifikation der Silent-Failure-Familie; zwei F-3.1-Pre-Dispositionen abgewichen (Bem 9 textarea-escape und Bem 14 window.open-noopener fielen zu Bug-only, weil keine UX-H-Komponente); Findings-Doc [docs/ui_findings_library_detail_2026-05.md](docs/ui_findings_library_detail_2026-05.md)) — 2026-05-09, commit `82f6fd8`.
- **F3-PICK** (UX-Inventur `library_detail`, Stufe 1 der Duan-Kaskade — analog F-1.1 / F-2.1; 21 interaktive Elemente kartiert, 18 Befunde mit Code-Ankern, davon 6 Bug-Ticket-Kandidaten; Code-only-Pfad, weil Sub-Thread ohne Browser-Access, daher 10 Live-Walkthrough-Lücken zur Master-Nachreichung gelistet — wichtigster: Sidebar-Active-State auf Detail-Seite vermutet aus Code, nicht verifiziert; Inventur-Doc [docs/ui_inventory_library_detail_2026-05.md](docs/ui_inventory_library_detail_2026-05.md)) — 2026-05-09, commit `e9cfd1a`.
- **CVE-DG** (deepgram-sdk 5.1.0 → 7.1.0 — drei Majors über 6.0/7.0/7.1, **kein Service-Refactor nötig** weil `client.listen.v1.media.transcribe_file` mit allen 8 verwendeten Kwargs surface-stable; die 6.0/7.0-Breakages liegen WebSocket-seitig, das nutzt CONVERTER nicht; `create_temporary_key` returnt nur `self.api_key` ohne SDK-Call; Pre-Flight: `requires_python<4.0,>=3.10` ✓, keine Hard-Pin-Konflikte; pytest 48/48 grün, Container-SDK-Smoke clean, Live-Roundtrip 25.9s → 334 Char + Chunking-Pfad 34.6min → 2 Chunks → 26621 Char) — 2026-05-09, commit `74589e3`.
- **CVE-RQ** (redis-py 5.0.1 → 7.4.0 — redis 6.x übersprungen wegen rq-2.x-Constraint `redis!=6,>=3.5`; rq 1.16.0 → 2.8.0 mit atomic worker.py-Refactor wegen `Connection`-Removal in rq 2.0; während Live-Smoke API-Drift entdeckt: `Job.get_id()` in rq 2.x entfernt, 2-Zeilen-Patch in `app_pkg/podcasts.py` plus 1-Zeile-Sync in `tests/conftest.py` weil MagicMock-Auto-Attribute den Drift in der Test-Suite versteckt hatten; pytest 48/48 grün im Container; Live-Smoke clean inkl. F-001 narrow → 404 und F-001 broad → 500; size mid-sprint von L auf XL hochgestuft) — 2026-05-09, commits `513844e` / `3cfebbe`.
- **CVE-PDF** (unstructured 0.14.5 → 0.18.32 statt 0.22.26 — gecappt weil 0.20.2+ Python ≥3.11 fordert, Container-Base auf 3.10.12; pdfminer.six 20221105 → 20251230 + forced co-bump pdfplumber 0.10.4 → 0.11.9 wegen `pdfplumber 0.10.4` Hard-Pin; Reihenfolge unstructured-zuerst weil unstructured 0.14.5 hard-importierte `PSSyntaxError` aus pdfminer, das in 20251230 entfernt ist; pytest 48/48 grün im Container, Live-Smoke clean über PDF normal + columnar + DOCX + PPTX + DOCX m. Bildern + Tabellen) — 2026-05-09, commits `c821615` / `85f1caf`.
- **CVE-LOW** (Pygments 2.18.0 → 2.20.0 / CVE-2026-4539; requests 2.31.0 → 2.33.0 / 3 CVEs; Flask 3.0.3 → 3.1.3 / CVE-2026-27205, Werkzeug zieht transitiv auf 3.1.6 nach; pytest 48/48 grün, Live-Smoke gegen rebuilt Mintbox-Container clean) — 2026-05-09, commits `fa98b35` / `0698748` / `73a45b9`.
- **HYG** (F-002 Pygments narrow-except, F-007 secure_filename(None) guard, F-008 5 Logging-Sites mit exc_info, F-011 `require_service`-Decorator + DE-Microcopy für 3 Services × 6 Endpoints, F-012 dead `if not file:` raus, F-015 Timeout-Konstanten in `app_pkg/config.py` zentralisiert, F-016 Doppel-Log raus, F-017 `isinstance(data, dict)`-Inline-Check an 6 Stellen; +5 Tests, 48/48 grün) — 2026-05-09.
- **SEC** (F-005 Path-Traversal `Path.is_relative_to`, F-006 markdown Backend-Whitelist, F-013 Input-Allowlists für Deepgram/Google-TTS/Gemini; +5 Tests, 43/43 grün) — 2026-05-09, commit `6a18086`.
- **F-2 Cluster II** (audio_converter Sev 2+1 Patterns P13–P21, F-2 strukturell abgeschlossen) — 2026-05-09.
- **Working-Practice-Bootstrap** (CLAUDE.md slim, STATUS.md, BACKLOG.md als Roadmap, Sprint-Prompt-Template) — 2026-05-09.
- **F-2 Cluster I** (audio_converter UX, Sev 4+3 Patterns P1–P12) — 2026-05-03, Live-Smoke ☑.
- **F-1 Hot-Fix** (`document_converter` Jinja2 Generator-Expression Production-Regression) — 2026-05-03.
- **F-1 strukturell abgeschlossen** (alle 14 Patterns + 3 Bug-Tickets, 6 Cluster) — 2026-05-03.
- **Cleanup-Wave Stages 0–7** (cleanup_plan.md, F-001…F-018 strukturell durch) — 2026-05-02/03.

## Methodik
**Master = Dispatch, Sub-Thread = Execute** (Working-Practice umgestellt 2026-05-09 — siehe CLAUDE.md, Sektion *Working Practice*). Master macht keine Code-Edits; jeder Sprint läuft als separater Sub-Thread mit Sprint-Prompt-Doc.
